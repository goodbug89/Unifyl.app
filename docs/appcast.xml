<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.net/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
    <channel>
        <title>Unifyl Updates</title>
        <link>https://unifyl.app/appcast.xml</link>
        <description>Unifyl — AI-Powered Dual-Pane File Manager for macOS</description>
        <language>en</language>
        <item>
            <title>Version 1.7.2</title>
            <description><![CDATA[
                <h2>Unifyl 1.7.2</h2>
                <h3>Added</h3>
                <ul>
                <li><b>Every language is complete again.</b> The 620 strings that were in no</li>
                </ul>
                <p>  catalogue at all are now translated into all 13 secondary locales — German,</p>
                <p>  French, Spanish, Italian, Portuguese (Brazil), Russian, Turkish, Arabic, Thai,</p>
                <p>  Vietnamese, Japanese, Simplified and Traditional Chinese — 8,060 entries,</p>
                <p>  each checked to carry the same format specifiers as its English source. Every</p>
                <p>  locale holds the same 2,072 keys.</p>
                <ul>
                <li><b>596 more strings now speak Korean.</b> They were in no catalogue at all — not</li>
                </ul>
                <p>  missing a translation, absent as keys — so every locale rendered them in</p>
                <p>  English while a locale-parity check reported all 15 languages in step. Settings</p>
                <p>  ▸ General was the clearest case: the row that chooses the app&#x27;s language was</p>
                <p>  itself labelled &quot;App Language&quot;. Also the connection sheet, Automations,</p>
                <p>  Multi-Rename, Directory Diff, Folder Sync, the theme editor, Advanced Search,</p>
                <p>  backups, the converters, the viewers, tab and header menus, and the panel&#x27;s own</p>
                <p>  status line. The other 13 languages fall back to English until they are</p>
                <p>  translated, as they already do for the rest of the catalogue.</p>
                <ul>
                <li><b>The app answers in Korean where it used to answer in English.</b> Go to Path,</li>
                </ul>
                <p>  the PDF overwrite and replace-failed dialogs, the &quot;LibreOffice required&quot;</p>
                <p>  prompt, the duplicate-cleanup and natural-language partial-failure reports,</p>
                <p>  File ▸ New Window, Unifyl ▸ Check for Updates…, and the overwrite dialog&#x27;s</p>
                <p>  message and &quot;apply to all&quot; checkbox. Thirteen of these were plain Swift</p>
                <p>  strings that no translation could ever have reached, including the entire</p>
                <p>  HWP/HWPX conversion failure path — a Korean-market feature that explained</p>
                <p>  itself only in English.</p>
                <ul>
                <li><b>Column widths are now per panel, with a choice.</b> They were a single shared</li>
                </ul>
                <p>  set, which is right when you want both panes to match and wrong when you are</p>
                <p>  comparing a wide listing against a narrow one. Settings ▸ General ▸ **Link</p>
                <p>  column widths across panels** decides; it is on by default, so nothing changes</p>
                <p>  unless you turn it off. A workspace saved by an earlier build gives both panes</p>
                <p>  the widths you already had rather than resetting them.</p>
                <h3>Fixed</h3>
                <ul>
                <li><b>Holding an arrow key stuttered.</b> AppKit asks every menu in the menu bar</li>
                </ul>
                <p>  to update itself on every key press, while it searches for a key equivalent</p>
                <p>  — not only when a menu opens. The PRO-badge delegate answered each of those</p>
                <p>  requests by re-decorating the *entire* main menu, nine menus deep, with a</p>
                <p>  localisation table rebuilt from scratch each time. Sampled at 16% of the main</p>
                <p>  thread while an arrow key was held down, on top of the panel&#x27;s own work.</p>
                <p>  A menu now decorates only itself, the table is built once per language, and</p>
                <p>  the delegate no longer hops through a task to do it.</p>
                <ul>
                <li>**Finder's "Open With", a folder dropped on the Dock icon, and `open -a Unifyl</li>
                </ul>
                <p>  &lt;folder&gt;` all ignored the folder.** The app declared no document types, so it</p>
                <p>  never appeared in Open With at all, and had no handler, so a path passed on</p>
                <p>  the command line was discarded and a window opened wherever the last session</p>
                <p>  left off. For a file manager that is the most ordinary way in. A folder is now</p>
                <p>  entered; a file opens its folder with the file selected, the same as the</p>
                <p>  long-standing &quot;Send to Unifyl&quot; Service — which now shares the code rather than</p>
                <p>  keeping its own copy. Unifyl offers itself for folders but never takes the</p>
                <p>  default away from Finder.</p>
                <ul>
                <li><b>Right-clicking a column header did nothing.</b> The standard place to add or</li>
                </ul>
                <p>  remove a column, or to put the widths back, was empty — the only route was</p>
                <p>  Settings. The header now has that menu, including **Reset Column Widths** for</p>
                <p>  undoing a drag that pushed the other columns off the edge.</p>
                <ul>
                <li><b>Listing a large folder kept a core busy longer than it needed to.</b> The</li>
                </ul>
                <p>  guard that keeps the unrequested size walk out of media libraries rebuilt its</p>
                <p>  answer — three URLs and a set — for every single file it looked at. Sampling</p>
                <p>  the app while it listed a home folder put that function at the top of the</p>
                <p>  profile. It cannot change while the app runs, so it is computed once.</p>
                <ul>
                <li><b>Opening your home folder asked for your Photos library.</b> The size column</li>
                </ul>
                <p>  measures every folder row in the background, and measuring `~/Library` reached</p>
                <p>  `~/Library/Photos` — a plain directory, not a `.photoslibrary` bundle and not</p>
                <p>  under `~/Pictures`, so neither rule the guard had could see it, and macOS gates</p>
                <p>  it behind the Photos service all the same (it holds the &quot;Shared with You&quot;</p>
                <p>  library). Every earlier fix assumed the walk never got past `~/Pictures`, and</p>
                <p>  every test agreed, because none of them knew this directory existed. Found by</p>
                <p>  attaching a debugger while the dialog was up: four walker threads were all</p>
                <p>  blocked in `open(&quot;~/Library/Photos&quot;)`. It is skipped now, like `~/Pictures`.</p>
                <p>  Two smaller holes closed on the way: ⌥⇧Return sized every row with no filter</p>
                <p>  at all (one filter now, shared with the automatic pass), and the walk no longer</p>
                <p>  asks the enumerator to pre-read each entry&#x27;s attributes, a read that happened</p>
                <p>  before the guard could rule on it.</p>
                <ul>
                <li><b>The pointer never showed a column could be resized.</b> The dividers dragged,</li>
                </ul>
                <p>  but the cursor stayed an arrow the whole way across the header, so the only way</p>
                <p>  to find out was to try. `NSTableHeaderView` installs those cursor rects itself</p>
                <p>  normally; inside the SwiftUI hosting view it does not. The header now adds them</p>
                <p>  and refreshes them whenever a column moves.</p>
                <ul>
                <li><b>Only the file-name column could be resized.</b> Every other divider ignored</li>
                </ul>
                <p>  the mouse and the cursor never changed over it. The table used</p>
                <p>  `.firstColumnOnlyAutoresizingStyle`, which keeps the table&#x27;s width tied to the</p>
                <p>  pane by flexing the first column — so a drag on any other divider had nowhere</p>
                <p>  to put the width and AppKit refused it. Column autoresizing is off now, so</p>
                <p>  every divider works.</p>
                <ul>
                <li><b>Column widths could not be set.</b> Dragging a column wider re-proportioned</li>
                </ul>
                <p>  every column on the next layout pass, so a column pulled to 900pt came back at</p>
                <p>  its 60pt minimum — &quot;it returns to its optimal width&quot;. The app no longer adjusts</p>
                <p>  column widths at all: a drag is left exactly as made, and the other columns are</p>
                <p>  not touched. Pane resizing still flexes the first column only. Columns can now</p>
                <p>  sum wider than the pane, which clips the trailing one; narrow a column or</p>
                <p>  reorder to bring it back.</p>
                <ul>
                <li><b>Widening a column made it narrower.</b> Column widths are shared by both panes,</li>
                </ul>
                <p>  and the panel broadcast every width change it heard from AppKit — which posts</p>
                <p>  the same notification whether the user dragged a divider, the app applied a</p>
                <p>  saved width, or `sizeToFit()` squeezed the columns into a narrow pane. Only the</p>
                <p>  first of those is a preference. So dragging Size wider in one pane pushed the</p>
                <p>  new width to the other, whose narrower pane made `sizeToFit()` shrink it, and</p>
                <p>  that shrunken value came back and overwrote the drag. Now only changes the app</p>
                <p>  did not initiate are reported. Note the remaining ceiling is deliberate: there</p>
                <p>  is no horizontal scroller, so a column can grow until the others reach their</p>
                <p>  minimum widths — hide columns you don&#x27;t need to give one more room.</p>
                <ul>
                <li><b>Text painted in a colour that cannot be read.</b> <code>textMuted</code> tops out at</li>
                </ul>
                <p>  2.36:1 against the lightest background token in the system and `textTertiary`</p>
                <p>  at 3.97:1 — neither can reach the 4.5:1 a body label needs, on any background.</p>
                <p>  They are decorative colours, and three pieces of real content were using one:</p>
                <p>  the empty-state message under &quot;nothing to show here&quot;, the palette&#x27;s &quot;no</p>
                <p>  commands found&quot;, and the shortcut badges (⌘D and friends) that are the whole</p>
                <p>  point of a palette row for a keyboard user. All three now use `textSecondary`</p>
                <p>  — 4.74:1 to 5.72:1 where they sit. Audit F63 fails on either colour unless the</p>
                <p>  call site marks itself `// decorative:`.</p>
                <ul>
                <li><b>Command palette group headers were unreadable, and untranslated.</b> The</li>
                </ul>
                <p>  headers used `textMuted`, which sits at 1.86:1 against the palette&#x27;s surface —</p>
                <p>  and lightening that surface in the fix above made it *worse*, not better</p>
                <p>  (2.25:1 before). A 10pt label needs 4.5:1, so they now use `textSecondary` at</p>
                <p>  4.74:1. They were also bare English literals, so a Korean or Japanese user read</p>
                <p>  &quot;FILE&quot; over a list of translated commands; all nine category names are</p>
                <p>  localised across the 15 shipping locales.</p>
                <ul>
                <li><b>Arrow keys in the command palette jumped around instead of stepping.</b> ↓ walked</li>
                </ul>
                <p>  `registry.search(query)` while the rows on screen were that list grouped by</p>
                <p>  category. Those are two different orders, so the next item in search order sat</p>
                <p>  somewhere else on screen — far below, then back above — and the auto-scroll</p>
                <p>  chased it. Selection now indexes the same flattened order the rows are drawn in.</p>
                <ul>
                <li><b>The palette's selected row was invisible.</b> It used <code>bgOverlay</code>, which had</li>
                </ul>
                <p>  just become the palette&#x27;s own surface — ΔL\* +0.0, the identical colour. It now</p>
                <p>  uses `bgSelectedActive`, +6.9 above the surface, which is also the token that</p>
                <p>  means &quot;this has keyboard focus&quot;.</p>
                <ul>
                <li><b>The command palette had no visible edge on dark themes.</b> It drew on</li>
                </ul>
                <p>  `bgSurface` behind a 35% black scrim — but a black scrim cannot darken a</p>
                <p>  backdrop that is already near-black, which every dark theme is. Measured in</p>
                <p>  CIE L\*, the palette sat between ΔL\* +6.9 and **−6.3**: on Nord it was</p>
                <p>  *darker* than the pane it covered, so it read as a dent rather than a panel,</p>
                <p>  and its border managed 1.2:1 against its own surface. The palette now uses</p>
                <p>  `bgOverlay` with a dedicated modal scrim and a brighter edge token, putting it</p>
                <p>  at ΔL\* +7.5 to +15.2 across the built-in themes — lighter than its backdrop</p>
                <p>  everywhere — with the border at 2.9:1.</p>
                <ul>
                <li><b>The Edit menu showed the wrong command names, all badged PRO.</b> Undo read</li>
                </ul>
                <p>  &quot;Save Layout… PRO&quot;, Copy read &quot;Text Tools… PRO&quot;, and basic operations —</p>
                <p>  Cut, Copy, Paste, Select All — appeared locked. The shortcuts were right and in</p>
                <p>  the right places because SwiftUI owns those; only the titles rotted.</p>
                <p>  `ProBadgeMap` remembered each item&#x27;s undecorated title in a static dictionary</p>
                <p>  keyed by `ObjectIdentifier`, which is the object&#x27;s address. SwiftUI destroys</p>
                <p>  and rebuilds the `NSMenuItem`s behind `CommandGroup`/`CommandMenu`, a new item</p>
                <p>  gets allocated on a dead one&#x27;s address, and the stale entry then answered for</p>
                <p>  it — so a recycled address made an ungated command render as whichever gated</p>
                <p>  command used to live there. The badge is now stripped back off the item&#x27;s own</p>
                <p>  title instead of being remembered anywhere. Audit F60 rejects a long-lived</p>
                <p>  static keyed by an object&#x27;s address.</p>
                <ul>
                <li><b>Holding an arrow key crawled instead of scrolling.</b> Moving the cursor made</li>
                </ul>
                <p>  the app read the keychain, once per keystroke. `tableViewSelectionDidChange`</p>
                <p>  assigned `appVM.activePanel` unconditionally, and `@Observable`&#x27;s setter</p>
                <p>  notifies even when the value is unchanged — so an arrow key inside one pane</p>
                <p>  invalidated every observer of `activePanel`, which `MainWindowView` reads in 42</p>
                <p>  places. Its body reads `isTrialActive`, which read two uncached computed</p>
                <p>  properties, each of which called `KeychainService.loadString`. A 15-second</p>
                <p>  profile of a real held key had the main thread 68.1% busy, with</p>
                <p>  `isTrialExpiredPermanently` alone accounting for about a second of it and the</p>
                <p>  rest going to AttributeGraph updates and repeated full-window Auto Layout</p>
                <p>  passes. Debug builds fared worse still: the file-backed dev keychain re-read</p>
                <p>  and re-decoded its JSON store and called `createDirectory` once per service</p>
                <p>  name on every get, putting `open()` at 4.4% of the profile and `mkdir` on the</p>
                <p>  list at all. The panel now compares before assigning, the trial state is cached</p>
                <p>  and invalidated where it is written, and the dev keychain resolves its path</p>
                <p>  once and keeps the decoded store in memory. Re-measured on the same folder</p>
                <p>  after the fix: the keychain is absent from the profile entirely. Audit F59</p>
                <p>  rejects a keychain or filesystem read in an uncached getter that a view body</p>
                <p>  can reach.</p>
            ]]></description>
            <pubDate>Sun, 23 Aug 2026 07:24:34 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.7.2/Unifyl-1.7.2.dmg"
                sparkle:version="44"
                sparkle:shortVersionString="1.7.2"
                sparkle:edSignature="CaEk9bO87KqHCQ2kBblQoIWrjyVoyPxtVEd3luvLGzq0c7v/1SjslTn0G67Q+/V+CYu6Ka1DmUD0+SOdCKY+CQ=="
                length="18549792"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.7.1</title>
            <description><![CDATA[
                <h2>Unifyl 1.7.1</h2>
                <h3>Security</h3>
                <ul>
                <li>**The plugin loader accepted any self-signed or ad-hoc-signed native-code</li>
                </ul>
                <p>  bundle.** Third-party plugins load from `~/Library/Application Support/Unifyl/</p>
                <p>  Plugins` — a user-writable path — and are loaded as native code with the</p>
                <p>  (non-sandboxed) app&#x27;s full privileges, so the code-signature check is the only</p>
                <p>  gate. It called `SecStaticCodeCheckValidity` with a nil requirement, which</p>
                <p>  verifies only that the signature seal is unbroken and accepts ANY valid</p>
                <p>  signature — including the ad-hoc (`codesign -s -`) and self-signed signatures</p>
                <p>  an attacker produces in seconds. Verified: an ad-hoc-signed bundle passed. The</p>
                <p>  check now pins `anchor apple generic`, requiring a real Apple-issued</p>
                <p>  Developer ID / Mac App Store certificate (traceable and revocable), which</p>
                <p>  rejects ad-hoc and self-signed bundles while still allowing legitimately</p>
                <p>  distributed plugins. Fails closed if the requirement can&#x27;t be built. Audit F50</p>
                <p>  rejects any signature check that passes a nil requirement.</p>
                <ul>
                <li><b>A filename could inject a shell command through the terminal's <code>$e</code> variable.</b></li>
                </ul>
                <p>  The inline and drop-down terminals expand `$F`/`$D`/`$S`/`$f`/`$n`/`$e` into a</p>
                <p>  command and run it as `/bin/zsh -lc`. Every variable was shell-escaped except</p>
                <p>  `$e` (file extension), interpolated raw on the theory that &quot;extensions are</p>
                <p>  safe&quot;. They are not: the extension comes from a filename the app didn&#x27;t author</p>
                <p>  (an archive entry, a remote share, a download), and `URL.pathExtension` rejects</p>
                <p>  only whitespace and `/` — it preserves `` ` ``, `$( )`, `;`, `|`, `&amp;`, `&gt;`,</p>
                <p>  none of which need a space to be dangerous. A file named</p>
                <p>  `photo.jpg$(id&gt;~/pwned)` made any command containing `.$e` execute the embedded</p>
                <p>  command. Proven end-to-end against a real login zsh. `$e` is now escaped like</p>
                <p>  every other variable; legitimate use (`convert $F out.$e`) is unchanged. Audit</p>
                <p>  F49 rejects any terminal variable that reaches the shell unescaped, and a</p>
                <p>  regression test runs the expanded strings through zsh to confirm no injection</p>
                <p>  executes.</p>
                <h3>Added</h3>
                <ul>
                <li><b>Four AI features left the command palette for the menus.</b> Natural Language</li>
                </ul>
                <p>  Command, Summarize, Organize and Enhanced Duplicate Scan had a palette entry</p>
                <p>  and nothing else — no menu item, and no way to assign them a shortcut.</p>
                <ul>
                <li><b>Seven more commands can finally be given a shortcut</b> (Disconnect Remote,</li>
                </ul>
                <p>  Undo History, the three AI tools, Statistics, Reclaim Space): they had no row</p>
                <p>  in the shortcut catalogue at all.</p>
                <ul>
                <li><b>Thumbnail Grid now behaves like the list.</b> Clicking a thumbnail moved the</li>
                </ul>
                <p>  cursor *and wiped every mark you had made* — the grid&#x27;s selection was bound</p>
                <p>  straight to the mark set — so marking ten files and then clicking one left</p>
                <p>  F5/F6/F8 acting on a single file. The grid also had no right-click menu at</p>
                <p>  all, which put Copy Path, Permissions, Create Symbolic Link, X-Ray and the</p>
                <p>  media tools out of reach in grid mode. Marks and the cursor are now separate</p>
                <p>  and drawn differently, Insert toggles a mark, and both views build their</p>
                <p>  context menu from the same code.</p>
                <ul>
                <li><b>Colour tags and the 12 themes are in the command palette.</b> Tags could only</li>
                </ul>
                <p>  be applied with ⌃0–⌃7, a binding documented nowhere a user reads; themes lived</p>
                <p>  only in the toolbar&#x27;s ⋯ menu, which the keyboard can&#x27;t reach.</p>
                <h3>Fixed</h3>
                <ul>
                <li><b>Two windows could be dragged narrower than their own contents</b> — the main</li>
                </ul>
                <p>  window with the Connections sidebar open (the splitter stopped responding) and</p>
                <p>  Directory Compare (the Actions column was clipped away).</p>
                <ul>
                <li><b>Tabs jumped sideways as the pointer crossed the tab bar</b>, because each</li>
                </ul>
                <p>  tab&#x27;s ✕ was inserted on hover and changed the tab&#x27;s width.</p>
                <ul>
                <li><b>Three-Way Merge asked for three files through three separate dialogs</b>, for</li>
                </ul>
                <p>  files the app was already showing. It pre-fills from the two panes now.</p>
                <ul>
                <li>**The F1–F8 bar, the folder-sync verdicts and the overwrite dialog's Replace</li>
                </ul>
                <p>  button were English in every language.** `Text` given a plain string doesn&#x27;t</p>
                <p>  localize — meanwhile eight translated function-key strings had been sitting</p>
                <p>  unused in all 15 locales, written for an older layout.</p>
                <ul>
                <li><b>Three flows couldn't be finished without a mouse</b>: running a folder sync,</li>
                </ul>
                <p>  deleting from Duplicate Finder, and using Clipboard History at all.</p>
                <ul>
                <li><b>The panel filter bar rendered in the middle of the file list</b>, floating over</li>
                </ul>
                <p>  the rows, because the stack it lives in was never given an alignment. It also</p>
                <p>  outlived the folder it was typed for — entering a subfolder left the new</p>
                <p>  listing mysteriously filtered — and when it emptied a pane, the &quot;no matches&quot;</p>
                <p>  state read a *different* filter, so its Clear Filter button couldn&#x27;t clear it.</p>
                <ul>
                <li><b>⌘W on the last tab replaced it with your home folder</b> instead of closing</li>
                </ul>
                <p>  the window, silently throwing away where you were.</p>
                <ul>
                <li><b>Compare Files with an empty opposite panel diffed the file against itself</b></li>
                </ul>
                <p>  and reported &quot;identical&quot;; **Compare Directories on a remote or in-archive pane</p>
                <p>  opened a window listing nothing**, which reads the same way. Both now say what</p>
                <p>  they need instead.</p>
                <ul>
                <li><b>Pinned tabs and renamed tabs didn't survive a relaunch</b> — there was nowhere</li>
                </ul>
                <p>  in the saved workspace to put either.</p>
                <ul>
                <li><b>22 Pro commands looked free in the command palette</b> until you picked one:</li>
                </ul>
                <p>  the PRO pill came from the command&#x27;s category rather than the gate it actually</p>
                <p>  enforces.</p>
                <ul>
                <li>Escape now dismisses both update-permission alerts, one of which appears</li>
                </ul>
                <p>  unannounced shortly after first launch.</p>
                <ul>
                <li><b>Drag-and-drop told you the wrong thing twice.</b> Dwelling over a folder</li>
                </ul>
                <p>  inside an open archive threw the pane *out* of the archive mid-drag, so the</p>
                <p>  files landed on real disk; and ⌘-dragging into a remote pane or an archive</p>
                <p>  showed the move badge and then copied — the remote case apologised afterwards,</p>
                <p>  the archive case said nothing. Panes that can only copy now say so in the</p>
                <p>  badge, before the drop.</p>
                <ul>
                <li><b>Cancel now stops a folder sync.</b> It used to dismiss the sheet while the</li>
                </ul>
                <p>  loop kept copying, so the window closed and files carried on landing.</p>
                <ul>
                <li><b>AI Auto-Classify and AI Smart Tagging</b> silently swallowed tag-write</li>
                </ul>
                <p>  failures and closed as if they had worked, the same way Smart Tags did.</p>
                <ul>
                <li><b>Cancel didn't cancel an archive extract or compress.</b> Copy/move/delete poll</li>
                </ul>
                <p>  for cancellation between files, but extract and compress hand the whole job to</p>
                <p>  one child process — so pressing Cancel flipped the button to &quot;Cancelling…&quot;</p>
                <p>  permanently while 7zz ran to completion. The engine already knew how to</p>
                <p>  terminate its child; nothing was wired to ask it to. A cancelled compress also</p>
                <p>  removes the partial archive, since a truncated .zip is not usable.</p>
                <ul>
                <li><b>Two Pro gates fired only after the work was done.</b> Choosing S3 in New</li>
                </ul>
                <p>  Connection looked free — the picker listed every protocol unmarked — so the</p>
                <p>  user typed host, bucket, region and their AWS access key and secret before</p>
                <p>  being refused at Connect. Reclaim Space ran a multi-minute home-folder scan and</p>
                <p>  let the user tick candidates before revealing the lock at &quot;Move to Trash&quot;, and</p>
                <p>  its menu item carried no PRO badge at all. Both now say so up front.</p>
                <ul>
                <li><b>Three more failures only the log ever heard about.</b> A custom theme that</li>
                </ul>
                <p>  failed to save looked fine until it was gone on the next launch; a failed</p>
                <p>  automation write silently lost every scheduled rule; and Smart Tags swallowed</p>
                <p>  per-file tag failures with `try?` and dismissed the sheet, so on volumes</p>
                <p>  without tag support (exFAT, many SMB shares) nothing was tagged and it looked</p>
                <p>  like it had worked.</p>
                <ul>
                <li><b>Ten operations changed your files without ever reaching Undo.</b> The app has</li>
                </ul>
                <p>  an undo stack and an Undo History timeline, and 1.6.7 fixed the copy/move</p>
                <p>  paths — but the natural-language bulk command, AI Organize, macro replay,</p>
                <p>  ⇧F5 Duplicate, the Hex editor, the EXIF editor, audio tags, delete inside an</p>
                <p>  archive, Text Tools and Git &quot;Restore this version&quot; all recorded nothing. ⌘Z</p>
                <p>  after any of them silently reversed an *older, unrelated* operation while the</p>
                <p>  thing you wanted back stayed gone. The bulk operations now record per-file</p>
                <p>  landing paths (undo refuses a record without them rather than guess a path and</p>
                <p>  delete the wrong file), and the in-place rewrites keep a `.unifyl-bak` sidecar</p>
                <p>  and reuse the restore path the encoding converter already had. Backups are</p>
                <p>  capped at 1 GiB — past that the edit proceeds exactly as before rather than</p>
                <p>  quietly consuming gigabytes. Audit F52 covers the class and found the last two</p>
                <p>  instances itself.</p>
                <ul>
                <li>**Dismissing the file-conflict sheet with Escape hung the copy or move</li>
                </ul>
                <p>  forever.** Copy/move, drag-and-drop and archive extraction suspend the</p>
                <p>  operation on a continuation and ask what to do about a name collision. The</p>
                <p>  sheet&#x27;s three buttons all answered it, but SwiftUI performs its own dismissals</p>
                <p>  — Escape, click-outside, interactive dismiss, window close — and signals them</p>
                <p>  by driving the presentation binding to `false`, not through any button. That</p>
                <p>  setter only cleared the sheet&#x27;s state, so the dialog closed and the</p>
                <p>  continuation was never resumed: the operation waited forever and every job</p>
                <p>  queued behind it stalled, unrecoverable without quitting the app. Eight call</p>
                <p>  sites fed that one sheet. Dismissal now resumes with &quot;skip&quot; — never overwrite</p>
                <p>  — and the cloud-only and remote delete confirmations got the same treatment so</p>
                <p>  a future view binding to their flags can&#x27;t reintroduce the hang. Audit F51</p>
                <p>  covers the class, and regression tests dismiss each dialog and fail on a</p>
                <p>  timeout rather than hanging the suite.</p>
                <ul>
                <li>**Remote and archive file dates were wrong by up to five centuries in some</li>
                </ul>
                <p>  regions — and folder sync trusted them.** An `ls -l` line from an SFTP server</p>
                <p>  and a `tar -tv` line from an archive both print Gregorian dates (&quot;Aug 17&quot;), but</p>
                <p>  both parsers assembled them through `Calendar.current`, which inherits the</p>
                <p>  system region&#x27;s calendar. Measured under ar_SA&#x27;s Islamic calendar: &quot;Aug 17</p>
                <p>  09:30&quot; parsed as 2027-01-25, and &quot;Aug 17 2025&quot; as 2586-11-20. Folder sync</p>
                <p>  decides which side is newer from exactly these timestamps, so a remote file</p>
                <p>  dated centuries ahead always won and would overwrite the user&#x27;s current local</p>
                <p>  copy. Both parsers now pin a Gregorian calendar while keeping the user&#x27;s</p>
                <p>  timezone. Audit F48 covers the whole class, distinguishing relative date</p>
                <p>  arithmetic (calendar-agnostic, fine) from constructing an absolute date out of</p>
                <p>  month/day/year numbers (not fine) — it found the third instance itself.</p>
                <ul>
                <li>**Every scheduled automation fired at the wrong time, or never, for anyone whose</li>
                </ul>
                <p>  Mac region uses a non-Gregorian calendar.** Cron fields are Gregorian by</p>
                <p>  definition — month 1-12, day 1-31 — but the matcher pulled date components</p>
                <p>  through `Calendar.current`, which inherits the system region&#x27;s calendar. In</p>
                <p>  Saudi Arabia and other ar_SA regions that is the Islamic calendar, so a</p>
                <p>  `0 9 15 * *` trigger compared its &quot;15&quot; against an Islamic day number and never</p>
                <p>  matched a Gregorian the 15th. The calendar is now pinned to Gregorian while</p>
                <p>  still honouring the user&#x27;s timezone. Found by running the whole suite under</p>
                <p>  `-testRegion SA` for the first time: three cron tests failed there and passed</p>
                <p>  in every other locale.</p>
                <ul>
                <li>**Pasting multiple files crashed the app in Korean — and would have in</li>
                </ul>
                <p>  Japanese and Thai.** Seven translations had reordered `%d` and `%@` for</p>
                <p>  natural word order without positional specifiers, so `String(format:)` handed</p>
                <p>  the file count to `%@`, which dereferenced it as an object pointer. A compiled</p>
                <p>  probe segfaults on the exact string. Reachable from the ⌘V paste toast and</p>
                <p>  Select Same Extension. All seven now use `%1$d`/`%2$@` positions, keeping the</p>
                <p>  natural word order. Found by a new cross-locale format-specifier check</p>
                <p>  (audit F45), which now guards all fifteen catalogues; no existing gate could</p>
                <p>  see it — key parity looked fine, the catalogue parsed, and the test suite runs</p>
                <p>  in English.</p>
            ]]></description>
            <pubDate>Wed, 19 Aug 2026 10:30:54 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.7.1/Unifyl-1.7.1.dmg"
                sparkle:version="43"
                sparkle:shortVersionString="1.7.1"
                sparkle:edSignature="VdT5QtAPASCYT2zcTqLzdXmPdRvbXsSuo7FnWVSLMmE9iUpZ4VMHqC+N9oBfbQtasuYgEKTd0+7PWboP7DZFCg=="
                length="18332710"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.7.0</title>
            <description><![CDATA[
                <h2>Unifyl 1.7.0</h2>
                <h3>Added</h3>
                <ul>
                <li><b>A short file list now says what the empty space below it is hiding.</b> A pane reading "4 items" could mean four files, or four out of two hundred with a filter on, and nothing distinguished them — which is how you conclude a folder is empty and re-download what you already have. Under a short list the pane now reports how many items the filter and the hidden-files setting are withholding, how many of these names are also in the other pane, and what changed here recently. It appears only when the list leaves room and only when one of the three has something true to say; a folder with nothing to report stays quiet.</li>
                <li><b>Swapping panels (⌃U) shows the panes trading places</b> instead of the contents changing under you, so you can see which side went where. The folder tree now slides in like the terminal and the command palette already did, and switching theme cross-fades rather than snapping the window to a new colour in one frame.</li>
                <li><b>A few quiet asides.</b> Open Unifyl between 1 and 5 in the morning and it says something short about the hour. Come back after a fortnight away and it tells you nothing moved while you were gone — which is the first thing you want to know about a file manager. Cross a thousand, ten thousand, fifty thousand or a hundred thousand files copied or moved and it says so once. Step into an archive and the ARCHIVE badge drops in rather than appearing. All of it is off with Settings ▸ Playful touches, and the animated part skips itself under Reduce Motion.</li>
                <li><b>Undoing an operation rewinds the timeline instead of blinking.</b> In Undo History, the entry you are undoing lifts up out of the list and fades as the rows below slide into its place, so the step back is something you watch rather than a row that vanishes between frames. It is decorative: Settings ▸ Playful touches turns it off, Reduce Motion skips it, and the undo itself runs either way.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>Pro is now $24.99, down from $39.99, and Korea is priced at ₩39,900.</b> The</li>
                </ul>
                <p>  in-app price, the bundled manual in all fifteen languages, and every landing</p>
                <p>  page follow the new figure. Korea gets a custom App Store price rather than</p>
                <p>  the automatic conversion of the base tier, so Korea lands on the advertised</p>
                <p>  figure exactly. Direct is a dollar price — about ₩40,100 all-in — so in Korea</p>
                <p>  the App Store is now the cheaper of the two.</p>
                <p>  The reason is not a sale. Korean launch copy quoted ₩39,900 — a number lifted</p>
                <p>  from a regional-pricing proposal that was never adopted — and it went out</p>
                <p>  publicly while the real price was ₩66,000. Repricing only Korea would have</p>
                <p>  made the cheaper channel the Mac App Store build, which is the one without</p>
                <p>  Sparkle, 7-Zip, unalz, the Git panel or the terminal; and a Korea-only Direct</p>
                <p>  discount cannot be enforced at all, since LemonSqueezy has no per-country</p>
                <p>  pricing and a coupon compiled into the app is a coupon for everybody. So the</p>
                <p>  base price came down far enough to make the advertised number true everywhere.</p>
                <ul>
                <li>The upgrade sheet's Mac App Store fallback label read from a second hardcoded</li>
                </ul>
                <p>  price string, so a price change could leave the two disagreeing while StoreKit</p>
                <p>  loaded. It now falls back to the same constant everything else reads.</p>
                <h3>Removed</h3>
                <ul>
                <li>A dead localization key carried the price inside the key itself</li>
                </ul>
                <p>  (`&quot;Buy Pro — $39.99&quot;`, in all fifteen catalogues). Nothing referenced it — the</p>
                <p>  live button has used a format string for some time — but a key like that puts</p>
                <p>  the price where a missing translation displays it verbatim.</p>
            ]]></description>
            <pubDate>Mon, 10 Aug 2026 13:50:02 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.7.0/Unifyl-1.7.0.dmg"
                sparkle:version="42"
                sparkle:shortVersionString="1.7.0"
                sparkle:edSignature="UPRvXoIyN8qISkr9ft9C76BC6BTq/GpRs5l5Tutzy2jLKlfXhAbz76aRnA/o8xPilCjKMb8uqnb4IUHxRnwbAA=="
                length="18215776"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.6.7</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.7</h2>
                <h3>Changed</h3>
                <ul>
                <li><b>The website finally matches the app.</b> The landing page had been describing 1.2.0 — four releases back — and not one of the 15 localised variants had ever been deployed: every locale URL returned 404 while the app itself shipped all 15 languages. All 15 pages are live now, each carrying the 60-second promo video, and two claims that were wrong went with them: &quot;120+ rebindable shortcuts&quot; is exactly 133, and the &quot;50+ professional tools&quot; line is gone in favour of naming the work instead of counting features.</li>
                <li><b>The other 13 languages caught up.</b> 128 strings had accumulated since 1.3.0 with a translation in Korean only — every Tools, Compare, Developer and Automation menu title, the panel and view labels, and the refusal messages added in 1.6.4–1.6.6. German, French, Spanish, Italian, Portuguese (Brazil), Japanese, Simplified and Traditional Chinese, Russian, Turkish, Arabic, Thai and Vietnamese now carry all 1,358 keys, matched to Apple&#x27;s own macOS terminology per language rather than translated literally.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Undoing a copy or move could delete or displace the wrong file.</b> Undo reconstructed what an operation did from the names it was <i>asked</i> for, not where files actually landed. So after answering a name-conflict dialog with &quot;keep both&quot;, ⌘Z deleted the pre-existing file the copy had collided with — and after &quot;skip&quot;, where nothing was copied at all, it still did. Undoing a folder copy that had merged into an existing same-named folder deleted that folder with everything already inside it. Every copy, move, drag and paste now records the real landing spot of each file — the renamed copy under its actual name, skipped and failed files not at all — and undo acts only on those. If a record somehow lacks them, undo now says it can&#x27;t rather than guessing.</li>
                <li><b>⌘D and the command palette&#x27;s copy/move were not undoable at all.</b> Only the F5 confirmation sheet recorded anything for undo; Copy to Opposite Panel, Move to Opposite Panel, the palette commands, the skip-confirmation paths and the single-file rename-copy recorded nothing. All of them do now.</li>
                <li><b>A file you deleted to the Trash could not be put back.</b> The delete itself was always correct, but Finder&#x27;s Put Back refused with &quot;…no longer exists&quot;, naming a folder that was still right there — so the one-click way back from an accidental delete was gone, for anything deleted out of Downloads, Desktop, Documents or another folder macOS protects.</li>
                <li><b>⌘Z did not undo a file operation.</b> Rename, copy, move and delete were all undoable from Edit ▸ Undo File Operation, and that item is where the keyboard shortcut is documented, but pressing ⌘Z did nothing: macOS was handing the key to its own Undo, which is inactive unless you are typing. ⌘Z now reaches the file undo, and text undo still works while editing a name or filling in a field.</li>
                </ul>
            ]]></description>
            <pubDate>Sun, 09 Aug 2026 07:35:48 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.7/Unifyl-1.6.7.dmg"
                sparkle:version="41"
                sparkle:shortVersionString="1.6.7"
                sparkle:edSignature="6SJv2/qxrg7cztkhM30aB5uP5nupauIFcREcwdR4Sqqv5QNFW0+da0jyT++dWwpmNf3xHxi5FN4nUIWUlMQWBQ=="
                length="18149387"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.6.6</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.6</h2>
                <h3>Fixed</h3>
                <ul>
                <li><b>Nothing you changed in Settings ▸ Shortcuts had any effect.</b> The pane listed 131 remappable commands and the app read none of them. Menu items declared their key twice — a hardcoded one and a catalogue-bound one layered on top — and the hardcoded one is the one macOS uses, so every catalogue binding was inert. The function-key handler never consulted the catalogue at all; F1–F8, ⌃0–⌃7 for tags, ⇧F4/⇧F5/⌥F5/⇧F6/⇧F9/⌥F9, ⌃T, ⌃Q, ⌃G, ⌃S, ⌃H, ⌃U, ⌃B, ⌃\, ⌥←, ⌥⇧↩, ⌘F and the file clipboard keys were all fixed in code. Every one of them now resolves through the catalogue, so remapping a command moves the key everywhere it is handled and clearing a row unbinds it. Refresh's hidden ⌘R handler and Reveal in Opposite Panel, which bypassed the pane entirely, are included.</li>
                <li><b>Two commands were listed twice in Settings ▸ Shortcuts.</b> "Select All" and "Invert Selection" appeared under both Edit and Selection with the same default; only one row could own the key, so editing the other did nothing. The duplicates are gone, and any customisation stored against them is surfaced as a legacy shortcut to re-assign rather than dropped. "Function Key Bar" and "Reveal in Opposite Panel" gained the rows they were missing.</li>
                <li><b>37 of the 133 rows in Settings ▸ Shortcuts still could not be given a shortcut</b> — every Tools, Compare, Developer, Automation and Analysis entry. Assigning one showed the new key in the row and in the menu, and pressing it did nothing, because the wiring pass had only reached menu items that already had a built-in shortcut to replace. All 133 rows are now connected. No new default shortcuts were added: these commands still ship with none, exactly as before — the difference is that a key you assign to them now works.</li>
                <li><b>Four menu items had their shortcut typed into the title</b> — "Compress… (⌥F5)", "Extract to Folder… (⌥F9)", "Duplicate in Same Folder (⇧F5)", "New Text File + Edit (⇧F4)". Now that those commands can be remapped, a printed key becomes wrong the moment you change it. The text is gone; macOS shows the real key, which follows the binding.</li>
                <li><b>Some Settings ▸ Shortcuts rows named a command differently from the menu item they control</b>, so searching Settings for what you had just clicked found nothing — "PDF Merge / Split" for the menu's "PDF Tools", "Folder Size Heatmap" for "Folder Heatmap", and six more. The rows now use the menu's wording.</li>
                <li><b>A shortcut you recorded on Home, End, Page Up, Page Down, keypad Enter or F13–F19 was accepted, displayed, and never fired</b>, while the old key quietly kept working. The recorder wrote one spelling of those key names and the two things that read shortcuts understood a different one. A key the app genuinely cannot bind is now refused with a reason instead of saved. Recording a bare letter or digit is refused too — those are what jump to a file by name — and so is a chord the file panel already answers to itself (⌘1–⌘9, ⌃V, ⌃X, ⌃A, Return and the rest), which used to be accepted and then swallowed.</li>
                <li><b>Dragging files between panels skipped four safeguards that every other way of copying had.</b> A drop into an open archive wrote the files next to the archive instead of into it; a drop onto a remote connection failed with a raw "URL type sftp isn't supported"; a drop onto a Smart Folder wrote into whatever directory the panel last showed; verify-after-copy never ran, even though this release made dragging copy by default and made verification free; and ⌘Z could not undo a drag, in the same release that made ⌘Z the main undo key. Dragging now uploads to a remote panel, lands inside the archive you are looking at, refuses a Smart Folder with an explanation, verifies when you have asked it to, and is undoable. Dragging a row *out* of an archive or a remote panel — which could only ever hand over a reference to nothing — no longer starts.</li>
                <li><b>Acting on a file inside an archive, or on a remote connection, failed without saying so.</b> ⌘C reported "Copied" and put an unusable reference on the clipboard; ⌃1–⌃7 announced a tag and drew it on the row without writing anything; Create Symlink did nothing at all; the preview pane went blank with a file plainly under the cursor; Get Info sat on "Calculating…" forever; and double-clicking with "Open with default app" selected handed the file to macOS, which answered with a dialog about a URL type no application can open. Each of these now explains what it cannot do and what to do instead.</li>
                <li><b>The right-click "Get Info" ran Reveal in Finder.</b> The menu-bar item was split into a real Get Info panel and a separate Reveal item; its twin in the right-click menu was missed, so the same words did two different things depending on where you clicked. Both menus now offer both, and each does what it says.</li>
                <li><b>F5 or F6 to a remote panel was refused whenever exactly one file was selected</b> — the ordinary case — with an instruction to navigate the other panel somewhere it already was. It also only ever checked the remote destination when the confirmation sheet was shown, so ⌘D, ⇧⌘D, the command palette and the skip-confirmation path all wrote the files to a local folder instead of uploading them.</li>
                <li><b>Get Info kept measuring a folder after you closed it</b>, and asked this Mac about a remote symlink's target, reporting either an unrelated local file or a broken link for a link that is fine where it lives.</li>
                <li><b>The App Store build still offered the terminal</b> through the command palette, the backtick key and the Function Key Bar button, after the View-menu items were removed. That build cannot run a shell, so every command in it failed.</li>
                <li><b>The manual described the old drag behaviour, a shortcut that belongs to another command, a Free edition that includes cloud storage, and a "Pro + AI" tier that does not exist.</b> Unifyl is Free and Pro, one-time purchase.</li>
                </ul>
            ]]></description>
            <pubDate>Mon, 03 Aug 2026 15:54:47 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.6/Unifyl-1.6.6.dmg"
                sparkle:version="40"
                sparkle:shortVersionString="1.6.6"
                sparkle:edSignature="b1Wr/uM/EM6fUEcPMox2uB/Qn6mv5ie7fLTYiW+HDO1rc1EahKpYKrp0kNif7Q9SfWI/2vQsQ+IZoMnH5/vICA=="
                length="18087190"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.6.5</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.5</h2>
                <h3>Fixed</h3>
                <ul>
                <li><b>Editing a file inside an archive lost the edit if you quit before switching back to Unifyl.</b> The write-back into the archive only ran when Unifyl regained focus. Save in your editor and then quit Unifyl from the Dock — or log out, or restart — and it never regained focus: the edit was never written back, and the staged copy holding it was deleted on the way out. The message shown when a write-back failed even pointed at that copy as somewhere safe, and the next launch erased it. Edits are now kept outside the temporary sweep, written back before the app quits, and picked up on the next launch if the app was force-quit or crashed.</li>
                <li><b><code>defaults write</code> instructions in the release notes named a preference domain the app does not use.</b> Following them enabled nothing and reported nothing. Three internal log subsystems named it too, so Reclaim Space, purchase and pricing activity was missing from Console for anyone filtering on the app's real subsystem.</li>
                </ul>
            ]]></description>
            <pubDate>Sun, 02 Aug 2026 13:16:06 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.5/Unifyl-1.6.5.dmg"
                sparkle:version="39"
                sparkle:shortVersionString="1.6.5"
                sparkle:edSignature="4CXsmyqU5IItxRXfZ6KsyUPyK1+XgKWDlmWsJRXwY5PKFFl8JsPVukdNG+L5Xlt5Hg6ODVWiK4gnVaeq7dQrCQ=="
                length="17976425"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.6.4</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.4</h2>
                <h3>Fixed</h3>
                <ul>
                <li><b>Folder sync moved the files it promised to copy.</b> A local-to-local sync ran <code>replaceItemAt</code>, which consumes its source — so every file the plan listed as "copy to right" was deleted from the left. Anyone syncing a folder as a backup lost the thing they were backing up. A name differing only in case from a file the plan said it would *skip* destroyed that file too. Cross-volume syncs copied nothing at all, and symlinks were planned as ordinary files.</li>
                <li><b>Multi-Rename crashed the app.</b> The Auto Number Start and Step fields accept any number, and the preview recomputes on every keystroke — a large value overflowed and took the app down mid-typing.</li>
                <li><b>Multi-Rename refused Korean, Japanese and Chinese names that macOS accepts.</b> The length check counted UTF-8 bytes against a limit the filesystem actually expresses in UTF-16 units, so a Korean name was capped at roughly a third of its real length and the error blamed a "/" that wasn't there. Folder names were also split at the last dot, so <code>v1.2.3</code> was renamed in the middle.</li>
                <li><b>The hex editor truncated large files on save.</b> Files over 10 MB are shown 10 MB at a time; saving wrote only that much back, permanently discarding everything past it. It also overwrote changes another program had made since the file was opened, with no warning.</li>
                <li><b>Content search behaved differently on the two editions.</b> The direct build treated your query as a regular expression and the App Store build treated it literally. Matches in files whose path contains a colon were silently dropped, an unbalanced bracket reported "No matches found" instead of an error, and only <code>*.ext</code> masks worked in the sandboxed build — <code>Makefile</code> or <code>report*.txt</code> quietly matched nothing.</li>
                <li><b>Encoding conversion had no Korean encoding in its fallback chain.</b> The entry commented as CP949 resolves to Shift-JIS, so Korean files fell through to a Latin decode and were reported as "ISO-8859-1". A valid UTF-8 file containing a single damaged byte was rewritten as whole-file mojibake, and a file cut mid-character lost all of its text where the system tool recovers everything but the last character.</li>
                <li><b>Reclaim Space offered to free space that does not exist.</b> Hardlinks and APFS clones — the copies Finder and <code>cp</code> make on modern Macs — were listed as deletable duplicates at full size. Deleting one frees nothing. Sparse files were counted by logical length, overstating by orders of magnitude.</li>
                <li><b>Advanced Search could hang on a short filename.</b> Its protection against runaway regular expressions limited the length of the text being searched rather than the time spent searching it, which is not a limit at all; a 40-character name was enough. Closing the sheet also left the scan running.</li>
                <li><b>Checksum verification failed for every binary-mode file.</b> Lines written as <code>hash *name</code> — what <code>shasum -b</code> and most Windows tools produce — parsed the mode flag as part of the filename, so every file was reported missing.</li>
                <li><b>Three-way merge reported conflicts that were not there</b> on about one in sixteen cleanly mergeable merges, and silently reordered content on some merges it did complete.</li>
                <li><b>Deleting a file inside an archive could destroy a different file outside it.</b> Pressing Delete on an archive entry ran the ordinary delete path, and inside an archive a row's underlying path is the archive's own folder plus the entry name — so if a real file of the same name sat beside the archive, that file was removed instead, and not to the Trash. The archive entry itself was left untouched. With the delete confirmation switched on the dialog routed correctly and hid this; with it off, nothing did.</li>
                <li><b>Copying a folder into one of its own subfolders filled the disk instead of refusing.</b> The refusal existed for drag-and-drop and nowhere else, so F5 and F6 walked straight in: 385 nested copies before it failed on path length, all left behind. Ordinary to hit in a dual-pane window — the left panel on a folder, the right panel already inside it.</li>
                <li><b>Copying a file onto itself deleted it.</b> After a failed copy Unifyl removes the half-written destination, and it could not tell that the destination *was* the source. On a case-insensitive disk — the macOS default — "DOC.txt" and "doc.txt" are one file, so the cleanup removed the original. It is now refused before anything is written.</li>
                <li><b>Multi-Rename could rename the folder you were standing in.</b> With nothing selected the sheet opens on the whole listing, and the ".." row is a real entry pointing at the parent directory. Applying any rule renamed that folder out from under the panel.</li>
                <li><b>Archives listed every file as 0 B, and mangled Korean, Japanese and Chinese filenames.</b> The tar listing asked for paths only, so no size or date ever came back, and without a UTF-8 locale the underlying tool escapes every non-ASCII byte — 한글.txt arrived as \341\204\222…. 7z had the same 0 B fault for a different reason. ZIP was never affected.</li>
                <li><b>Editing a file inside an archive now writes it back.</b> F4 did nothing at all — no editor, no error — and opening a file with Return unpacked it to a temp folder that was deleted sixty seconds later, so an edit was lost and the archive never changed. Both now stage the entry, open it, and save it back into the archive when you save in the editor. F3 and Space had the same root cause and showed an empty window.</li>
                <li><b>Any change inside a tar archive corrupted its structure.</b> Adding, removing, renaming or creating a folder inside a .tar/.tar.gz/.tar.bz2/.tar.xz/.tar.zst rewrote every entry with the full path of a temporary directory. Creating one had the same fault: an archive built from /tmp/x/a.txt contained "tmp/x/a.txt". Replacing a file that already existed failed outright.</li>
                <li><b>Opening Statistics could quit the app.</b> The all-time totals are summed with plain addition, which crashes rather than wraps if the stored numbers are large enough — and the stored numbers are read straight off disk without a sanity check. Totals now saturate.</li>
                <li><b>Corrupt settings were overwritten instead of backed up.</b> Workspace layouts, automations, custom keyboard shortcuts and — worst — your saved folder permissions each got emptied and then saved over the file that still held them. The folder permissions are the painful one: losing them means granting access to every folder again by hand. All four now set the damaged file aside first.</li>
                <li><b>A re-download could replace a good file with a truncated one.</b> Five cloud providers wrote straight over the destination, so a crash or a full disk partway through left the fragment where a complete earlier download had been.</li>
                <li><b>The App Store build pointed users at a web address that does not exist.</b> Every "not available in this version" message ended by naming a domain that was never registered.</li>
                <li><b>Git History never worked on a folder — including the repository you were standing in.</b> It resolved git's working directory as the parent of whatever it was opened on, which is right for a file and wrong for a directory: asking for the history of a repository root ran git from *outside* the repository, so it reported "Not a git repository or git is not available." while the path bar was showing the branch name two inches away. A directory is now its own git context (a nonexistent path still falls back to its parent). The same formula was behind the diff, restore, and is-in-a-repo paths, so all four were affected. Locked by tests.</li>
                <li><b>Content search couldn't find Korean, Japanese or Chinese text in legacy-encoded files</b> — the one thing the CJK pitch leads with. Searching is done with grep, which compares raw bytes, so a Korean query could never match a CP949 document however the file was written; the sandboxed build had a Swift fallback but it only tried UTF-8 and Latin-1, and Latin-1 accepts any byte, so those files decoded to gibberish and reported no matches. Non-ASCII queries now go through a decoder that recognises CP949, Shift-JIS, EUC-JP, GBK and Big5 — and picks between them by detection rather than by order, since those encodings share byte space and a fixed order turns Chinese into Korean nonsense. ASCII searches still use grep and are as fast as before.</li>
                <li><b>File X-Ray called every arm64e binary "arm64".</b> The reader was already parsing the CPU subtype; the name just ignored it. Slices are now labelled the way <code>lipo</code> labels them, including x86_64h — a distinction that matters in a tool whose whole purpose is telling you precisely what a binary is.</li>
                <li><b>Archives made by the App Store build dated every file 1980-01-01.</b> ZIP stores timestamps in an MS-DOS format, and the sandboxed build's own ZIP writer left those fields at the format's epoch instead of reading the source file's date — so unzipping a Unifyl-made archive anywhere else showed 1980 for everything. The direct build was unaffected (it shells out to a tool that gets this right), which is why archive timestamp preservation could ship in 1.1.0 and still be missing here. Dates outside what the format can express (anything before 1980) still fall back to the epoch rather than wrapping to a wrong year.</li>
                <li><b>Uninstalling an app no longer stops at the first thing it can't remove.</b> The App Uninstaller bailed out on the first failure, so a locked or in-use support file left the app half-removed — usually the bundle gone but its leftovers still on disk — and reported only that one problem. It now moves everything it can, keeps the entry in the list when anything survives (removing it would claim an uninstall that didn't happen), and says how many items moved, how many didn't, and why the first one failed.</li>
                <li><b>Four windows could trap the app with no way out.</b> About Unifyl, the Plugin Manager, the Macro Recorder and the Enhanced Duplicate scan are presented as sheets, which carry no titlebar — and none of them had a close button. Escape did nothing, clicking outside was swallowed, and even ⌘W was blocked, so the only escape was force-quitting. All four now have a Close button (with the standard Escape binding), and a release check fails the build if any sheet ships without one.</li>
                <li><b>AI scans no longer ask for your Photos and Apple Music libraries.</b> Reclaim Space was taught to step around ~/Music, ~/Pictures and ~/Movies, but the AI engines weren't — so AI Organize, Enhanced Duplicate Scan, AI Summarize, smart tagging and folder profiling each still walked straight into them and raised a privacy prompt mid-scan. Seven walk sites now share one rule, and the rule lives in a common module rather than inside one scanner, which is why the two had drifted apart in the first place. Pointing a tool at one of those folders on purpose still scans it — only incidental descent during a broader sweep is skipped.</li>
                </ul>
                <h3>Added</h3>
                <ul>
                <li><b>Reclaim Space (Tools ▸ Disk & System).</b> One sweep that answers "where did my disk go": byte-for-byte duplicates, regenerable caches and build artifacts, big files you haven't opened in months, and apps you've stopped launching — all totalled into a single recoverable number instead of four separate tools nobody found. The scan is free (the menu item carries no PRO badge — the number is exactly what a free user needs to see); clearing the whole selection in one go is Pro. Nothing is ever pre-selected and everything goes to the Trash, so a mis-click is recoverable. The sweep steps around Photos, Music and Movies entirely and says so on screen — merely listing those folders raises a privacy prompt nobody asked for, and a cleanup tool has no business offering to delete what's inside a photo library. Point Reclaim Space at one of them deliberately and it's scanned normally. (Measuring other apps' caches still asks for the standard "access data from other apps" permission on first run — every disk-cleanup tool needs it.) Duplicates always keep the most recently used copy of each set, and only well-known regenerable trees (Xcode DerivedData, ~/Library/Caches) are ever offered as "safe" — a folder merely *named* cache is not.</li>
                <li><b>The Tips sheet now tells you the free trial is running.</b> New installs get 14 days of full Pro, but nothing said so on the one screen every new user actually sees — so the PRO badges read as "locked" when they were open, and the trial routinely expired unused. A strip above the card now counts down the remaining days while the trial is live, and switches to a one-line unlock note once it lapses.</li>
                <li><b>Launch-sale pricing without a rebuild (direct edition).</b> The upgrade sheet's price copy — sale price, struck-through regular price, a localized promo note, and the local-currency estimates — can now be updated by publishing a small static file instead of shipping a notarized build. The payload must declare its own expiry date and the app falls back to the built-in price the moment it passes, so a sale ends on schedule even if nobody remembers to change anything. The Mac App Store build ignores all of it and keeps showing Apple's own localized price.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>The rating prompt is no longer limited to enormous transfers (Mac App Store build).</b> It only ever fired after a second 1 GiB+ copy, which most users never make. Ordinary successful copies, moves, and deletes now count toward it as well — big transfers simply count for more — and it still only appears after an error-free operation, at most once per version, on Apple's own 3-per-year leash.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>329 strings showed up in English no matter which language you picked.</b> Menu titles, context-menu commands, half the feature names on the paywall, and most error messages were never in the string catalogs at all — they only existed in the source, so every language fell through to English. All 15 languages are now complete (1,122 strings each). A further 67 already *had* translations in every language that the app could never reach, because the code looked them up under one name while the catalogs stored them under another.</li>
                <li><b>"残り 3 日s".</b> The free-trial countdown made plurals by gluing an "s" onto the day word, which is only correct in English and the Romance languages — Japanese, Chinese, Korean, Italian, Turkish, Russian, Arabic, Thai and Vietnamese all got a stray Latin "s". Both trial lines now use proper per-language plural rules.</li>
                <li><b>VoiceOver read four labels in English in every language</b> — the path-bar breadcrumbs ("Navigate to Documents"), the function-key hints, and the Smart Folder summary. Each built its own lookup key at runtime, so no translation could ever match.</li>
                <li><b>Purchase dates are recorded from now on.</b> Nothing displays them yet; they exist so a future paid upgrade can grandfather recent buyers, which is only possible if the date was captured at purchase time. Recorded from the App Store transaction on the Mac App Store build and from the license activation on the direct build, keeping the earliest date ever seen so a reinstall or a Family Sharing hand-off can never move it forward.</li>
                </ul>
            ]]></description>
            <pubDate>Sun, 02 Aug 2026 07:03:14 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.4/Unifyl-1.6.4.dmg"
                sparkle:version="38"
                sparkle:shortVersionString="1.6.4"
                sparkle:edSignature="LTD5XPz1flbmVALiIs2NCYre8IOYv8w1BIClelMBUEotEQSKNuI+Q3UIJMCQglJUimCaEPAlFzLRtWkg9o6CAA=="
                length="17962895"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.6.3</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.3</h2>
                <p><b>Tips</b> — a card for every feature, shown at launch and anytime from Help &gt; Tips. Each card explains what a tool does, why it matters, and when to reach for it, with the real keyboard shortcut — all 15 languages. Browse the whole set with Previous/Next, or turn the launch popup off with one checkbox.</p>
                <p>Also includes every fix and refinement from 1.6.2 (marked-file actions, dark-theme visibility, Restore Purchases feedback, safer 3-way merge, no surprise keychain prompts, and fewer stalls).</p>
            ]]></description>
            <pubDate>Wed, 22 Jul 2026 20:37:24 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.3/Unifyl-1.6.3.dmg"
                sparkle:version="37"
                sparkle:shortVersionString="1.6.3"
                sparkle:edSignature="2NUpedfCNm5DIiP4ofFT2rAWnaAO0dJ7401AXOdOIb/JRyXbIV6mJBDd/zgAaBfc//BKuk4Dc/5+fROQmZc3Dw=="
                length="17151062"
                type="application/octet-stream" />
        </item>
        <item>
            <title>Version 1.6.2</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.2</h2>
                <h3>Added</h3>
                <ul>
                <li><b>A well-timed ask for a rating (Mac App Store build).</b> After your second 1 GiB+ transfer lands cleanly, Unifyl may show the standard macOS rating prompt — once per version at most, on Apple's own 3-per-year leash, and only ever right after something went well.</li>
                <li><b>File &gt; New Window (⌘N).</b> Closing the main window previously left no menu item to reopen it (App Review, Guideline 4) — the File menu's standard New Window now recreates the main window, and the shortcut matches the macOS convention.</li>
                <li><b>Unifyl &gt; Unifyl Pro… menu item.</b> A direct menu path to the upgrade/purchase sheet (one-time Pro purchase + Restore Purchases), instead of only reaching it through the upgrade banner or a PRO-gated feature (App Review, Guideline 2.1(b)).</li>
                <li><b>Grant Access… actually grants access (Mac App Store build).</b> The permission-denied banner's button now opens a folder-picker (powerbox) grant pre-pointed at the blocked folder and persists it as a security-scoped bookmark for future launches — previously it pointed at System Settings, which cannot pierce the app sandbox. If persisting the bookmark fails, the banner now says so instead of silently forgetting the grant on relaunch.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>Mac App Store entitlements slimmed.</b> Removed the music/movies/pictures asset-folder entitlements from the MAS build — they had no matching functionality (App Review, Guideline 2.4.5(i)); media and iCloud browsing already flow through user-selected access and the Grant Access flow. Downloads access is kept: the sidebar's ~/Downloads is core file-manager functionality behind the standard sandbox consent prompt.</li>
                <li><b>Descriptive consent prompts (Mac App Store build).</b> The Downloads/Documents/Desktop sandbox consent dialogs now explain what Unifyl actually does with each folder instead of showing a generic one-liner (App Review, Guideline 5.1.1(ii)).</li>
                <li><b>The Mac App Store paywall no longer advertises features absent from that build.</b> Docker/Git-panel and other sandbox-dropped items were purged from the upgrade sheet's Pro feature list.</li>
                <li><b>Paywall stops promising what Pro doesn't deliver.</b> The upgrade sheet and nudge banner advertised "60+ professional tools" (the audited count is 50+) and a "Team Management · SSO · Audit Log" row — a feature set that sits behind an off-by-default pilot flag no buyer can reach. The count is corrected everywhere, and the team row is replaced with shipped Pro value (Multi-Rename · Saved Searches · Verify-After-Copy). The Pro-unlock subhead is now actually localized too — its lookup key never existed in the string tables, so every locale silently fell back to English.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Right-click menu and Cmd+Delete now act on the marked files again.</b> The red-mark rendering fix below stopped mirroring marks into the table's native selection — but the context-menu actions (Copy/Move/Delete/Rename/Get Info/…) and the table-level Cmd+Delete/⌦ handlers still read the native selection, so with 5 files marked they silently operated on only the cursor row. They now use the marked set with the cursor row as fallback, matching every other operation path. Locked by tests.</li>
                <li><b>Free tier no longer gets Pro remote protocols, clouds, or archive editing.</b> Connecting to WebDAV/SMB and to S3/B2/GCS/Azure/Google Drive/Dropbox/OneDrive, and editing archives in place (delete/rename/add/drag-drop into an existing archive) had no tier gate at any entry point — all Pro features per the published tier table. They now show the upgrade prompt on Free; FTP/FTPS/SFTP, archive browsing/extraction, and creating new ZIPs stay free.</li>
                <li><b>Restore Purchases now tells you what happened.</b> It distinguished nothing: restored, "no purchase on this Apple Account", and "App Store sync failed" all looked like a dead button. It now shows a spinner while running and a per-outcome message. An Ask to Buy purchase (<code>.pending</code>) likewise gets an "awaiting approval — activates automatically" notice instead of silence, a failed product load surfaces the real StoreKit reason instead of a generic "Product unavailable", and a stale purchase error from a previous attempt is cleared when a new attempt starts.</li>
                <li><b>Unifyl &gt; Unifyl Pro… works with every window closed.</b> The upgrade sheet is presented from the main window; with none open the menu item silently did nothing. It now reopens a main window first — keeping the deterministic IAP path App Review asked for actually deterministic.</li>
                <li><b>Advanced Search's Delete reports why files failed.</b> Bulk-deleting search results showed only "N trashed, M failed", discarding the per-file reason — including the new online-only-file guidance. The first failure's message is now appended to the status.</li>
                <li><b>3-way merge no longer silently drops a side it couldn't read.</b> A file that isn't UTF-8 (e.g. CP949/Shift-JIS) or fails to read was treated as empty, so the merge looked clean while one side's entire content vanished from the result. The merge now aborts with a message naming the unreadable file and suggesting encoding conversion first.</li>
                <li><b>Cancelling a delete warns when an in-flight item may still be deleted.</b> Cancel abandons a blocked cloud-provider call by design (so the UI never wedges), but the orphaned call can still complete — the file later disappears with no explanation and no Undo entry. A notice now says so.</li>
                <li><b>Mac App Store build no longer fails to compile in Release/archive.</b> A strict-concurrency isolation error in the 3-way merge's MAS-only code path (<code>normalizedText</code>) broke <code>xcodebuild archive</code> for the MASLite target.</li>
                <li><b>No more surprise "enter your login keychain password" dialogs.</b> Saved connection passwords, licenses, and trial state moved to the data-protection keychain, which is gated by app identity rather than per-item ACLs — so switching between the direct and App Store editions (or a future signing-certificate renewal) can never again trigger macOS's keychain-access prompt. Existing entries migrate automatically and losslessly on first read.</li>
                <li><b>The cursor row no longer drowns its own filename.</b> The keyboard-focus row painted the theme's cursor colour as a solid band while cell text kept its file-type colour — on the default theme that meant dark text on a saturated indigo band. The band is now a 30% tint (text reads against effectively the normal background) and the solid 3-px left-edge stripe continues to mark the cursor position unambiguously.</li>
                <li><b>Marked files are visible again on dark themes.</b> Marked filenames were painted with the theme's <code>selection</code> token — a row-highlight *band* colour that is deliberately dark and near-surface — so on the default Unifyl Dark Pro theme a marked file's text sat at ≈1.6:1 contrast against the background, effectively invisible. Marks now use a dedicated mark-text colour derived from each palette's bright red (TC's red-marking convention), with an adaptive system-red fallback for the automatic and custom themes.</li>
                <li><b>Assorted main-thread stalls removed.</b> AI duplicate scan no longer enumerates the whole folder tree on the main thread; AI Keep-Best / organization recommendations and macro replay run their file operations off-main (multi-GB replays no longer beachball with a frozen progress bar); AI search results can no longer be overwritten by a slower, older query; hovering a cloud-only file's tooltip no longer triggers a blocking download; the copy-progress size probe on slow network volumes moved off-main; the companion server's search no longer wedges every other companion request behind a full-tree walk; and double-clicking an iCloud placeholder twice no longer opens it twice.</li>
                <li><b>The upgrade sheet now recognizes an existing Pro license.</b> Opening Unifyl &gt; Unifyl Pro… after purchasing showed the buy button again; it now shows a "Unifyl Pro is active" confirmation (verified end-to-end in the App Store sandbox: purchase → relaunch → owned state persists).</li>
                <li><b>Cmd+A now marks files in red like every other marking path, instead of blue-washing the whole list.</b> Select All from the keyboard (and select-by-pattern / Invert Selection — anything that sets the marks from outside the table) mirrored the marked set into NSTableView's native row selection: every row got the cursor-colored background with emphasized white text, looking nothing like the same marks made with Insert, right-click, or Edit &gt; Select All (red filename text). The native (blue) selection is reserved for the single cursor row again; externally-set marks repaint as red text, exactly like in-table marking. Drag payloads and batch operations always used the marked set, so only the rendering changes. Locked by tests asserting both select-all entry points produce identical selection state.</li>
                <li><b>Deleting a folder of online-only cloud files no longer downloads everything first — and Cancel actually cancels.</b> The 1.6.1 dataless-placeholder guard checked only the deleted item itself; a *folder* of OneDrive/iCloud online-only files usually stats as materialized, so it slipped into the <code>trashItem</code> path, where the file provider hydrates (downloads) every child before letting the folder leave the sync root — the progress dialog sat at 0/N looking like a file transfer, and Cancel wedged at "Cancelling…" forever because the provider call is synchronous and uninterruptible. Delete now scans for dataless descendants (budget-capped, never descending into online-only directories) and routes such folders to the cloud-delete confirmation, and every blocking trash/remove call is awaited abandonably — Cancel closes the progress UI immediately even while a wedged provider call is still unwinding in the background. The nine remaining direct <code>trashItem</code> call sites (Duplicate Finder, App Uninstaller, Directory Diff, Large File Finder, macro playback, NL command preview, Advanced Search, AI Keep-Best, companion delete) now refuse online-only placeholders with a readable message instead of hanging or failing cryptically.</li>
                </ul>
            ]]></description>
            <pubDate>Tue, 21 Jul 2026 05:29:04 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.2/Unifyl-1.6.2.dmg"
                sparkle:version="36"
                sparkle:shortVersionString="1.6.2"
                sparkle:edSignature="vn4i2AiNil7TtPCAYYevsZrLjRpovqXqHRxc6Li+hymGhGZIZG0cQwlopEOzo6fkXPPSYDupajoqSftq+JA+Bw=="
                length="16998079"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.6.1</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.1</h2>
                <h3>Fixed</h3>
                <ul>
                <li><b>THE chronic drag-breakage root cause: an exception inside AppKit's drag-completion procedure.</b> Caught live with a full backtrace: <code>UnifylTableView.draggingEnded(_:)</code> (added for spring-loaded-folder timer cleanup) called <code>super.draggingEnded(sender)</code> — but <code>draggingEnded:</code> is an *optional* <code>NSDraggingDestination</code> method that NSTableView/NSView do not implement, so the super call raised an unrecognized-selector exception <b>inside <code>NSCoreDragCompletionProc</code>, on every single drag's completion</b>. The aborted completion meant <code>draggingSession:endedAt:</code> was never delivered (confirming why the 10 s watchdog fired after every drag), the session stayed stranded in AppKit's registry, and once enough state accumulated the process wedged: every new drag was refused with AppKit's "Cannot start a new drag. A previous drag has not finished." and left-drags degraded to a confusing band-selection until relaunch. <code>super</code> is now called only when an ancestor actually implements the selector (same guard on <code>draggingExited:</code>), locked by tests that dispatch both selectors through ObjC. Additionally, the missing-<code>endedAt</code> watchdog (list + grid) is now button-aware: it only treats <code>endedAt</code> as swallowed after the primary button has been *up* for ~10 s, so a legitimately long held drag (spring-load browsing, hovering another app) can no longer be force-cleared and reloaded from underneath.</li>
                <li><b>Drags no longer come loose mid-motion after the display-wake fix.</b> The 1.6.x recovery for post-display-sleep drag corruption recreated both panels' file tables on every <code>didChangeScreenParameters</code> notification — but macOS also posts that for Night Shift/gamma transitions, HDR (EDR) headroom changes, and Dock/menu-bar geometry churn, none of which need recovery. If one landed while a file drag was in flight, SwiftUI tore the drag's source view out of the hierarchy and the drag silently died mid-motion. Table recreation is now gated on a *material* display-layout change (screen count, frame, or scale — a wake always qualifies), and any recreation requested during a drag is deferred until the drag fully closes via a new app-wide drag-session census (list + grid, both panels, with a failsafe so recovery can never be blocked forever). The thumbnail grid also gains the 10 s missing-<code>endedAt</code> watchdog the list view already had. Locked by tests covering the census balance, mid-drag deferral, coalescing, and layout-signature stability.</li>
                <li><b>Deleting an online-only cloud file no longer just fails.</b> Moving an OneDrive/iCloud "online-only" file (a dataless placeholder whose data lives in the cloud, 0 bytes on disk) to the Trash failed with a cryptic "the volume doesn't contain the item" — <code>trashItem</code> can't move a file with no local data into the local Trash. Unifyl now detects these, and after a single confirmation deletes them from the cloud instead (recoverable from your provider's own trash, e.g. OneDrive online). Fully-downloaded files in the same cloud folder were never affected.</li>
                <li><b>One-click Restart (⌥⌘R) to recover drag after the display sleeps.</b> macOS can corrupt this process's drag promotion at the window-server level after a display sleep/wake or monitor change — file drags degrade to a rubber-band selection and ⌘-shortcuts stop landing, even though the window stays focused. Unifyl mitigates it (opts out of App Nap; rebuilds the file tables on display wake) which holds for days, but no in-app rebuild fully escapes the corruption once it sets in. So there's now <b>Restart Unifyl…</b> (Unifyl menu, ⌥⌘R, or the command palette): it saves your workspace and relaunches — the reliable fix when drag stops, and the menu still works even while drag/keys are stuck. Tabs and panels come right back.</li>
                <li><b>"Drag stops working until restart" — root cause fixed.</b> After a long session, file drag in both the list and the thumbnail grid could stop working entirely until the app was relaunched. The cause: <code>updateSelection</code> and <code>ensureCursor</code> (list) and <code>applySelection</code> (grid) issued <code>selectRowIndexes</code>/<code>selectItems</code>/<code>scrollRowToVisible</code> straight at the table on *every* background-driven view update — folder-size recalcs, cloud-status polls, file-watcher refreshes — and the drag guard only deferred <code>reloadData</code>, not these selection mutations. One such background update landing during a drag (or its teardown tail) disturbs AppKit's drag session; over hours the coincidences accumulate until the session is corrupted for good. Selection and cursor updates are now deferred during the drag window and flushed when it closes, the same way reloads already were — closing the last unguarded path. Locked by tests asserting both defer mid-drag and flush afterward.</li>
                </ul>
            ]]></description>
            <pubDate>Fri, 10 Jul 2026 10:09:41 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.1/Unifyl-1.6.1.dmg"
                sparkle:version="33"
                sparkle:shortVersionString="1.6.1"
                sparkle:edSignature="5wJnGtieFFZ4V1wGW0Q2/I2qJdnfJlUxRlyWA7nEIz9EF7olGIkFO96wuKqRQwajBzkX/XZhq5R9iloZsQCcCg=="
                length="16939161"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.6.0</title>
            <description><![CDATA[
                <h2>Unifyl 1.6.0</h2>
                <h3>Added</h3>
                <ul>
                <li><b>Compact list density (View → Compact List Density).</b> TC-style tight rows for the file list: row height drops from 28 to 19 px at the default font size, icons shrink to 16 px, and the macOS edge inset goes away — roughly 50% more files visible per screen. On by default; the View menu (and command palette) toggles back to the original airy Comfortable layout, and the shortcut is customizable in Settings → Key Bindings. Pairs well with ⌘− to step the panel font down a notch.</li>
                <li><b>Statistics (Tools → Statistics…).</b> Unifyl now counts what you do with it — bytes copied and moved, files renamed and deleted, archives handled, searches run — bucketed by month, entirely on-device, with a reset button and an off switch (Settings → General). The numbers never leave your Mac; they exist so you can see "2.4 TB moved" and feel something.</li>
                <li><b>A little celebration for big transfers.</b> When a 1 GiB+ copy or move lands with zero failures, the progress card's checkmark does one happy bounce and the toast says something warmer than "done". Respects Reduce Motion, never appears on errors, and Settings → General → "Playful touches" turns it (and the friendly empty-folder lines) off entirely.</li>
                <li><b>A hidden something.</b> Type a certain word into the command palette. Norton Commander fans will know which one.</li>
                <li><b>Automation run history.</b> Every automation run — manual, scheduled, or event-triggered — is now recorded (start time, duration, trigger, success/failure with the error, and a tail of the script output) in a 200-entry persistent log, inspectable from a new Run History sheet in Automation. An automation whose last run failed shows an error badge right in the list with the failure message on hover — a silently failing scheduled job is no longer invisible.</li>
                <li><b>Transient network errors auto-retry.</b> Remote downloads and uploads now retry automatically (1s, then 3s backoff) when the failure is a transient one — timeout, dropped connection, reset — so a Wi-Fi hiccup mid-batch no longer fails the file. Permanent errors (auth, missing file, disk full) still fail immediately; retries are logged to Console.</li>
                <li><b>Content search says when results are capped.</b> The 50-matching-line backend cap now surfaces as an explicit "results are capped" notice instead of a silently truncated list that read as complete.</li>
                <li><b>Folder Heatmap: file-type breakdown.</b> The heatmap now aggregates the scanned tree by file category into a proportional stacked bar with a legend (size, count, share per type, top 8 + Other) — "this folder is 50 GB of video" at a glance, computed in one pass over data the scan already collected.</li>
                <li><b>Multi-Rename shows exactly what happened.</b> After applying a batch rename the sheet switches to a result phase: "Renamed N file(s)" with a per-file old → new list audited against what actually landed on disk, and failed files broken out separately with the reason — no more inferring the outcome from the folder listing.</li>
                <li><b>Verify file integrity after copy and move (Pro).</b> New Settings → Confirmations toggle: after each file copy — and after the copy half of a cross-volume move, *before* the original is deleted — both sides are re-read and their SHA-256 checksums compared (streaming, 1 MB chunks, off the main thread). The progress card shows a "Verifying integrity" phase; mismatches are reported per file and never modify the original (a corrupt cross-volume move removes its bad copy and leaves the source in place). Covers panel copy/move, drag-and-drop moves, rename-collision moves, and clipboard cut-paste. This closes the classic "did 40 GB really arrive intact on the NAS?" anxiety that drives TC users to <code>Verify</code> after every transfer.</li>
                <li><b>Saved Searches.</b> Find Files (Advanced Search) criteria — the full condition list, scope folder, and subfolder toggle — can now be saved as named templates and re-applied from a new "Saved" menu next to Add Condition. Saving under an existing name overwrites that template; templates persist across launches (corrupt persistence is stashed for recovery, never silently dropped). Applying a template fills the form without auto-running, so it can be tweaked first.</li>
                <li><b>Undo History timeline.</b> Edit → Undo History… (also in the command palette) shows every undoable file operation of the session, newest first — operation type, file count, destination, relative timestamp — with each row expandable to the exact files (old → new pairs for renames) and a plain-language note of what undoing it would do ("restores the file(s) from the Trash"). The top entry carries a "Next Undo" badge, and a prominent button steps back one operation at a time, so ⌘⌥Z stops being a leap of faith.</li>
                <li><b>Smart Tagging learns your folder's own taxonomy.</b> Tag suggestions now include the tags already applied to sibling files in the same folder, weighted by how widely each tag is used there — if everything else in the folder is tagged "Invoice", that's the first chip offered. The folder is scanned once per batch (capped at 500 siblings) and every suggestion chip now explains where it came from on hover (folder usage, content keyword, file type, date, language).</li>
                <li><b>AI indexing reports failures and fragile storage.</b> If files fail to write to the semantic index, the progress card and final status now say "N failed" (causes in Console) instead of an unqualified "Done". And if the vector index couldn't open at its normal location and fell back to temporary or in-memory storage — meaning it would vanish on relaunch — the Index pane shows a persistent warning instead of letting you re-index every session without knowing why.</li>
                <li><b>AI Rename explains empty suggestions and retries them in bulk.</b> Rows that produced no suggestion now say why — "file content couldn't be read", "name already looks good", "image couldn't be classified" — instead of a bare dash, and a "Retry Failed (n)" button re-runs just those files. Per-row refresh and the batch path now share one image-detection route (the sheet's own copy was missing TIFF/BMP).</li>
                <li><b>Folder Sync lists what failed, per file.</b> A sync that ends "3 error(s)" now shows which files failed and why (first three inline, full list on hover) instead of a bare count.</li>
                <li><b>Folder Sync shows its execution plan.</b> After Compare, every entry row gets a plan column — "→" / "←" (will copy) or "–" (will skip) with a tooltip explaining exactly why ("modification times within 2s — can't tell which is newer", "not included in current direction", "excluded from this sync"…), and the status line summarizes "will copy X →, Y ←, skip Z". The indicator live-updates with the direction picker, and the executor now routes through the same pure <code>SyncPlanner</code> the preview uses (locked by 14 unit tests), so what you see is exactly what Execute will do.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>Default panel font size is now 11 pt (was 13).</b> Together with the new compact density this matches Total Commander's information density out of the box. Existing users who ever adjusted the size keep their setting; ⌘+/⌘− and Settings → General still range 9–24 pt.</li>
                <li><b>Folders always sort by name, pinned above files.</b> Sorting by Size, Date, or Kind now reorders only the files; folders keep their alphabetical (natural) order at the top — Total Commander's default behavior. Only the Name column's direction applies to folders (Name ↓ reverses them too). Trade-off: ranking computed folder sizes via the Size column no longer reorders folders.</li>
                <li><b>README feature-count claim corrected to match reality.</b> All 15 README translations claimed "75+ professional tools"; the canonical tier map (<code>FeatureTier.swift</code>, 51 Pro features) and every other artifact say 50+. The READMEs now say 50+ too — undercounting beats a claim we can't back.</li>
                <li><b>Design-token sweep across 24 views — one visual language everywhere.</b> Status colors that had drifted to raw <code>.green/.red/.orange/.blue</code> literals (sync results, merge markers, diff lanes, macro recording, install states, progress chips) now use the semantic palette (<code>success/warning/error/info</code>), so every "done", "failed" and "in progress" reads the same hue app-wide. All rounded corners are continuous squircles on the shared radius scale (the deprecated <code>.cornerRadius</code> calls are gone); ad-hoc animation timings collapsed onto the standard motion presets; magic shadows onto the shared elevation tiers. The file-operation progress card drops one over-rendered shadow tier (modal → popover) so it floats instead of looming.</li>
                <li><b>Sheets share one chrome pattern.</b> Compress, AI Rename, AI Classify, Smart Tags, and NL Command Preview previously opened with a bare headline; they now lead with the standard icon + 15pt-semibold title row (AI sheets use the reserved cyan accent). Esc/Return now use the platform <code>.cancelAction</code>/<code>.defaultAction</code> bindings in the file-operation confirm and AI Classify dialogs, which also gives the primary button the correct prominent styling.</li>
                <li><b>Quieter, more responsive bottom chrome.</b> The F1–F8 bar's full-strength divider washboard is halved in opacity and each key now gives a subtle hover wash (it previously read as a static legend); the status bar aligns to the chrome rhythm (22→24pt) and the "Space = preview" hint is legible without hunting. Conflict dialog's warning icon and padding now come from the token palette.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>List row height no longer depends on whether you ever touched the font size.</b> The table started at a hardcoded 28 px but recomputed as fontSize + 10 after any ⌘+/⌘− press, so going 13 → 14 → 13 pt left rows at 23 px — two users at the same font size saw different densities. One formula now drives both the initial value and every change.</li>
                <li><b>Folder Sync uploads to remotes are now atomic.</b> Syncing local → remote (and remote → remote) uploaded straight to the final filename, so a dropped connection mid-transfer left a truncated file sitting where the real one should be — and the next Compare could read it as "identical" by date. Uploads now go to a hidden sibling temp name and are renamed into place on completion; if the server's rename can't land (e.g. an FTP server refusing RNTO onto an existing file), it falls back to the previous direct upload, so behavior is never worse than before. Remote → local sync already had this protection.</li>
                <li><b>F2 rename no longer freezes the app on Return.</b> Committing an inline rename handed keyboard focus back to the table *while the field editor was still resigning*, which re-entered AppKit's end-editing machinery (<code>makeFirstResponder</code> → <code>resignFirstResponder</code> → <code>textShouldEndEditing</code> → repeat) and hung the main thread in infinite recursion. Focus is now restored in <code>controlTextDidEndEditing</code>, after editing has fully ended. The hazardous call shipped as far back as 1.2.0; the hang reproduces on current macOS.</li>
                <li><b>MAS-Lite build: content search no longer fails to compile.</b> The 1.5.0 multi-mask change renamed the filename-filter variable but missed the one use site that only compiles in the Mac App Store target's sandbox search path — and the multi-mask semantics now actually apply there (any mask may match, not just the first).</li>
                <li><b>Batch delete no longer abandons the rest of the batch on one failure.</b> Moving N items to Trash stopped at the first item that errored (e.g. a permission failure) and silently skipped everything after it — the error banner gave no hint the operation was partial. Each item is now attempted independently and a partial result is reported explicitly ("Moved 7 of 10 item(s) to Trash — 3 failed: …").</li>
                <li><b>Copy and paste failures now say how far they got.</b> A mid-batch copy/paste error intentionally stops the batch (so a disk-full doesn't cascade), but the error message now includes "N of M file(s) copied — remaining files were not attempted" instead of leaving the user to guess which files landed.</li>
                <li><b>Undo of a rename no longer claims success when nothing was undone.</b> If the renamed files had since been moved or deleted, ⌘Z reported "Undid rename of N item(s)" while restoring zero files; it now counts actual restores and reports an error when none happened (matching delete-undo behavior).</li>
                <li><b>Cloud folders (rclone remotes) no longer masquerade as empty on a bad listing.</b> If <code>rclone lsjson</code> exited 0 but its output was unparseable (truncated stream, log lines mixed into stdout), the panel showed an empty folder with no error — now it surfaces a listing error instead.</li>
                <li><b>Office preview no longer shows the previous file's content or error after switching files.</b> Navigating from a Word document to a spreadsheet (or from a failed file to a good one) kept the prior PDF/error on screen because per-file state wasn't reset on reload.</li>
                <li><b>Stale error banners cleared on retry.</b> Git History, Media Info, File Diff, Team Settings, and the video preview kept showing an old error across a successful reload; user-visible error state is now reset when the action starts. The video preview also shows an explicit error when no frames can be extracted instead of a blank strip.</li>
                <li><b>Duplicate keyboard-shortcut registrations removed from the toolbar ⋯ menu.</b> ⌘= / ⌘− / ⇧⌘P were registered both in the menu bar and in the toolbar overflow menu, making dispatch ambiguous (shortcuts could be swallowed depending on focus). The menu-bar registrations remain the single source.</li>
                <li><b>Auto-confirmed file operations can no longer run twice.</b> With a confirmation toggle disabled, the operation was executed from a sheet-binding getter that SwiftUI may evaluate more than once per update pass; the execution hop is now idempotent.</li>
                <li><b>Launching a damaged or Gatekeeper-blocked .app now reports an error</b> instead of silently doing nothing; "Open in Terminal Here" and dropping a folder onto the tab bar also beep on failure instead of being silent.</li>
                <li><b>Upgrade banner no longer advances its message rotation during a trial</b>, so the first nudge after trial expiry starts from the entry message as intended.</li>
                </ul>
            ]]></description>
            <pubDate>Sat, 13 Jun 2026 20:09:34 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.6.0/Unifyl-1.6.0.dmg"
                sparkle:version="32"
                sparkle:shortVersionString="1.6.0"
                sparkle:edSignature="dGHIF2D7DF2GkUzcGFkDrae7//v9qx4gad9AH5Fp0j5W5iybHIMjW4ogrZkc5LW6UZHg0L7qqQXK19jjX5xOCg=="
                length="16896470"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.5.0</title>
            <description><![CDATA[
                <h2>Unifyl 1.5.0</h2>
                <h3>Added</h3>
                <ul>
                <li><b>Thumbnail grid keyboard parity (Total Commander navigation in grid mode).</b> The thumbnail grid (⌃G) previously only moved its selection with the arrow keys; it now matches the list view's core navigation: <b>type-ahead</b> (type a few letters to jump to the matching file — case/diacritic-insensitive, Hangul partial-jamo aware), <b>Return</b> to open the selected item, and <b>Backspace</b> to go to the parent folder. The type-ahead matcher is factored into the pure, unit-tested <code>TypeAheadMatcher</code> shared by both views (10 tests), so list and grid stay in lockstep.</li>
                <li><b>Left-drag marquee (rubber-band) selection in the list view.</b> Press and drag from the empty area below the file list to sweep a selection rectangle over rows (hold Shift/Cmd to add to the current marks, Finder-style). The table view now fills its scroll view even with only a few rows, so the empty space below them is part of the table where the drag can begin. Driven by the normal <code>mouseDragged</code>/<code>mouseUp</code> overrides gated strictly on a mouse-down that hits no row, so it leaves NSTableView's file-drag (NSDraggingSession) path untouched; reuses the existing <code>dragSelectRows</code> (which excludes the <code>..</code> parent). Rows are selected by the marquee's vertical span (full-width band) so a straight up/down drag works too.</li>
                <li><b>Compare Directories (Mark Newer) — Total Commander panel comparison.</b> New Compare-menu / command-palette entry marks, in each panel, the files that are unique to that side or newer than the same-named file on the other panel — i.e. the files you'd copy outward. Identical or older files stay unmarked; directories and <code>..</code> are ignored; name matching is case-insensitive with a 2-second mtime tolerance. The decision logic lives in the testable <code>PanelComparison.markNewerAndUnique</code>. Complements the existing diff-window "Compare Directories…". (Pro, gated like the directory diff.)</li>
                <li><b>Select / Unselect by pattern (Total Commander Num+ / Num−).</b> New Edit-menu commands "Select by Pattern…" and "Unselect by Pattern…" (also in the command palette, rebindable in Settings) mark or unmark every item in the active panel matching a Total Commander-style mask. Multiple masks separated by <code>;</code>, <code>,</code> or space are OR'd (<code>*.txt;*.doc</code>); a <code>|</code> splits include from exclude masks (<code>*.* | *.bak;*.tmp</code> = everything except <code>.bak</code>/<code>.tmp</code>). Mask matching is exposed as the reusable <code>SearchEngine.matchesMask(_:mask:)</code>. Shows a count toast after applying. Closes a long-standing TC-parity gap (group selection was previously impossible).</li>
                <li><b>"Open in New Tab" on folders (Total Commander panel tabs).</b> Right-clicking a directory (or the <code>..</code> parent) now offers "Open in New Tab", which opens that folder in a fresh tab of the same panel and switches to it — previously a new tab could only be opened at the current path (⌘T). <code>..</code> resolves to the real parent rather than a path ending in <code>/..</code>.</li>
                <li><b>Drag a tab to reorder it (Total Commander tab reordering).</b> Tabs in a panel's tab bar can now be dragged onto each other to change their order; the active tab stays selected as it moves. The index math lives in the pure, unit-tested <code>CollectionReorder</code> (UnifylShared) so the active-tab-follows-content behavior is locked.</li>
                <li><b>"Open in Other Panel" on folders.</b> Right-clicking a directory also offers "Open in Other Panel", which navigates the opposite pane to that folder — a discoverable surface for the existing keyboard-only "reveal in opposite panel" (handy to line up two folders before a compare / sync). Reuses <code>navigateForce</code>; <code>..</code> resolves to the real parent.</li>
                <li><b>Numpad gray keys for group selection (Total Commander Num+ / Num− / Num\*).</b> In the file list, the keypad <code>+</code> opens Select by Pattern, keypad <code>−</code> opens Unselect by Pattern, and keypad <code>*</code> inverts the selection — TC's muscle-memory selection keys, now bound. Matched by keypad keyCode so the top-row <code>+ − *</code> still flow into type-ahead search. Wired through a new optional <code>FileTableViewDelegate.fileTableViewRequestSelectByMask</code>.</li>
                <li><b>Select Same Extension (Total Commander Alt+Num+).</b> New Edit-menu command adds every file in the active panel sharing the cursor item's extension to the selection — a one-keystroke "select all the <code>.png</code>s" built on the tested <code>SearchEngine.matchesExtensionList</code>.</li>
                <li><b>Test Archive Integrity (Total Commander "Test archive").</b> New File-menu / command-palette entry verifies one or more selected archives without extracting to disk — <code>7z t</code> (per-entry CRC) for ZIP/7z/RAR, <code>unzip -t</code> fallback for ZIP, full-stream walk via <code>tar -tf</code> for the tar family, <code>gunzip -t</code> / <code>zstd -t</code> for single-stream formats, and a listing probe for ALZ. Reports the first corrupt archive with the tool's diagnostics, or a "passed" toast. Backed by <code>ArchiveEngine.test(_:)</code>.</li>
                <li><b>Content search now accepts multiple file masks (Total Commander style).</b> The Search File Contents "Pattern" field takes <code>*.h;*.cpp</code>-style multi-masks (split on <code>;</code>, <code>,</code>, space) on both the Direct grep path (one <code>--include</code> per mask) and the MAS-Lite in-process path (suffix-match against any token). Previously only a single mask was honored.</li>
                <li><b>Find Files "Extension" filter accepts multiple extensions.</b> The advanced-search Extension criterion now matches any of a <code>pdf;doc;txt</code> (also <code>*.pdf, *.doc</code>, <code>.png</code>) list via the new <code>SearchEngine.matchesExtensionList</code>, instead of a single extension. A single bare extension still works.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Drag-and-drop no longer stops working after a long session (chronic AppKit drag-stability bug — root cause).</b> The recurring "drag breaks until you restart the app" symptom was a <code>reloadData()</code> corrupting the table's drag machinery in the brief window AFTER <code>endedAt</code> but before NSTableView finishes unwinding its own drag-session state. Earlier fixes guarded the live <code>willBegin → endedAt</code> window (<code>dragInProgress</code>), but that flag is cleared synchronously at <code>endedAt</code> so a brand-new drag isn't blocked — leaving the post-<code>endedAt</code> unwind tail unguarded. A background <code>updateItems</code> (file-watcher / cloud-poll / folder-size refresh) landing in that tail still fired <code>reloadData()</code> mid-unwind; over a long session the coincidence eventually corrupts the table for good. New <code>dragUnwinding</code> flag extends the reload deferral one runloop turn past <code>endedAt</code> — closing the gap without re-introducing the "second drag won't start" bug, since a new drag's <code>willBegin</code> re-arms <code>dragInProgress</code> before the turn ends. Applied to both the list view (<code>FileTableView</code>) and the thumbnail grid (<code>NSCollectionView</code>). The drag reload-guard state machine is now factored into <code>beginDragGuard</code>/<code>endDragGuard</code> and locked by 5 unit tests (verified to fail on the pre-fix code). A <code>DragStability</code> os_log trail was added so any future recurrence is diagnosable.</li>
                <li><b>Type-ahead search now shows what you've typed (Total Commander search box).</b> Typing in the file list to jump to a file previously gave no visual trace of the accumulated search buffer (Finder-style silent jump), so you couldn't tell what you'd typed so far. A small "🔍 …" capsule now appears at the bottom-left of the list while you type and fades out when the buffer resets. Surfaced through a new optional <code>FileTableViewDelegate.fileTableView(_:typeAheadBufferChanged:)</code>.</li>
                <li><b>SFTP: filenames with multiple consecutive spaces are no longer mangled.</b> The <code>ls -la</code> parser split the whole line on spaces and rejoined the name with single spaces, so <code>my  file.txt</code> (two spaces) became <code>my file.txt</code> — an un-openable name. It now splits only the 8 leading metadata columns and keeps the filename remainder byte-for-byte. New <code>SFTPListingParserTests</code> lock multi-space names, space-padded single-digit days, symlink/dir classification, and <code>total</code>/dot-entry skipping.</li>
                <li><b>SMB: directory entries no longer vanish under a non-English locale.</b> smbclient now runs with <code>LC_ALL=C</code> so its <code>ls</code> date column stays English; the listing parser's date regex matches English weekday/month names only, so a <code>ko_KR</code> / <code>ja_JP</code> client locale previously made every entry fail the regex and disappear from the folder. New <code>SMBListingParserTests</code> pin the parser.</li>
                <li><b>FTP: you can finally enter remote subdirectories.</b> Directory listings used NLST (<code>--list-only</code>), which returns bare filenames with no type information — so every subfolder showed up as a file you couldn't open, making remote trees un-navigable past the top level. Listings now use a full <code>LIST</code> (the parser already understood the Unix <code>drwx…</code> form; added a Windows/IIS <code><DIR></code> branch) and fall back to NLST only when the server's format is unrecognized. New <code>FTPListingParserTests</code> lock Unix/IIS/NLST parsing, spaces-in-names, symlink-target stripping, and dot-entry skipping.</li>
                <li><b>Quick Terminal (backtick) now expands <code>$F</code> / <code>$D</code> / <code>$S</code> / <code>$f</code> / <code>$e</code> / <code>$n</code>.</b> The Quake-style drop-down terminal sent the raw command straight to the shell, so a <code>$F</code> was passed literally instead of the cursor file — unlike the bottom Integrated Terminal, which already expanded them. Both terminals now share the same <code>TerminalVariableExpander</code> behavior.</li>
                <li><b>Folder Sync no longer treats local and remote hidden files asymmetrically.</b> The local tree was enumerated with <code>.skipsHiddenFiles</code> while the remote tree included dotfiles, so a remote <code>.DS_Store</code> / <code>.git</code> looked "right only" and could be copied down (or deleted) while local dotfiles were ignored outbound. The remote walk now skips dotfiles / dot-directories too, matching the local side.</li>
                <li><b>Folder Sync result count no longer overstates work done.</b> A direction mismatch (e.g. a left-only file under a right→left sync), or a bidirectional tie / missing modification date, performs no copy but was still counted as "synced". Those entries are now reported as "skipped" and the completion message breaks out <code>synced / skipped / errors</code>.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>Cleaner chrome + a useful toolbar center — less clutter, more function around the file panels.</b> The toolbar's principal slot keeps the <b>Unifyl</b> brand lockup (app icon + wordmark) and turns the once-inert current-folder label into an <b>action hub</b>: a dropdown that operates on the active panel's location — Reveal in Finder, Copy Path, Open in Terminal Here, Add to Favorites, and a Recent Folders submenu. The font-size stepper, theme picker and command-palette button collapse into a single trailing <b>⋯</b> menu, leaving filter / hidden-files / help as the only always-visible right-edge controls (every entry keeps its keyboard shortcut). The bottom upgrade nudge loses its orange gradient pill for a quiet accent text link on the standard chrome surface, picks up the same 1px hairline as the other bars, and can now be dismissed with a <b>×</b>. Status and path bars are repadded from the shared spacing tokens so the chrome rhythm is consistent.</li>
                <li><b>Upgrade sheet pricing presentation — reframe the one-time price to reduce sticker shock.</b> Same $39.99, no change to what's billed; the sheet now does the value framing the bare number never did. Four levers, all honest by construction: (1) one-time emphasis — <code>Buy Pro — $39.99 once</code> plus a <code>No subscription. Yours forever on this Mac.</code> caption on both channels, the strongest lever in a subscription-fatigued market; (2) a competitor value anchor (<code>Replaces ForkLift ($39.95) + Transmit ($45) + an AI toolkit — one app.</code>) so the single price reads as a bundle discount; (3) an optional Direct-only struck-through regular price driven by <code>LicenseConfig.regularPriceDisplay</code> — <b>disabled by default (<code>nil</code>)</b>, only to be enabled with a price the Pro variant is actually listed at on LemonSqueezy (a real list price backed by a permanent launch discount, never a fictitious one; also never shown on MAS, where Apple forbids fictitious reference prices); (4) Direct-only approximate local-currency hint (<code>≈ ₩54,000 · billed via LemonSqueezy</code>) for non-USD regions via <code>LicenseConfig.approxLocalPriceForCurrentLocale</code>, explicitly marked approximate since the real charge happens at checkout (enable LemonSqueezy "show prices in customer's local currency" so the two match). MAS already shows StoreKit's localized <code>displayPrice</code>, which is what Apple charges, so it needs neither (3) nor (4).</li>
                </ul>
            ]]></description>
            <pubDate>Tue, 09 Jun 2026 21:44:52 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.5.0/Unifyl-1.5.0.dmg"
                sparkle:version="31"
                sparkle:shortVersionString="1.5.0"
                sparkle:edSignature="q6a6NBxoxTACAFMp3V/O47PDV+zfv0J4lUD3niWDt6tEI8xGGLo6CwtrKZ8s8b6XcIu2PIJi6TzSRH4X0skJCQ=="
                length="16537446"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.3.2</title>
            <description><![CDATA[
                <h2>Unifyl 1.3.2</h2>
                <h3>Changed</h3>
                <ul>
                <li><b>MAS-Lite backlog reaches zero <code>[drop-pending-lib]</code> — every Process() site in the app target is now either truly in-process migrated or a documented <code>[drop]</code>.</b> Two final-pass moves:</li>
                </ul>
                <p>  - **FileXRayView.ZipArchiveReader.entries migrated to `InProcessZipReader`** — was previously `[drop]` returning empty in MAS-Lite (and `/usr/bin/zipinfo` subprocess in Direct). Now both targets share one in-process path; the FileXRay ZIP analysis tab works in MAS-Lite, and Direct also drops the zipinfo spawn. Mapping layer between `InProcessZipReader.Entry` (UInt32 sizes) and the local `XRayNode`-shaped `Entry` (UInt64). FileXRayView gains an `import UnifylFileSystem`.</p>
                <p>  - **Git (3 sites) + MediaConverter ffmpeg-check (1 site) reclassified `[drop-pending-lib]` → `[drop]`.** Both families need work beyond a single sprint: Git needs a libgit2 SPM dependency (SwiftGit2 / ObjectiveGit — license vetting, MAS submission review of the bundled libgit2 binary, Swift 6 strict-concurrency audit, and full operation mapping); `which ffmpeg` has no Foundation equivalent and MAS forbids unsigned CLI binaries in `Resources/`. The comments now spell out the deferral rationale so a future contributor sees what the lift would be instead of seeing &quot;pending-lib&quot; and assuming it&#x27;s small. MAS-Lite users see localized &quot;Git integration is not available in the Mac App Store variant — use unifyl.app for git&quot; / &quot;ffmpeg unavailable&quot; messages exactly as in the prior pass; no UX change.</p>
                <p>  - **MAS-INCOMPAT site breakdown: 17 `[drop]`, 0 `[drop-pending-lib]`, 0 `[lib]`.** Every `Process()` spawn in the MAS-Lite binary is documented and either compiled out via `#if MAS_LITE` or routed through an in-process equivalent. The 6 in-process equivalents added across 1.3.x-Unreleased sprints: pure-Swift content search (grep → FileManager enumerator), `InProcessZipReader` (ditto-on-xlsx → in-process xlsx preview), `InProcessZipWriter` (zip → Compress dialog), `InProcessTarWriter` (tar / tar.gz → Compress dialog), `Diff3Merger` (diff3 → 3-way merge), `PDFConvertSheet.textutil` → `NSAttributedString`. UnifylFileSystem test count: 102 → 128 (26 new tests for the new libraries).</p>
                <h3>Added</h3>
                <ul>
                <li><b>New <code>InProcessMachOReader</code> + <code>InProcessDMGReader</code> (Packages/UnifylFileSystem) + FileXRay deep parsers now sandbox-safe.</b> Pure-Swift Mach-O parser (~280 LOC) replacing <code>/usr/bin/otool -l</code>: parses magic (32/64/fat-universal, both endianness), header (cputype/cpusubtype/filetype/ncmds/flags), and load commands with typed per-LC interpretation (LC_SEGMENT[_64] names, LC_LOAD_DYLIB paths + versions, LC_UUID canonical string, LC_RPATH, LC_VERSION_MIN_*, LC_BUILD_VERSION platform + version, LC_MAIN entry-point offset). Returns a structured <code>Analysis { isFat, architectures, slices }</code> so the FileXRay tree view can group fat slices and show typed fields per LC instead of text-scraping otool output. Companion <code>InProcessDMGReader</code> detects UDIF disk images by the trailing "koly" magic and reads the resource-file trailer (total size + data fork + resource fork). Deep DMG inspection (partition tables, encrypted volumes) needs hdiutil + privileges and isn't sandbox-compatible — out of scope. <b><code>FileXRayView.analyzeMachO</code> + <code>analyzeDiskImage</code> migrated</b> — both targets share the new in-process path, the now-unused <code>runProcess(path:arguments:)</code> removed entirely from the file. The Mach-O tree view actually surfaces more information than the previous otool text-scrape (fat slices are first-class, per-LC fields are typed). 7 new tests against system <code>/bin/ls</code> (fat universal binary fixture) + synthetic UDIF trailer + truncated-file rejection. UnifylFileSystem test count 128 → 135. MAS-INCOMPAT site count drops by 2 (otool + hdiutil sites collapse into one shared <code>[drop]</code>-comment block now covering only mdls, which the codebase doesn't actually call).</li>
                <li><b>New <code>Diff3Merger</code> (Packages/UnifylFileSystem) + 3-way merge now sandbox-safe.</b> Pure-Swift LCS-based 3-way merge replacing <code>/usr/bin/diff3 -m</code> in MAS-Lite. ~250 LOC. Algorithm: compute LCS(base, left) and LCS(base, right) — each gives a monotone (base_idx, other_idx) pair sequence — then take the intersection on base index as the stable anchor set; everything between anchors is a hunk classified by comparing the base / left / right slices (all-same → emit base, one-side-changed → take that side, both-changed-identically → take the change, otherwise → emit <code><<<<<<< LEFT … ======= … >>>>>>> RIGHT</code> markers identical to <code>diff3 -m</code> so <code>ThreeWayMergeView.parseConflicts</code> reads the output unchanged). <b><code>ThreeWayMergeView.analyzeMerge</code> <code>#if MAS_LITE</code> branch migrated</b> — reads file bytes off-main via <code>Task.detached</code>, calls <code>Diff3Merger.merge</code>, returns the merged text + conflict list to the existing UI. Direct keeps <code>/usr/bin/diff3</code>. 8 new tests: no-change / left-only / right-only / both-same / true-conflict / non-overlapping changes / left-side insertion / marker round-trip. Line-granular (not character-level), O(N×M) memory — covers source-file 3-way merges up to a few thousand lines per side. UnifylFileSystem test count 120 → 128. MAS-INCOMPAT <code>[drop-pending-lib]</code> backlog: 5 → 4.</li>
                <li><b>New <code>InProcessTarWriter</code> (Packages/UnifylFileSystem) + Compress dialog tar / tar.gz paths now sandbox-safe.</b> Companion to InProcessZipWriter: ~250 lines covering POSIX USTAR (512-byte header per entry with octal-text size + mtime + mode + 8-byte checksum; payload zero-padded to 512-byte boundary; two trailing zero blocks marking end-of-archive) plus an optional gzip wrapper (10-byte header + raw DEFLATE via <code>compression_encode_buffer(COMPRESSION_ZLIB)</code> + 8-byte CRC-32 / ISIZE footer per RFC 1952). Round-trips through host <code>/usr/bin/tar</code> (<code>-tf</code> lists every entry, <code>-xf</code> restores byte-for-byte; same for <code>-tzf</code> / <code>-xzf</code> on the gzip path). Long-path handling splits paths > 100 chars into USTAR <code>prefix</code> + <code>name</code> on the latest <code>/</code> boundary that fits; rejects paths beyond 255 chars with typed <code>.pathTooLong</code>. <b><code>CompressDialogView.compressTarWithProgress</code> <code>#if MAS_LITE</code> branch migrated</b> — MAS-Lite can now create <code>.tar</code> and <code>.tar.gz</code> archives in-process with per-entry progress. Direct keeps <code>/usr/bin/tar</code>. <b>CRC-32 implementation extracted to a shared <code>InternalCRC32</code> enum</b> so the ZIP writer and gzip footer share one table + one routine instead of duplicating. 5 new tests: plain TAR round-trip / TAR directory tree / <code>.tar.gz</code> round-trip + gzip-magic byte check / unicode filenames / progress callback. UnifylFileSystem test count 115 → 120. MAS-INCOMPAT <code>[drop-pending-lib]</code> backlog: 6 → 5.</li>
                <li><b>New <code>InProcessZipWriter</code> (Packages/UnifylFileSystem) + Compress dialog ZIP path now sandbox-safe.</b> Sibling to InProcessZipReader: a 300-line pure-Swift PKZIP writer using Foundation's <code>compression_encode_buffer</code> (COMPRESSION_ZLIB → raw DEFLATE, same encoding as the reader's decode side) plus a standard reflected polynomial CRC-32 table. Walks source URLs depth-first via <code>FileManager.enumerator</code> with <code>nextObject()</code> (Swift 6 async-safe); resolves symlinks before building entry-relative paths (<code>/tmp</code> ↔ <code>/private/tmp</code> on macOS used to bite the recursion). Per-entry: DEFLATE first, fall back to STORED when DEFLATE bloats incompressible data (jpg / mp4 / random). UTF-8 filename flag set (bit 11 of general-purpose). Refuses ZIP64 (4 GiB single-entry cap), passwords (ZipCrypto is weak, AES is heavier), and ZIP encryption — those stay on the Direct <code>/usr/bin/zip -e -P</code> path. <b><code>CompressDialogView.compressZipWithProgress</code> <code>#if MAS_LITE</code> branch migrated</b> — MAS-Lite can now create ZIP archives in-process with progress reporting per entry. Direct keeps <code>/usr/bin/zip</code> for raw speed. 7 new tests cover single-file / multi-file / nested directory tree / unicode filenames / incompressible STORED fallback / password rejection / progress callback firing per entry. UnifylFileSystem test count 108 → 115. MAS-INCOMPAT <code>[drop-pending-lib]</code> backlog: 7 → 6.</li>
                <li><b>New <code>InProcessZipReader</code> (Packages/UnifylFileSystem) + xlsx preview now sandbox-safe.</b> A 200-line pure-Swift ZIP reader that parses End-Of-Central-Directory + central directory + local file headers, and decompresses STORED (method 0) + DEFLATE (method 8) entries via Foundation's Compression framework (<code>COMPRESSION_ZLIB</code> operates on raw DEFLATE — exactly what ZIP entries store). Sized to the few use cases that need it: xlsx / docx / pptx central-directory pull. Rejects ZIP64 (sentinel <code>0xFFFFFFFF</code> on size fields) and unsupported compression methods with typed <code>ReaderError</code>. <b>OfficePreviewView.parseAllSheets migrated to use it for both Direct AND MAS-Lite</b> — both targets now read xlsx in-process with zero <code>ditto</code> subprocess, zero tmp dir, identical XML-parsing pipeline. ArchiveEngine still owns the broader read/write surface for Direct (7zz + unalz); InProcessZipReader is the narrow sandbox-safe alternative. 6 new tests cover round-trip / unicode filename / missing entry / non-zip rejection / multi-block DEFLATE. UnifylFileSystem test count 102 → 108. MAS-INCOMPAT <code>[drop-pending-lib]</code> backlog: 8 → 7.</li>
                <li><b>MAS-Lite: pure-Swift content search (grep → FileManager enumerator).</b> ContentSearchView's <code>#if MAS_LITE</code> path now does a real in-process directory sweep instead of returning an empty stub. Uses <code>FileManager.default.enumerator(at:, options: [.skipsHiddenFiles, .skipsPackageDescendants])</code> walked via <code>.nextObject()</code> (Swift 6 async-safe), filters by extension (the existing <code>*.swift</code> / <code>*.txt</code> glob input), respects case-sensitivity, caps individual file size at 16 MB, caps total matches at 50 (same as the Direct path's <code>grep -m 50</code>). Tries UTF-8 then ISO-Latin-1 before skipping unreadable files. Identical <code>[ContentSearchResult]</code> shape as the grep path so the view doesn't branch. Direct builds keep <code>/usr/bin/grep</code> for raw speed. MAS-INCOMPAT <code>[drop-pending-lib]</code> backlog: 9 → 8.</li>
                <li><b>Git + Compress stub strings translated to all 15 locales.</b> "Git integration is not available…" + "Compress is not available…" now ship in ko / ja / zh-Hans / zh-Hant / fr / de / es / pt-BR / it / ru / ar / th / vi / tr (en baseline). Audit-guard F4 remains 0 drift.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>All remaining <code>[lib]</code> Process() sites in the app target now <code>#if MAS_LITE</code>-wrapped.</b> After this pass the MAS-INCOMPAT backlog is 14 <code>[drop]</code> (fully wrapped) + 9 <code>[drop-pending-lib]</code> (wrapped now, true library replacement deferred — git → SwiftGit2, zip/tar → ArchiveEngine, grep → pure-Swift sweep, diff3 → pure-Swift 3-way merge, ditto on xlsx → ArchiveEngine entry API, etc.). Sites wrapped in this pass:</li>
                </ul>
                <p>  - `GitPanelView.runGit`, `GitHistoryView.GitHistoryHelper.runGit`, `GitStatusProvider.runGitCommand` — returns a localized &quot;Git integration is not available in the Mac App Store variant…&quot; stub (3 sites in the git family share one stub string).</p>
                <p>  - `ThreeWayMergeView.analyzeMerge` — diff3 absent, MAS-Lite shows empty merge result.</p>
                <p>  - `ContentSearchView.runGrep` — grep absent, MAS-Lite returns empty content-search results.</p>
                <p>  - `OfficePreviewView.parseAllSheets` — ditto-on-xlsx absent, MAS-Lite shows the existing fallback preview.</p>
                <p>  - `CompressDialogView.compressZipWithProgress` / `compressTarWithProgress` — zip/tar binaries unavailable, MAS-Lite throws a localized &quot;Compress is not available…&quot; error (2 sites share one stub string).</p>
                <p>  - `MediaConverterView.checkFFmpeg` — reports `ffmpegAvailable=false` immediately so the Convert button shows the existing hint.</p>
                <p>  - `PDFConverter.convertWithLibreOffice` — LibreOffice absent, returns false.</p>
                <p>  - `FileXRayView.runProcess` + `ZipArchiveReader.entries` — otool / hdiutil / mdls / zipinfo absent, deep file analysis tab degrades gracefully.</p>
                <p>  - Total `#if MAS_LITE` directives in the codebase: 19 → 36.</p>
                <h3>Added</h3>
                <ul>
                <li><b>MAS-Lite first-launch onboarding NSOpenPanel.</b> Sandboxed App Store builds can't read any folder until the user explicitly grants access via a <code>files.user-selected.read-write</code> open panel. <code>MainWindowView.promptForMASRootPickerIfNeeded()</code> fires once on first launch (after the existing onboarding overlay dismisses), opens an NSOpenPanel rooted at <code>~/Downloads</code> (entitlement granted), and on selection saves a <code>withSecurityScope</code> bookmark via <code>SecurityBookmarkManager.saveBookmark(for:)</code> + navigates the active panel there. <code>unifyl.mas.hasSeenRootPicker</code> UserDefaults flag stops the prompt re-firing on subsequent launches. Direct builds compile this block out — no behavior change for the notarized DMG distribution.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>MAS-Lite startup is now sandbox-aware end to end.</b></li>
                </ul>
                <p>  - `AutoBookmarkScanner.detected()` returns `[]` in MAS_LITE — the 12 known messenger / cloud paths all live under `~/Library/Containers/&lt;other-app&gt;/` or `~/Library/CloudStorage/`, neither of which the sandbox grants access to. Probing them with `fileExists(atPath:)` would either silently fail or trigger TCC dialogs at app launch. MAS users add bookmarks manually via Cmd+Shift+O.</p>
                <p>  - `CloudMountScanner.scan()` returns `[]` in MAS_LITE — `~/Library/CloudStorage/` listing is sandbox-blocked. The Connections sidebar no longer shows phantom &quot;Permission denied&quot; cloud mount entries on App Store builds.</p>
                <p>  - `AppViewModel.init` only restores tabs whose security-scoped bookmark is on file (`SecurityBookmarkManager.hasBookmark(forPath:)` — new cheap lookup that doesn&#x27;t resolve, used inside `init` where async work isn&#x27;t possible). Falls back to `~/Downloads` (entitlement granted) so the panel has SOMETHING to show before the onboarding NSOpenPanel fires.</p>
                <ul>
                <li><b>Function Key Bar default flipped back to ON.</b> 1.0.8's design overhaul had defaulted the TC-style F1–F8 hint bar to OFF on the theory that "Mac-natives don't want it." User feedback after 1.3.1 indicated TC migrators expect it visible at first launch — that's the whole point of the "TC parity + Mac polish" pitch. <code>@AppStorage("unifyl.showFunctionKeyBar")</code> default flipped <code>false</code> → <code>true</code> in MainWindowView, GeneralSettingsView, and the View-menu toggle. Users who explicitly toggled it OFF previously keep that preference (UserDefaults key already set); only new installs and users who accepted the prior default see the change. The View → Show Function Key Bar toggle and the Settings → General toggle still work for anyone who wants to hide it. The first-launch <code>hasSeenFnGuide</code> banner above the bar explains that MacBook users need the Fn modifier (default top-row maps to brightness / volume / Mission Control).</li>
                </ul>
            ]]></description>
            <pubDate>Thu, 28 May 2026 07:56:38 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.3.2/Unifyl-1.3.2.dmg"
                sparkle:version="28"
                sparkle:shortVersionString="1.3.2"
                sparkle:edSignature="88T+9l/zyZTR5dL6HL4XQOanGHs4hNoH5KTFPGNpYOBu9UI28f6wGc3u00asrhN3HMXwNw3LXuIEF6R4egExCQ=="
                length="16378813"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.3.1</title>
            <description><![CDATA[
                <h2>Unifyl 1.3.1</h2>
                <h3>Changed</h3>
                <ul>
                <li><b>MAS-Lite migration: first <code>[lib]</code> site replaced with native API + 3 more <code>[drop]</code> sites wrapped</b>:</li>
                </ul>
                <p>  - **PDFConvertSheet.textutil → NSAttributedString in-process** (true `[lib]` win): `convertDocToPDF` for legacy `.doc` / `.docx` / `.rtf` / fallback no longer spawns `/usr/bin/textutil`. Reads `Data(contentsOf:)` off-main, then constructs `NSAttributedString(data:options:documentAttributes:)` on the main actor (TextKit Cocoa importers sniff the document type from the data prefix — the same path textutil(1) wraps internally). Renders to PDF via a new `Self.renderAttributedToPDF(_:destination:)` helper extracted from the existing `convertTextToPDF` so both code paths share one NSPrintOperation pipeline.</p>
                <p>  - **AboutView git fallback wrapped with `#if !MAS_LITE`**: the dev-build commit-count / short-hash lookup (only ever runs when CFBundleVersion is the placeholder &quot;1&quot;, which release builds override) is gone from the MAS-Lite binary entirely. About-view footer degrades to plist values, which is correct for the App Store distribution.</p>
                <p>  - **PDFConvertSheet osascript Office-to-PDF wrapped with `#if !MAS_LITE`**: the iWork / Microsoft Office AppleScript automation path would need a per-target `com.apple.security.scripting-targets` entitlement plus explicit consent. Reclassified as `[drop]` for MAS-Lite; Direct keeps the existing AppleScript flow.</p>
                <p>  - **AudioConverterView afconvert wrapped with `#if MAS_LITE`**: `/usr/bin/afconvert` is Apple-bundled but `Process()` is still sandbox-banned. Returns a localized `ConversionResult(success: false, message: …)` in MAS variant. A proper `[lib]` replacement (AVAudioConverter + ExtAudioFile in-process) is tracked as a future sprint.</p>
                <p>  - **One new locale key × 15 locales**: `&quot;Audio conversion is not available in the Mac App Store variant. Use the direct download from unifyl.app to convert audio files.&quot;` translated to ko / ja / zh-Hans / zh-Hant / fr / de / es / pt-BR / it / ru / ar / th / vi / tr (en baseline). Audit-guard F4 remains 0 drift.</p>
                <p>  - **Three mislabeled MAS-INCOMPAT tags corrected**: `[lib] system_profiler` → actually git, `[lib] ffmpeg` → actually afconvert, `[lib] LibreOffice subprocess` → actually osascript. The 26-site backlog now reflects the real binaries.</p>
                <p>  - Verification: `make audit` → 13/13 PASS, 0 WARN, 0 FAIL. Both `Unifyl (Debug)` and `Unifyl MASLite (Debug)` `xcodebuild` BUILD SUCCEEDED. UnifylCore tests 131/131, UnifylFileSystem 102/102.</p>
                <h3>Added</h3>
                <ul>
                <li><b>Separate UnifylMASLite Xcode target shipped</b>: <code>project.yml</code> now defines a parallel application target that produces a Mac App Store-ready build alongside the Direct distribution. <code>com.unifyl.app.maslite</code> bundle ID (vs <code>com.unifyl.app</code>), separate <code>Info.MASLite.plist</code> (no Sparkle keys), separate <code>Unifyl.MASLite.entitlements</code> (sandbox=on, user-selected files RW, security-scoped bookmarks, network.client, print), <code>SWIFT_ACTIVE_COMPILATION_CONDITIONS=MAS_LITE</code> so all 9 <code>[drop]</code> wraps fire, no Sparkle SPM dependency, no 7zz/unalz <code>Resources/bin/</code> postBuildScript. Two Xcode schemes (<code>Unifyl MASLite (Debug)</code> + <code>(Release)</code>) for everyday dev and archive. Sparkle imports in <code>UnifylApp.swift</code> / <code>AboutView.swift</code> / <code>CheckForUpdatesView.swift</code> (and their menu/button call sites) are <code>#if !MAS_LITE</code>-gated so neither variant pulls the framework needlessly. Verified: <code>xcodebuild</code> of both targets succeeds; the built <code>UnifylMASLite.app</code> has <code>com.apple.security.app-sandbox</code> live, no <code>Frameworks/Sparkle.framework</code>, no <code>Resources/bin/</code>. The two <code>.app</code> bundles can be installed side-by-side on one Mac.</li>
                </ul>
                <h3>Added</h3>
                <ul>
                <li><b>MAS-Lite scaffold complete — 9 of 9 <code>[drop]</code> Process() sites wrapped</b>: <code>#if MAS_LITE</code> guards now cover every subprocess invocation classified as "drop in MAS variant" — ScriptRunner.launch, InlineTerminalView.ProcessRunner.run, CommandLineBar shell exec, SSHTunnelView.startTunnel, DockerExplorerView.runShellCommand, PortViewerView.runShellCommandForPorts, ProcessFileMapView.runShellCommandForProcessMap, NewConnectionSheet.runTestProcess (ssh-keygen), MediaConverterView.convertWithFFmpeg. Each MAS_LITE branch returns or throws a localized "feature unavailable in the Mac App Store variant — use the direct download from unifyl.app …" message via a typed <code>LocalizedError</code> enum (per audit-guard F11). Both <code>xcodebuild</code> (default) and <code>xcodebuild OTHER_SWIFT_FLAGS=-DMAS_LITE</code> BUILD SUCCEEDED. The remaining <code>[lib]</code> sites (git, ffmpeg main path, textutil etc.) need actual library replacements rather than wraps — separate effort.</li>
                <li><b>All 15 locales now translated for F7-lite + MAS-Lite strings</b>: 9 new entries × 14 secondary locales = 126 translations on top of en baseline. <code>make audit</code> reports parity across en / ko / ja / zh-Hans / zh-Hant / de / fr / es / pt-BR / it / ru / tr / ar / th / vi with zero WARN, zero FAIL. Every CJK / European / SEA / RTL user sees the F7-lite badge ("📋 Korean government / public-institution form (heuristic)" + "%lld signature phrase(s) matched") and any MAS-Lite stub messages in their language.</li>
                </ul>
                <h3>Added</h3>
                <ul>
                <li><b>F7-lite UI badge in HWPX preview</b>: HWPXTextPreview now runs the gov-form heuristic on the extracted body and shows a tinted pill above the text when <code>isLikelyGovForm</code>. Hover the badge to see every matched signature phrase. Respects the <code>unifyl.feature.koreanGovFormDetection</code> opt-out.</li>
                <li><b>F7 fingerprint database scaffold</b>: <code>KoreanGovFormDatabase</code> + bundled <code>Resources/data/kr-gov-forms.json</code> seed with 3 placeholder entries (표준 도급 계약서 / 부가가치세 신고서 / 민원 신청서) + SHA-256 + bytes-level matcher. Curation guide at <code>scripts/data/README-kr-gov-forms.md</code> documents the schema and 50-form rollout target. 8 unit tests on the loader + match logic.</li>
                <li><b>MAS-Lite compile-flag scaffold</b>: <code>Unifyl/Automation/ScriptRunner.swift</code> demonstrates the canonical <code>#if MAS_LITE / #else</code> wrap pattern — throws a typed <code>LaunchError.unavailableInMASVariant</code> instead of running <code>Process()</code> when the flag is set. Both <code>xcodebuild</code> (default Direct path) and <code>xcodebuild OTHER_SWIFT_FLAGS=-DMAS_LITE</code> (MAS-Lite path) succeed. Future MAS-Lite target just sets <code>SWIFT_ACTIVE_COMPILATION_CONDITIONS=MAS_LITE</code> and incrementally wraps the remaining 11 <code>[drop]</code> sites. Updated <code>docs/business/mac-app-store-readiness.md</code> to mark the audit prep-work checkbox ✅.</li>
                </ul>
                <h3>Added</h3>
                <ul>
                <li><b>CJK wedge F7-lite — Korean government form heuristic detection</b>: new <code>KoreanGovFormDetector</code> enum recognises 31 high-density Korean form signature phrases (주민등록번호 / 사업자등록번호 / 신청인 / 신고인 / 결재자 / 기안자 / 별지 제 / 호 서식 / 작성일자 / 신청합니다 / 위와 같이 / 우편번호 / 도로명주소 / 직인 / 귀하 / 갑(이하 / 을(이하 etc.). Threshold of 3 distinct hits keeps single-mention false positives out of the badge set. 256 KB scan window caps worst-case latency at ~100 ms on a 1 MB document. Pure-text input (caller extracts body via HWPXParser F2). 9 unit tests including 4 real-world templates (신청서, 도급 계약서, 부가가치세 신고서). Foundation work for the full F7 fingerprint-database UI; badge wiring lands in a follow-up.</li>
                <li><b>50K-item ASCII filter benchmark</b>: PerformanceTests gained two new cases — empty-query identity (sub-ms) and 50K ASCII substring (270 ms, under the 500 ms type-ahead budget). The suite now covers 10K and 50K across empty / ASCII / CJK / multilingual / fullwidth / regex / glob paths.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>MAS-INCOMPAT comment sweep — 26 <code>Process()</code> sites tagged</b>: all subprocess invocations in the app target (<code>Unifyl/Views/*</code>, <code>Unifyl/Services/*</code>, <code>Unifyl/Automation/*</code>) now carry a <code>// MAS-INCOMPAT: [drop|lib] <migration note></code> comment one line above. Future Mac App Store conversion can <code>grep -r MAS-INCOMPAT Unifyl/ | sort -u</code> to see the entire migration backlog with per-site replacement notes (git → SwiftGit2, ffmpeg → AVAssetExportSession, tar/ditto → Compression framework, etc.). Comments-only change — zero behaviour delta.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Force-unwrap in AzureBlobFileSystem paging loop</b>: the <code>do { ... } while marker != nil && !marker!.isEmpty</code> condition relied on a manual nil-check + force-unwrap. Rewrote as <code>marker.map { !$0.isEmpty } ?? false</code> so a future contributor doesn't trip over the unwrap if they refactor the surrounding code.</li>
                </ul>
                <h3>Added</h3>
                <ul>
                <li><b>Universal undo for encoding conversion</b>: the "Convert to UTF-8" Replace-Original path now writes a <code>.unifyl-bak.<unixtime>.<ext></code> sidecar with the original bytes BEFORE overwriting. The batch is recorded as a single <code>FileOperationHistory.encodingConvert</code> op so Cmd-Z restores every converted file at once. Power-cut safe — the backup lands before the rewrite, leaving recoverable bytes on disk even if the in-place write fails mid-flight.</li>
                <li><b>10K-item search performance benchmark</b>: new <code>PerformanceTests</code> suite in UnifylCore that exercises ASCII substring / CJK substring / multilingual expansion / fullwidth fold / regex / glob paths against a 10K-item synthetic listing. Runs as part of <code>swift test</code>; budget assertions catch regressions before release.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>Hangul → Hanja search expansion: 563 ms → 36 ms (15.6× faster on 10K items)</b>. The expansion can produce 200+ single-character Hanja candidates for a 2-syllable query; the old per-item linear loop over all candidates was the dominant per-keystroke latency on Korean-content directories. Switched to a Set<Character> membership scan over haystack chars for single-character expansion candidates; multi-character candidates (original query, Romaji hira/kata segments, fullwidth-folded forms) still use the substring path. Net effect: Korean type-ahead is now instant on listings up to 50K items.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Encoding conversion sheet was blocking main actor during disk I/O</b>: <code>runDetection</code> / <code>performConversion</code> now hop to <code>Task.detached</code> so per-file read + decode + write don't freeze the spinner during a multi-file batch. <code>Detection</code> and <code>ConversionResult</code> gained explicit <code>Sendable</code> conformance for the boundary crossing.</li>
                <li><b>Fire-and-forget <code>Task</code> after encoding conversion</b>: the prior code chained <code>Task { sleep(2); refreshPanel; dismiss }</code> which raced manual sheet dismissal — closing the sheet during the sleep still fired the refresh against a stale view tree. Replaced with synchronous <code>await</code> in the same MainActor context so ordering is deterministic.</li>
                </ul>
                <h3>Added</h3>
                <ul>
                <li><b>CJK wedge F4 — Messenger / cloud-sync folder auto-bookmarks</b>: KakaoTalk Downloads, KakaoTalk Images, WeChat Files (both sandboxed and <code>~/Documents</code>), LINE Files, Whale Downloads, Naver MyBox, Baidu NetDisk, iCloud Drive, Pictures/Screenshots, Telegram, Slack — auto-detected and surfaced in a new "Auto-detected" section above the user's curated bookmarks. Each row gets context-menu actions (Open / Hide / Promote to Bookmark). Dismiss persistence via UserDefaults key <code>unifyl.bookmarks.autoDismissed</code>; feature gate <code>unifyl.feature.autoBookmarks</code>. 8 unit tests covering empty / single / multi / dismiss round-trip / canonical-id / idempotence on a fake HOME.</li>
                <li><b>CJK wedge F5 — Fullwidth ↔ halfwidth search normalization</b>: typing <code>123</code> matches <code>１２３_見積書.xlsx</code>; typing <code>２０２６</code> matches <code>report-2026.pdf</code>. Direction-symmetric folding via <code>CFStringTransform</code>. Opt-out toggle <code>unifyl.search.fullwidthHalfwidthFolding</code> (default on). 7 new test cases covering both fold directions, pure-ASCII pass-through, Korean / generic-CJK unaffected, opt-out actually disables folding on <code>ko_KR</code> / <code>ja_JP</code> locales (where <code>localizedCaseInsensitiveContains</code> silently folded — we use locale-free <code>.caseInsensitive</code> comparison after folding).</li>
                <li><b>CJK wedge F6 — Encoding conversion context menu</b>: right-click any <code>.txt</code> / <code>.csv</code> / <code>.srt</code> / <code>.log</code> / <code>.md</code> / etc. → "Convert Encoding to UTF-8…". Auto-detects CP949 (Korean), Shift-JIS (Japanese), GB18030 (Simplified Chinese), Big5 (Traditional Chinese), ISO-8859-1 fallbacks via the existing <code>TextEncodingDetection</code> chain. Sheet shows per-file detected encoding + 200-char preview + Replace-Original or Save-As-New toggle. Atomic write + modification-date preservation. Batch conversion across multi-selection. Binary files rejected at detection time (NUL-byte sniff). 10 unit tests covering CP949 / Shift-JIS / binary rejection / empty rejection / sidecar naming / mtime preservation.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Silent theme export failure</b>: <code>CustomTheme.exportTheme</code> previously used <code>try?</code> for both <code>JSONEncoder().encode</code> and <code>Data.write</code> — disk-full / permission-denied / read-only-volume errors were swallowed silently. Users saw nothing happen and assumed the file landed. Now logs to <code>subsystem:com.unifyl.app category:Settings</code> and surfaces an NSAlert anchored to the editor window.</li>
                </ul>
            ]]></description>
            <pubDate>Tue, 26 May 2026 09:59:46 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.3.1/Unifyl-1.3.1.dmg"
                sparkle:version="27"
                sparkle:shortVersionString="1.3.1"
                sparkle:edSignature="ZrISObXojdz+CmX6HEssoPhWeHZnGKOsO8Pe2oH+j0XRWXFk09/iOC3ejd0WgP+qRbTrvRQEJsym6FnB7TC/Dg=="
                length="16264922"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.3.0</title>
            <description><![CDATA[
                <h2>Unifyl 1.3.0</h2>
                <h3>Added</h3>
                <ul>
                <li><b>CJK wedge F1 — ALZ archive support</b>: <code>.alz</code> archives (Korean legacy format from ESTsoft's ALZip) now extract via bundled <code>unalz</code> binary (kippler/unalz, BSD-licensed, universal arm64+x86_64, codesigned). Read-only — listing, extract-all, and selective-extract supported; create/add/remove/rename throw "read-only" errors. ALZDriver routes through ArchiveEngine's split extension files (Extract / List / Create / Modify). EGG (also ESTsoft) deferred to 1.2.1 — no maintained open-source extractor.</li>
                <li><b>CJK wedge F2 — HWP inline preview</b>: HWPX files (Hancom Office's XML format, .hwpx) now preview inline in the file panel via pure-Swift section-XML text extractor (HWPXParser.extractText). 6 parser tests cover empty doc / multi-paragraph / XML entities / multi-run paragraphs / real HWPX fixture. Legacy binary .hwp formats (HWPML/HSP/HWT) deferred to F9 in 1.3.0.</li>
                <li><b>CJK wedge F3 — Multilingual filename search</b>: type "wen" → matches 文書/文件/文字 (Pinyin → Hanzi via CC-CEDICT-derived map), "tokyo" → matches 東京 (Romaji → Kana → Kanji), "한글" → matches 韓國語 (Hangul → Hanja via Unihan map). All three transforms ship with bundled JSON resource data and per-transform tests. Opt-out toggle in Settings > General; telemetry counts expansion-driven hits for future tuning.</li>
                <li><b>Global rollout — landing page</b>: docs/landing/ is now built per-locale (15 languages) from <code>docs/landing/i18n/keys.{locale}.json</code> + a shared template via <code>scripts/build_landing_i18n.py</code>. Each variant ships with the correct <code>lang</code>/<code>dir</code> attribute, hreflang map (15 + x-default), OG locale tag, and an in-nav language switcher. Arabic gets <code>dir="rtl"</code>. Hosting layout: <code>/</code> is English (canonical), <code>/ja/</code>, <code>/de/</code>, <code>/zh-Hans/</code>, … serve translated copies.</li>
                <li><b>Global rollout — README</b>: top-5 README localizations (<code>README.ja.md</code>, <code>README.zh-Hans.md</code>, <code>README.de.md</code>, <code>README.es.md</code>, <code>README.fr.md</code>) plus a language-switcher banner on the canonical English README. Abbreviated form — full plug-in SDK / contribution docs stay single-source in English.</li>
                <li><b>Global rollout — Info.plist</b>: <code>Unifyl/Resources/{locale}.lproj/InfoPlist.strings</code> for all 15 locales. Localizes <code>NSHumanReadableCopyright</code>; <code>CFBundleDisplayName</code> / <code>CFBundleName</code> stay as the brand "Unifyl". Picked up by XcodeGen's source sweep — no project.yml change needed.</li>
                <li><b>Global rollout — marketing copy</b>: <code>docs/marketing/i18n/copy.{ja,zh-Hans,de,es,fr}.md</code> — reusable per-locale tagline / long description / category tags / pricing phrasing / CTA variants, drives every directory submission from one file per language instead of ad-hoc translation.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>RTL (Arabic) navigation directionality</b>: 7 SF Symbols that used literal-direction variants now use auto-mirroring directional variants. Toolbar back/forward (<code>chevron.left/right</code> → <code>chevron.backward/forward</code>), breadcrumb separators in PathBarView and ArchivePathBarView (<code>chevron.right</code> → <code>chevron.forward</code>), and rename-flow indicators in MultiRenameView / RegexRenameView / LargeFileFinderView (<code>arrow.right</code> → <code>arrow.forward</code>). Bidirectional icons (<code>arrow.left.arrow.right</code>) left as-is.</li>
                </ul>
            ]]></description>
            <pubDate>Sun, 17 May 2026 17:01:30 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.3.0/Unifyl-1.3.0.dmg"
                sparkle:version="26"
                sparkle:shortVersionString="1.3.0"
                sparkle:edSignature="iT/G2V80X5iydFZrvQO0BIkZuRPky30f3/c9YycAWNLbdADrf2PJY1nP91kFXzCvH+KbhgTEiY52QEh6FniZAg=="
                length="16116683"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.2.3</title>
            <description><![CDATA[
                <h2>Unifyl 1.2.3</h2>
                <h3>Fixed</h3>
                <ul>
                <li><b>2 NSAlert sites in sheet-hosted views switched to <code>beginSheetModal(for:)</code> instead of <code>runModal()</code>.</b> <code>NLCommandPreviewSheet</code> (sheet host confirmed at <code>MainWindowView:631</code>) and <code>AIEngineViewModel.applyKeepBest</code> (called from <code>EnhancedDuplicateView</code>, sheet status indeterminate). NSAlert.runModal() from inside a SwiftUI sheet has no guaranteed presentation path — same family of defect as the PDF Tool sheet-modal fix earlier in [Unreleased]. The other 8 NSAlert.runModal() sites were audited and confirmed safe (menu / keyboard / file-op / Sparkle launch contexts, all main-window-modal).</li>
                <li><b>15 persistence sites now route corrupt blobs through <code>PersistenceBackup</code> instead of <code>try?</code>-wiping user data.</b> The pattern <code>guard let decoded = try? JSONDecoder().decode(T.self, from: data) else { return }</code> silently dropped the user's saved data on schema drift across an upgrade. Sites converted: SSHTunnelView (saved tunnels), ScheduledTaskView (cron jobs), NewConnectionSheet (OAuth tokens — Keychain path), IncrementalBackupView (backup profiles), FileColorSettings (per-category colors), IconThemeManager × 3 (installed themes / icon-pack mapping / external themes — all file-based, use <code>stashCorruptFile</code>), SSOAuthManager × 2 (Keychain — corrupt entry now deleted + logged so re-sign-in is forced instead of looping), KeychainService.debugLoadAll (DEBUG-only dev keychain), SecurityBookmarkManager (legacy migration), RecentPathsStore (recent folders), Companion SessionManager (paired devices), CompanionServerConfig (companion server config). UserDefaults blobs are stashed to <code><key>.corrupt.<timestamp></code> (recoverable from <code>defaults read</code>); on-disk files are moved to <code><name>.corrupt.<timestamp>.<ext></code> sidecars. TeamSyncClient's 403-body decode is intentionally left on <code>try?</code> because that's a server-controlled response, not user data. Per memory <code>feedback_persistence_decode_must_stash</code>.</li>
                <li><b>AIEngine no longer crashes the app when every disk path for the vector index fails.</b> Previous: <code>fatalError("AIEngine: unable to create VectorIndex at any path")</code> after the primary / temp / UUID-stamped emergency paths all failed (full disk, sandbox denial). Now falls back to SQLite <code>:memory:</code> so AI search and indexing keep working for the current session — they just don't persist across launches. The <code>fatalError</code> is preserved as a backstop for the (unreachable) case where bundled SQLite itself is broken, so support still gets a crash report. Added <code>VectorIndex.init()</code> that opens SQLite <code>:memory:</code>.</li>
                <li><b>All 7 hardcoded English alert + confirmation-dialog titles now ship native translations in all 15 locales.</b> <code>.alert("Error", isPresented:)</code> / <code>.confirmationDialog("Sync All Differences?", ...)</code> and friends — SwiftUI auto-infers <code>LocalizedStringKey</code> from string literals, but the keys (<code>System Shortcut</code>, <code>Reset all keyboard shortcuts?</code>, <code>Save Macro</code>, <code>Navigation Error</code>, <code>Save Backup Profile</code>, <code>Plugin Installation Failed</code>, <code>Sync All Differences?</code>) had no entries in any <code>Localizable.strings</code>, so non-English locale users saw English fallback. Generated +7 entries per locale across ko / ja / zh-Hans / zh-Hant / de / fr / es / pt-BR / it / ru / tr / ar / th / vi / en (105 total). The other alert keys (<code>Error</code>, <code>Move duplicates to Trash?</code>, <code>Leave this team?</code>, <code>Sign out of SSO?</code>, <code>Delete AI index?</code>) were already translated.</li>
                <li><b><code>Select PDF...</code> (and every other "Browse for file" button inside a sheet) now actually opens the panel.</b> PDF Tool, Folder Report export, SSH Tunnel key chooser, Large-File Finder scan-root picker, Scheduled Task source/dest pickers, Audio Converter output folder, Audio Metadata album-art chooser — all of them are SwiftUI sheets, and inside a sheet <code>NSOpenPanel.runModal()</code> / <code>NSSavePanel.runModal()</code> is silently swallowed by the outer modal hierarchy. The buttons appeared dead. Switched all 9 sites across 7 views to <code>panel.begin { … }</code> (application-modal, attaches above the sheet) with a <code>Task { @MainActor in … }</code> continuation for the post-pick state mutation.</li>
                <li><b>Audit-driven follow-up: 14 more sheet-hosted <code>NSOpenPanel.runModal()</code> sites that were missed in the previous pass.</b> Same family of defect as above — all hosted in SwiftUI sheets and silently swallowed. Sites converted: <code>AIOnboardingSheet.chooseFolderAndIndex</code>, <code>NewConnectionSheet</code> SSH-key picker, <code>AdvancedSearchView</code> × 3 (scope change, copy destination, move destination), <code>IncrementalBackupView.pickFolder</code>, <code>FileSplitMergeView</code> × 2 (split source, merge first-part), <code>SemanticSearchView</code> Choose-Directory button, <code>FileOperationConfirmView</code> destination Browse, <code>CompressDialogView</code> destination Browse, <code>MediaConverterView</code> output-folder Choose. After this pass, every confirmed sheet-hosted <code>runModal()</code> on either NSAlert or panel is gone. <code>CustomTheme.exportTheme/importTheme</code> keep <code>runModal()</code> because they're called from <code>ThemeEditorView</code>, which is hosted as an independent <code>NSPanel</code> via <code>ViewerWindowManager</code> — not a sheet, so <code>runModal()</code> works correctly there.</li>
                <li><b>Companion error messages now ship native translations in all 15 locales instead of always rendering in Korean.</b> <code>CompanionError.errorDescription</code> had 10 cases (notPaired, pairingFailed, connectionLost, connectionTimeout, serverUnreachable, authenticationFailed, permissionDenied, transferFailed, pathNotAllowed, rpcError) with hardcoded Korean string literals, used in 15 sites across the Mac-side Companion server (RequestRouter / SessionManager / WebSocketServer / handlers). Non-Korean Mac users would see Korean banners and toasts during pairing failures or transfer errors. Replaced every literal with <code>NSLocalizedString(key, value:, comment:)</code> using English base values; new keys carry the <code>companion.error.*</code> prefix.</li>
                <li><b>AI auto-classify suggestion reasons translate to all 15 locales.</b> <code>AIClassifier.describeReason</code> returned three hardcoded Korean strings ("N개의 유사한 파일이 존재", "관련 파일 N개 존재", "폴더명 유사도 기반 추천") that appeared in the classification preview next to every suggested destination. Wrapped each in <code>NSLocalizedString(value:)</code> with English source; new keys are <code>ai.classify.reason.similarExtension</code>, <code>ai.classify.reason.relatedFiles</code>, <code>ai.classify.reason.folderName</code>.</li>
                <li><b>Permission-denied banner no longer relies on substring-matching the localized error message.</b> <code>PanelView</code> decided whether to show the "Open System Settings ▸ Privacy & Security" affordance by doing <code>err.lowercased().contains("permission") || err.contains("권한")</code>, which worked only in English- or Korean-localized installations. Ja/zh/de/fr/es users saw a generic red banner with no escape hatch. Added <code>PanelViewModel.lastErrorIsPermission</code> — a typed flag set in <code>loadCurrentDirectory</code>'s catch from <code>Error.isPermissionDeniedError</code>, which pattern-matches Cocoa <code>fileRead/WriteNoPermission</code>, POSIX <code>EACCES</code>/<code>EPERM</code>, and <code>UnifylFileError.permissionDenied</code> instead of the localized message. Reset alongside <code>lastError = nil</code> everywhere it's cleared (initial load, retry, archive enter/exit, manual dismiss).</li>
                <li><b>Help window (F1) actually focuses in the 13 non-English / non-Korean locales.</b> <code>MainWindowView</code>'s <code>isHelpOpen</code> handler matched <code>NSApp.windows.first(where: { $0.title.contains("Help") || $0.title.contains("도움말") })</code>, which silently failed for the other 13 localized titles ("ヘルプ", "帮助", "Hilfe", "Aide", "Ayuda", "Помощь", and so on) — F1 did nothing if the window was already open in those locales. Replaced with <code>@Environment(\.openWindow) openWindow(id: "help")</code>, which SwiftUI handles as "focus existing or create" by scene id, locale-independent.</li>
                <li><b>Forward Delete (⌦) key now moves selected files to the Trash, same as F8.</b> Previously only F8 and <code>⌘Delete</code> (Cmd+Backspace) triggered the move-to-Trash flow; the labelled "Delete" key on full-size keyboards (or <code>fn+delete</code> on laptops, keyCode 117) did nothing. Wired in both <code>FileTableView.handleKeyDown</code> (when the file table has focus) and <code>FunctionKeyMonitor</code> (when focus is elsewhere — pathbar / toolbar / etc.) so the affordance is consistent across the window. <code>Backspace</code> alone (no modifier) still navigates to the parent directory — that's the TC + Finder convention and stays put.</li>
                <li><b><code>FileTableView.userDefaultsDidChange</code> no longer SIGTRAPs when defaults are written off-main.</b> <code>UserDefaults.didChangeNotification</code> posts on whatever thread modified defaults (e.g. <code>XCTTelemetryLogger.+initialize</code> triggers <code>registerDefaults:</code> on a background dispatch queue during test bundle init), and Swift 6 strict concurrency traps when the @MainActor <code>applyColumnVisibility()</code> is called from there. Marked the selector <code>nonisolated</code> and hopped to MainActor via <code>Task</code>. Real-world impact: the test runner no longer crashes at bootstrap; production launch was already on main but is now race-proof for any future off-main defaults writer.</li>
                <li><b>CSV Preview now splits rows on Windows-style <code>\r\n</code> line endings.</b> Swift's <code>String</code> iteration groups <code>\r\n</code> into a single extended grapheme cluster that matched neither <code>char == "\r"</code> nor <code>char == "\n"</code> in the char-by-char parser, so the line ending leaked into the field value and a Windows-saved CSV rendered as a single giant row. Replaced the manual check with <code>Character.isNewline</code>, which matches <code>\n</code> / <code>\r</code> / <code>\r\n</code> + Unicode line separators (<code>\u{0085}</code>, <code>\u{2028}</code>, <code>\u{2029}</code>) uniformly. The pre-existing <code>if char == "\r" { continue }</code> branch was dead code (never reached because Swift had already merged the CR+LF). Caught by the existing <code>handlesCRLF</code> unit test, which was previously failing.</li>
                </ul>
                <h3>Added</h3>
                <ul>
                <li><b>CSV Preview: text-encoding auto-detect + large-file safety.</b> The previewer used to hardcode <code>String(contentsOf: url, encoding: .utf8)</code>, so a Korean Excel CSV (CP949 default) or any non-UTF-8 file threw or rendered mojibake, and a multi-hundred-MB CSV would block the main thread / OOM on whole-file load. Now: BOM-checks UTF-8 / UTF-16 LE / UTF-16 BE first, then falls through <code>.utf8 → CP949 (kCFStringEncodingDOSKorean) → Shift_JIS → EUC-KR → Big5 → GB18030 → Windows-1252 → Mac OS Roman</code> until one succeeds; caps the byte payload at 50 MB (drops the trailing partial row so half-fields don't render as data); caps the rendered table at 5000 rows; surfaces both the detected encoding and a yellow truncation banner ("Showing 5000 of 50000 rows" / "File exceeds 50 MB — preview shows the leading slice") in the footer. Resolves issue #7. Carries unit tests for UTF-8 BOM, plain UTF-8, CP949 fallback, ASCII passthrough, empty input, quoted CSV, CRLF, and TSV.</li>
                <li><b>CSV parser drops the phantom blank row on a trailing <code>\r\n</code> / <code>\n</code>.</b> Pre-existing parser bug — a well-formed CSV that ended with a newline gained a <code>[""]</code> row at the end of the rendered table. Caught by the new CRLF unit test.</li>
                <li><b>Hex Editor: in-place byte editing.</b> The Hex viewer was misnamed — its underlying <code>NSTextView</code> had <code>isEditable = false</code>, so the Save / Revert buttons were dead UI and any "edit" affordance was a lie. Now genuinely editable: <code>shouldChangeTextIn</code> delegate intercepts single-character keystrokes, decodes the cursor's character offset into either a hex high-nibble / low-nibble or an ASCII byte position (deriving from the per-row layout of <code>8-digit offset + 16 bytes × 3 chars + separator + 16 ASCII chars</code>), patches the byte in <code>Data</code> and the rendered <code>NSTextStorage</code> in place, and advances the cursor to the next nibble / byte. Hex columns accept <code>0-9 / A-F / a-f</code> only; ASCII column accepts printable bytes (<code>0x20–0x7E</code>) only; offset column and gap characters reject input. Paste, multi-character IME, and selection-replace are rejected to keep the grid layout stable. Save (⌘S) writes the edited bytes atomically to disk via <code>Data.write(to:options:.atomic)</code>. Status bar shows the hint "Click a hex digit or ASCII char and type to edit · ⌘S to save" on file load. Coordinator class is now <code>@MainActor</code>-isolated, which incidentally clears the 5 pre-existing Swift 6 strict-concurrency warnings on <code>updateContent</code> / <code>scrollToOffset</code> / <code>highlightRange</code> accessing <code>textStorage</code>.</li>
                <li><b><code>scripts/audit-guard.sh</code> + <code>make audit</code></b> — a codified sweep that runs every audit-pattern this project has accumulated across multiple sessions: sheet-hosted <code>runModal()</code> (F1), hardcoded CJK inside <code>errorDescription</code> (F2), <code>MainActor.assumeIsolated</code> and new <code>ObservableObject</code> bans (F3), Localizable.strings parity across all 15 locales (F4 — FAIL on en/ko drift, WARN on any other locale's gap with a count + pointer to the CSV workflow), locale-fragile substring matching like <code>title.contains("도움말")</code> (F5), ghost build-flag references in <code>docs/</code> (F6), v1.0-era 4-tier pricing residue (F7), Sparkle <code>Info.plist</code> invariants (F8), public mirror URL hygiene (F9), missing <code>LocalizedError</code> conformance (F10), raw <code>throw NSError(domain:code:userInfo:)</code> calls (F11). The previous audit had marked the sheet-modal NSAlert family "done — 9 sites across 7 views" but missed 14 additional NSOpenPanel sites in the same family; the new script caught all of them mechanically. FAIL-class violations exit non-zero (block CI/PR); WARN-class print but don't block. Opt-in <code>// audit-guard:allow-runmodal</code> marker for legitimate fallbacks (e.g. NLCommandPreviewSheet's no-keyWindow defensive branch). Run via <code>make audit</code> or wire into a pre-commit / CI hook.</li>
                <li><b><code>docs/translations-pending.csv</code> + <code>scripts/apply-translations.py</code></b> — translator-ready workflow for the 1807 untranslated strings (139 keys × 13 secondary locales). The CSV has columns <code>locale, key, english_source, translation</code>; a translator fills the <code>translation</code> cells, then <code>python3 scripts/apply-translations.py</code> appends each completed row into the right <code>*.lproj/Localizable.strings</code> (skips keys already present, idempotent on rerun, rewrites the CSV with only the rows still pending). Per memory <code>feedback_audit_one_shot</code> and the new F4 WARN — generating placeholders ourselves would have masked the gap from real translators, so the CSV-and-apply pattern surfaces work to do without papering over it.</li>
                <li><b>All 1807 missing translations filled in across 13 secondary locales</b> (ja, zh-Hans, zh-Hant, de, fr, es, pt-BR, it, ru, tr, ar, th, vi). Translations were machine-generated (Claude) following per-locale conventions: keycap symbols (⌘ ⌥ ⌃ ⇧) and format specifiers (%@, %d, %1$@) preserved verbatim; literal <code>\\n</code> retained as the strings-format escape; macOS-native terminology where applicable (e.g. ja "ゴミ箱" for Trash, de "Papierkorb", zh-Hans "废纸篓"). Native speaker review is still warranted before a public press push, but the table is now the single source of truth (<code>scripts/_translation-table.py</code>) — each cell is reviewable and re-emitable. All 15 locales now hold the same 687 keys; F4 audit is fully PASS, the 1807-row WARN is gone. Total .strings file size went from 548 keys × 14 secondary locales (7672) to 687 × 14 (9618) entries.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>ArchiveEngine class body split into 4 method-group extension files (1092 → 173 + 4 extensions).</b> The <code>ArchiveEngine</code> class — list / extract / create / modify pipelines all sharing a small set of helpers — split as:</li>
                </ul>
                <p>  * `ArchiveEngine.swift` (173 lines) keeps the class declaration, `init`, and the cross-group helpers `isSafeArchivePath` (path-injection guard), `find7z` (7z binary discovery), `run` (Process shell-out), plus the `ProcessRunner` nested actor. These three helpers were `private` and have been promoted to module-internal so the extension files can reach them.</p>
                <p>  * `ArchiveEngine+List.swift` (277 lines) — `list()` plus the format-specific `listZip`/`listTar`/`list7z` and Korean-aware ZIP name decoding.</p>
                <p>  * `ArchiveEngine+Extract.swift` (203 lines) — `extract` variants plus the `preflightOrThrow` (zip-bomb guard) and `verifyNoPathTraversal` helpers, both promoted from `private` to module-internal because the password-aware extract methods in +Create.swift need them too.</p>
                <p>  * `ArchiveEngine+Create.swift` (236 lines) — `requiresPassword`, `probeZipEncrypted`/`probe7zEncrypted`, all `create*` formats (`createZip`/`createTarGz`/`createTar`/`createTarBz2`/`createTarXz`/`createTarZst`/`create7z` + dispatcher `create`), and the password-aware extract methods.</p>
                <p>  * `ArchiveEngine+Modify.swift` (248 lines) — in-place editing of existing archives: `addToArchive`, `removeFromArchive`, `renameInArchive`, `createDirectoryInArchive`, and the `modifyTarArchive` round-trip helper.</p>
                <ul>
                <li><b>FileTableView.swift NSTextFieldDelegate extension moved to <code>FileTableView+TextFieldDelegate.swift</code> (2352 → 2317 + 38 lines).</b> Just the inline-rename text-field delegate methods (<code>controlTextShouldEndEditing</code> / <code>cancelOperation</code>). Access promotions: <code>tableView</code> and <code>items</code> <code>private</code> → module-internal (the extension routes first-responder focus back to the table and reads the row being renamed by index). Small win as a low-risk first cross-file split test for the FileTableView extension surface; the bigger NSTableViewDataSource (266 lines) and NSTableViewDelegate (464 lines) blocks stay in the main file because their cross-section private-state touch surface is wide — splitting them needs a per-section manual NSTableView drag/drop smoke pass.</li>
                <li><b>FileTableView.swift core auxiliary types extracted (2598 → 2352 + 2 new files, 249 lines).</b> Two file-private collaborators moved out: <code>FileTableViewDelegate.swift</code> (30 lines) — the <code>@MainActor public protocol</code> plus its <code>didDropURLs intoArchive</code> default extension; every adopter (<code>FileTableBridge</code>, <code>PanelView</code>, archive list views, the Smart Folder cart) builds against it. <code>UnifylTableView.swift</code> (219 lines) — the NSTableView subclass that intercepts <code>keyDown</code>, <code>mouseDown</code>, and the drag pipeline. Promoted from <code>private class</code> to module-internal so it can live across files. The memory note about <code>@MainActor</code> annotation silently breaking drag initiation (a regression last quarter) is preserved verbatim at the top of the new file — this class STAYS non-<code>@MainActor</code>, methods all run on main via AppKit's runloop, timer/Task closures hop to MainActor explicitly. Verified by <code>swift test UnifylAppKit</code> (6/6 pass).</li>
                <li><b>FileTableView.swift auxiliary types extracted (2753 → 2598 + 3 new files, 167 lines).</b> Three standalone types lived in the same file as the 1500-line <code>FileTableView</code> NSView subclass: <code>FileTableContextAction</code> enum (raw-value-stable, persisted as user keybinding identifiers — moved to its own file with the doc note about the stability contract), <code>ThemedTableRowView</code> NSTableRowView subclass (renders alternating row backgrounds + selection stripes from theme tokens), and <code>AudioSpecCache</code> (sample-rate / bit-depth cache keyed by URL). All three are now in single-purpose sibling files. The big <code>FileTableView</code> class itself stays in one piece — NSView lifecycle + NSTableView drag/drop delegates share too much synchronous state to safely split into extension files in an autonomous pass; that work needs a manual NSTableView drag-smoke pass per split. 6/6 UnifylAppKit tests pass against the new layout.</li>
                <li><b>AppViewModel.swift File Tags + Cloud Download bloc split into <code>AppViewModel+TagsAndCloud.swift</code> (441 → 266 + 191-line extension file).</b> macOS Finder tag manipulation (per-item apply/strip, AI-suggested tagging, batch ops) plus the cloud-only file download/progress watcher used for iCloud / Dropbox stub files.</li>
                <li><b>AppViewModel.swift File Operations / Remote download / Drop / Compare bloc split into <code>AppViewModel+FileOps.swift</code> (768 → 441 + 344-line extension file).</b> Four MARK sections moved together (they share the same <code>fileOps</code>-delegating idiom): Forwarding (Conflict Resolution), File Operations (copy/move/delete/new-folder/batch dispatch), Remote download/upload (with <code>reportTransferFailures</code> aggregator — stays file-private to the extension), Drop handling, Compare. One file-private helper kept private. No new access promotions needed.</li>
                <li><b>AppViewModel.swift TC-compat shortcuts section split into <code>AppViewModel+TCShortcuts.swift</code> (1810 → 768 + 451-line extension file, plus the prior Archive split).</b> Total-Commander parity actions: <code>swapPanels</code>, <code>copyCurrentPathToOppositePanel</code>, <code>revealCursorInOppositePanel</code>, <code>moveTab(from:at:)</code> (cross-panel), <code>computeAllDirectorySizes</code>, <code>goToVolumeRoot</code>, <code>createNewTextFileAndEdit</code>, <code>duplicateInSameDirectory</code>, <code>insertToggleAndAdvance</code>, <code>beginInlineRename</code>, the HWP conversion alert flow, and a clutch of file-action dispatchers that delegate to fileOps. Access promotion: <code>cloudDownloadTask</code> <code>private</code> → module-internal so the cloud-download dispatcher in the extension can swap its watcher Task.</li>
                <li><b>AppViewModel.swift Archive operations bloc split into <code>AppViewModel+Archive.swift</code> (1810 → 1202 + 628-line extension file).</b> Four MARK sections moved: Archive operations (<code>extractArchive*</code>, <code>extractArchiveHere</code>, <code>extractArchiveWithPassword</code>), Compress/Extract (Cmd+F5 / Cmd+F9 sheet flow), Extract and Open from Archive, and Archive File Operations (in-archive copy/move/delete pipelines that keep <code>ArchivePanelView</code> working when browsing inside a ZIP or 7z). Four file-private helpers (extractArchive, archiveEntryPaths, extractAndRelocate, resolveExtractDestination) stay private to the extension file — their only callers are the methods in the same section, so no access promotion needed.</li>
                <li><b>AppViewModel.swift Clipboard (Copy/Cut/Paste) section split into <code>AppViewModel+Clipboard.swift</code> (2135 → 1810 + 343-line extension file).</b> All clipboard methods (<code>copyFilesToClipboard</code>, <code>copyFilePathsToClipboard</code>, <code>copyPaths</code>, <code>cutFilesToClipboard</code>, <code>pasteFilesFromClipboard</code>, plus the related <code>showLaunchTipIfAppropriate</code>, <code>scheduleTagNotificationDismiss</code>, <code>insertFileNameIntoTerminal</code>). <code>isCutOperation</code> stored-property declaration stayed in the main file (Swift extensions cannot carry stored properties); promoted from <code>private</code> to module-internal, same for <code>tagNotificationTask</code>.</li>
                <li><b>AppViewModel.swift Workspace Persistence section split into <code>AppViewModel+Workspace.swift</code> (2200 → 2135 + 78-line extension file).</b> <code>SavedTabSort</code> and <code>SavedWorkspace</code> Codable types plus <code>saveWorkspace()</code> / <code>loadWorkspace()</code>. Access promotion: <code>loadWorkspace</code> <code>private</code> → module-internal (AppViewModel.init reads workspace on launch from the main file).</li>
                <li><b>PanelViewModel.swift TC shortcuts section split into <code>PanelViewModel+TCShortcuts.swift</code> (852 → 631 + 237-line extension file).</b> Total-Commander-style operations: <code>insertToggleSelectAndAdvance</code>, <code>computeAllVisibleDirectorySizes</code>, <code>enterBranchView</code> (recursive flat listing), <code>setFilter</code>, <code>sortItems</code>, the canonical static <code>sortItemsStatic</code> comparator, and <code>syncVisibleItems</code>. Access promotions: <code>sortItemsStatic</code> from <code>private</code> to module-internal (PanelViewModel.loadCurrentDirectory needs it off-main).</li>
                <li><b>PanelViewModel.swift Navigation section split into <code>PanelViewModel+Navigation.swift</code> (1128 → 852 + 289-line extension file).</b> All 17 navigation functions (<code>navigate*</code>, <code>goUp/Back/Forward</code>, <code>restoreTab</code>, <code>selectTab</code>, <code>addTab</code>, <code>closeTab</code>/<code>closeOtherTabs</code>/<code>closeTabsToRight</code>, <code>duplicateTab</code>, <code>togglePinTab</code>, <code>setTabTitle</code>, <code>setSortOrder</code>, <code>spaceBarToggle</code>, <code>calculateDirectorySize</code>). Access promotions: <code>sortItems</code>, <code>syncCalculateDirectorySize</code> from <code>private</code> to module-internal.</li>
                <li><b>PanelViewModel.swift Remote Connect/Disconnect section split into <code>PanelViewModel+Remote.swift</code> (1219 → 1128 + 112-line extension file).</b> <code>connect(to:)</code>, <code>disconnect()</code>, and the OAuth-cloud <code>makeOAuthFileSystem</code> helper. The protocol-specific VFS adapter selection (SFTP / FTP / WebDAV / SMB / S3 / Azure Blob / OAuth-cloud) now reads as one ~100-line file independent of the directory-loading / archive / navigation sections.</li>
                <li><b>PanelViewModel.swift Archive browsing section split into <code>PanelViewModel+Archive.swift</code> (1503 → 1219 + 300-line extension file).</b> <code>enterArchive</code>, <code>exitArchive</code>, <code>navigateInArchive</code>, <code>goUpInArchive</code>, <code>refreshArchiveItems</code>, plus the <code>listArchiveEntries</code> ZIP-central-directory reader, all moved to the extension. Access promotions: <code>allArchiveEntries</code> and <code>archiveEngine</code> from <code>private</code> to module-internal; <code>watchTask</code> / <code>archiveExitTask</code> accessor pairs from <code>private</code> to module-internal so the extension can drive cancellation and exit deferral. <code>archiveOpenFailureURL</code> stored property stayed declared on the main class (Swift extensions can't carry stored properties). The view-model's archive-mode surface is now locally readable in its own ~300-line file.</li>
                <li><b><code>TaskBox</code> extracted out of PanelViewModel.swift (1534 → 1503 + 36-line TaskBox.swift).</b> The lock-protected Task box that owned <code>watchTask</code> / <code>debounceTask</code> / <code>archiveExitTask</code> was a file-private class within the view-model file. Hoisted to its own file <code>Unifyl/ViewModels/TaskBox.swift</code> and promoted from <code>private</code> to module-internal so PanelViewModel can hold it across files. Cleaner reading: the nonisolated-deinit cancellation contract is now locally inspectable without scrolling past 1500 lines of view-model code.</li>
                </ul>
                <ul>
                <li><b>3 large source files split into feature-group files (1190+1160+1312 → 600+1092+1107 with 7 new files).</b> Long-form refactor work to lower the cost of touching any single hot area:</li>
                </ul>
                <p>  * `Unifyl/Services/IconThemeManager.swift` (1190 → 600) — model + manager class kept; `IconPack` (28), `ExtensionSvgMapping` (181), `PopularTheme` (390) moved to siblings. Each new file has a single feature group (data registry / preset registry / SVG pack model).</p>
                <p>  * `Packages/UnifylFileSystem/Sources/UnifylFileSystem/ArchiveEngine.swift` (1160 → 1092) — `ArchiveError` (19) and `ArchiveFormat` (55) extracted to siblings. The class body itself (list / extract / create / modify / 7z helpers) is still ~1092 lines because every section reaches into the same set of `private` helpers (`isSafeArchivePath`, `verifyNoPathTraversal`, `run(...)`, etc.); splitting those across files requires promoting the helpers from `private` to `internal`, deferred to a session where access scope changes can be reviewed.</p>
                <p>  * `Unifyl/App/UnifylApp.swift` (1312 → 1107) — `ProBadgeMap` (208 lines) plus the associated `badgeLogger` and doc comment hoisted to `Unifyl/App/ProBadgeMap.swift`. The remaining content (the `App` scene composition, `.commands { }` menu blocks, settings + help windows) is where the file&#x27;s bulk lives and stays put — `.commands` and `WindowGroup` blocks are body composition that resists clean extension-based splits.</p>
                <p>  Tests `IconThemeRefactorTests.swift` (3 suites, 8 tests) added covering the IconPack directory contract, PopularTheme repo-path parsing edge cases, and `ExtensionSvgMapping.defaultMapping` UX-stable entries — the table is a UX contract (broken icon themes look obviously wrong), so a regression test for the known mappings catches accidental deletions during future cleanup.</p>
                <h3>Changed</h3>
                <ul>
                <li><b><code>en.lproj/Localizable.strings</code> synced with <code>ko.lproj</code> (548 → 687 keys).</b> Translator workflow drifted — Korean translations were added for 139 strings whose English source never made it into the English baseline <code>.strings</code> file. Users on every locale still saw correct text because <code>NSLocalizedString(key, value:)</code> falls back to the inline <code>value:</code> when the locale file lacks the key, but the missing baseline broke localizer tooling that pivots off <code>en.lproj</code>. Backfilled all 139 keys with <code>"key" = "key";</code> entries (plus the one true identifier-style key <code>companion.iosAppPending.notice</code> whose English value was copied from the call site). The other 13 locales still hold 548 keys — backfilling translations there is a content task, not a code one, and untranslated keys continue to render via the inline <code>value:</code> fallback.</li>
                <li><b>EnterpriseTeamView CSV audit-log export switched to <code>panel.begin {}</code> — same <code>sheet+runModal()</code> family as the 14 other audit-driven sites above.</b> Caught by the new <code>make audit</code> script in the same pass that produced the script. The "Export Audit Log to CSV" button on the Enterprise → Audit Log tab was a dead button in production (sheet swallowed the panel).</li>
                <li><b>SummaryTypes.SummarizationError errorDescription wrapped in <code>NSLocalizedString</code>.</b> 5 cases (<code>unsupportedFormat</code>, <code>noExtractableContent</code>, <code>modelUnavailable</code>, <code>modelLoadFailed</code>, <code>inputTooLarge</code>) had Korean string literals — same family bug as the CompanionError fix earlier in [Unreleased], caught by <code>make audit</code>'s F2 check. Keys carry <code>ai.summarize.error.*</code> prefix.</li>
                <li><b>5 raw <code>throw NSError(domain:code:userInfo:)</code> sites typed as <code>LocalizedError</code> enums.</b> Each used <code>NSLocalizedDescriptionKey</code> with an English literal, so non-EN locale users saw English when the error reached the UI — same locale-fragility family as F5. Converted:</li>
                </ul>
                <p>  * `PluginManager.swift` &quot;Invalid plugin ID: …&quot; → `PluginLoadError.invalidPluginID(String)` (added case to the existing enum).</p>
                <p>  * `SFTP/FTP/WebDAVFileSystem.swift` &quot;Refusing remote operation: path contains control characters …&quot; → `VFSError.unsafeRemotePath(String)` (added case to the shared VFS error in `UnifylCore`; all three providers route through the same typed case).</p>
                <p>  * `AudioMetadataView.swift` &quot;Cannot create export session&quot; / &quot;Export failed&quot; → new `AudioMetadataError { exportSessionUnavailable, exportFailed }`.</p>
                <p>  All cases wrap their message with `NSLocalizedString(key, value:, comment:)`. Caught and locked in by new audit-guard check F11.</p>
                <ul>
                <li><b>2 icon-only Buttons gained <code>.accessibilityLabel(...)</code>.</b> <code>BookmarkView</code> row trash button ("Remove bookmark") and <code>PathBarView</code> cancel-edit button ("Cancel path edit"). VoiceOver previously announced the bare SF Symbol name. Initial 310-vs-33 ratio of <code>Image(systemName:)</code> to <code>accessibilityLabel(</code> was misleading — almost every site is either inside a <code>Button</code> with a <code>Text</code> sibling (auto-derived label) or used as decoration on a labelled control; only these 2 were truly orphaned.</li>
                <li><b>UnifylAppKit gains its first 6 test functions across 3 suites.</b> The package was at 0 tests despite housing the 2744-line <code>FileTableView</code> NSView bridge that drives every directory listing in the app — any change to the table logic shipped without a regression net. New <code>UCAppKitTests.swift</code> covers (1) <code>FileCategory.color</code> mapping completeness (every case has an explicit color; a new case added without one trips the test), (2) <code>FileTableContextAction</code> rawValue stability (its raw strings are persisted in user keybindings — silently renaming a case would orphan the user's saved customization), and (3) <code>AudioSpecCache</code> behaviour (non-audio extensions short-circuit before disk I/O; <code>clearCache()</code> actually resets state). NSView lifecycle and NSTableView drag pipelines remain manual-test only — they require a host AppKit run loop. 6/6 pass.</li>
                </ul>
                <h3>Removed</h3>
                <ul>
                <li><b><code>LicenseConfig.aiCheckoutURL</code>, <code>teamCheckoutURL</code>, <code>aiVariantId</code>, <code>teamVariantId</code> deleted.</b> All four constants pointed at the same single Pro variant (1,544,858) and the same single checkout URL — v1.0-era ghosts of a once-planned 4-tier (Free / Pro / AI / Team) pricing model that collapsed to 2 tiers (Free + Pro) before public release. Only <code>aiVariantId</code> was actually referenced (<code>LemonSqueezyClient.tier(fromVariantId:)</code>), folded back to <code>proVariantId</code>. The other three were dead code. Resurrect with real variant IDs only when a separate SKU actually exists in the LemonSqueezy store.</li>
                <li><b><code>docs/DEVELOPMENT.md</code> ghost build-flag references corrected.</b> The doc claimed feature gating used compile-time flags <code>UNIFYL_PRO</code> / <code>UNIFYL_AI</code> / <code>UNIFYL_TEAM</code> — none of those have ever existed in <code>project.yml</code> or any <code>Package.swift</code>. All gating is runtime via <code>FeatureGateManager</code>. The doc now reflects the single-binary, runtime-only reality so contributors don't go hunting for build-flag plumbing that isn't there.</li>
                </ul>
            ]]></description>
            <pubDate>Wed, 13 May 2026 08:06:16 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.2.3/Unifyl-1.2.3.dmg"
                sparkle:version="25"
                sparkle:shortVersionString="1.2.3"
                sparkle:edSignature="Azm+RKMfq2Lh9FWCPOmr7AEZm9AdMxSIxfXWXgQzT0aLR4rRfF35z8CkjlNMbdcUwXA4rgyTWoTpcj9laq1BCA=="
                length="15925733"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.2.2</title>
            <description><![CDATA[
                <h2>Unifyl 1.2.2</h2>
                <p>Multi-axis polish pass on top of 1.2.1, accumulated across multiple audit-driven sessions.</p>
                <h3>Added</h3>
                <ul>
                <li><b>193 previously-hardcoded UI strings localized into all 14 non-English locales</b> (ja, ko, zh-Hans, zh-Hant, de, fr, es, pt-BR, it, ru, tr, ar, th, vi). Roughly half of the app's <code>Text(...)</code> literals (mostly inside viewer / tool sheets — Checksum, AI Auto-Classify, AI Search, App Uninstaller, Audio Converter, SSH Tunnel, Git History, File X-Ray, PDF Convert, Theme presets, etc.) had no entries in <code>Localizable.strings</code>, so non-English locale users saw English fallback text in those surfaces despite the rest of the app being native. Each locale now ships +193 entries (343 → 536 keys) with macOS-convention vocabulary per language.</li>
                <li><b>Onboarding slide 4 ("Pick a folder to begin") now localized in all 15 languages.</b> The slide was added in 1.2.0 but its 5 NSLocalizedString keys (title, body, drag, goto, connect) were never added to <code>Localizable.strings</code>, so 14 non-English locale users saw the English <code>value:</code> fallback for the post-onboarding "now what?" guidance. Added native translations for ko / ja / zh-Hans / zh-Hant / de / fr / es / pt-BR / it / ru / tr / ar / th / vi.</li>
                <li><b><code>Error.humaneMessage</code> extension</b> that maps low-level Foundation / POSIX / NSURLError codes to localized user-facing strings. Falls through to <code>errorDescription</code> for <code>LocalizedError</code>-conforming types (<code>UnifylFileError</code>, <code>VectorIndexError</code>), and to <code>localizedDescription</code> as last resort.</li>
                <li><b>Viewer NSPanel windows now remember their size and position</b> across launches. The Hex / Log / Git / EXIF / Compare / Dir Compare / Media / Docker / Git History / Process Map / Port Viewer / Theme Editor / 3-Way Merge / File Preview viewer windows used to open at the same default-centered + offset rect every time, even after the user resized or moved them. They now derive a per-viewer autosave key from the title prefix (e.g. "Hex: foo.bin" → key "Hex"), so AppKit persists the frame to defaults under <code>UnifylViewer.<type></code> and restores it on the next open. The Main window already had this via <code>WindowFrameAutosave</code> since 1.1.0; this brings viewers to parity.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>All user-facing error banners now route through <code>error.humaneMessage</code></b> instead of <code>error.localizedDescription</code>. ~112 user-facing error sites total: AppViewModel (26), FileOperationManager (10), AIEngineViewModel (7), PanelViewModel (5), ConnectionViewModel (1), TeamSettingsView (4), TeamOnboardingSheet (2), AuditLogView (2), plus 55 sites across 43 view-layer files (CompressDialog, FilePermissions, MediaInfo, AppUninstaller, FileDiff, DirectoryDiff, MediaConverter, SQLitePreview, JSONPreview, PlistPreview, FontPreview, VideoPreview, ArchivePreview, AudioPlayer, EnterpriseTeam, etc.). All produce localized messages for <code>EACCES</code> / <code>ENOSPC</code> / <code>ENOENT</code> / <code>EBUSY</code> / etc. instead of raw English <code>localizedDescription</code> from the system. OSLog / <code>UnifylLogger</code> calls are intentionally left on <code>localizedDescription</code> so logs stay machine-greppable in English.</li>
                <li><b>Semantic color literals migrated to DesignTokens.</b> 93 sites across 52 view/feature files where <code>.foregroundStyle(.green / .red / .orange)</code> was hard-coding success / error / warning intent now go through <code>DesignTokens.Colors.success / .error / .warning</code>. The token registry already had matching values (<code>#34D399</code>, <code>#F87171</code>, <code>#F59E0B</code>) chosen for ≥4.5:1 contrast against the dark surface — this brings the rest of the app onto them so a designer can re-tune the status palette in one place. <code>.blue</code> and <code>.accentColor</code> left untouched (ambiguous between info-status and system-accent intent — needs case-by-case review).</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>CLDR plural rules via <code>Localizable.stringsdict</code>.</b> Five hardcoded English plural patterns (<code>count == 1 ? "" : "s"</code>) in AppViewModel (clipboard sources missing), AIRenameView ×2 (rename suggest / will-rename count), EnterpriseTeamView (member count), DuplicateFinderView (will-go-to-Trash count) used to render "1 file" / "5 files" in English but "5 파일s" / "5 ファイルs" in non-English locales. Generated <code>.stringsdict</code> for all 15 locales with proper plural categories (single-form for ja/ko/zh/th/vi, two-form one/other for en/de/fr/es/pt-BR/it/tr/vi, two-form for ru/ar — Arabic could be expanded to 6-form later if specific n=2/few/many phrasing matters), and switched the call sites to <code>String.localizedStringWithFormat(NSLocalizedString("ai.renameSuggest", …), count)</code>. Native macOS plural infrastructure now picks the right form per locale.</li>
                <li><b>Hidden-file toggle (⌘⇧.) now persists across launches.</b> <code>AppViewModel.showHiddenFiles</code> was a plain <code>var</code> initialised to <code>false</code>, so every relaunch reset it — a developer who flipped it for the session had to re-toggle each launch. Added a <code>didSet</code> observer that mirrors the <code>splitRatio</code> / <code>bookmarks</code> persistence pattern (<code>@AppStorage</code> doesn't work on <code>@Observable</code> stored properties — init-accessor synthesis rejects it), backed by <code>unifyl.showHiddenFiles</code> in UserDefaults.</li>
                <li><b>OAuth callback URL scheme now declared in <code>Info.plist</code>.</b> <code>OAuth2Configuration</code> ships <code>redirectURI = "com.unifyl.basic://oauth2/callback"</code> for Native Google Drive / Dropbox / OneDrive providers, but <code>CFBundleURLTypes</code> was missing — macOS had no app to route the callback to, so the auth flow would hang forever. Added a single URL-types entry registering <code>com.unifyl.basic</code>. (Native OAuth providers are still gated behind <code>unifyl.enableOAuthPilot</code> while their client IDs remain placeholders.)</li>
                <li><b>Smart quote / dash / period / text-replacement substitutions disabled app-wide.</b> macOS NSTextField and NSTextView ship with these ON by default, which silently mangles file names typed into inline rename, ⇧⌘G Go to Folder, and the inline command bar (<code>"data".csv</code> becomes curly-quoted, <code>foo--bar.txt</code> becomes en-dashed, <code>omw to a folder</code> expands via System Settings text replacement). <code>applicationDidFinishLaunching</code> flips the four <code>NSAutomatic*Enabled</code> defaults to <code>false</code> for *this app's* text controls without touching system-wide settings.</li>
                <li><b>Custom theme load no longer silently wipes the user's saved themes on schema drift.</b> <code>try?</code> was returning nil when the on-disk theme JSON failed to decode (typical after an upgrade adds a new color token), and the next <code>save()</code> overwrote the disk file with <code>[]</code>. Now stashes the corrupt file via <code>PersistenceBackup.stashCorruptFile</code> (recoverable for support) and logs to <code>UnifylLogger.settings</code>. Same pattern applied to the import-from-file path so a malformed <code>.ultratheme</code> file logs the parse error instead of vanishing.</li>
                <li><b>A11y polish on tab bar + status bar.</b> TabBar's <code>xmark</code> close button gained <code>.accessibilityLabel("Close Tab")</code> (was announcing as a generic "button"); the <code>pin.fill</code> and <code>cloud.fill</code> chips on the tab title gained <code>.accessibilityHidden(true)</code> because they're decorative — VoiceOver was announcing "image: pin.fill" before every pinned tab's title. PanelStatusBar's optional toast icon now <code>.accessibilityHidden(true)</code> for the same reason — the toast text right next to it carries the message.</li>
                <li><b>URLSession requests now cap their wait</b> so a hung server doesn't keep the request UI spinning forever. <code>TeamSyncClient.buildRequest</code> gets <code>timeoutInterval = 15 s</code> (matches <code>LemonSqueezyClient</code>); <code>IconThemeManager</code> per-asset SVG fetch caps at 30 s and the manifest JSON download at 60 s. Other URLSession sites already had explicit timeouts via the request builder pattern.</li>
                <li><b><code>applicationWillTerminate</code> now flushes UserDefaults before quit.</b> Quick Cmd+Q right after adding a bookmark could lose the entry on systems that hadn't auto-synced defaults yet; the new handler calls <code>UserDefaults.standard.synchronize()</code>. Doesn't block termination (no beachball) — file copies still rely on FileOperationManager's atomic-write + half-file cleanup.</li>
                <li><b>18 Xcode build warnings cleared.</b> PortViewerView's <code>errData</code> was a captured <code>var Data()</code> mutated from <code>Pipe.readabilityHandler</code> callbacks while the outer <code>Task.detached</code> owned the closure scope — Swift 6 strict concurrency flagged it as a real data race; wrapped in a lock-protected <code>ErrDataBox</code> (<code>@unchecked Sendable</code>, <code>NSLock</code>-guarded). UnifylTests' <code>override func setUp() / tearDown()</code> were declared without <code>async throws</code> in <code>@MainActor</code> classes, breaking MainActor isolation; converted to async-throws variants with <code>try await super.<x>()</code>. Cloud HTTP calls (Dropbox/Google/OneDrive — <code>post</code>/<code>patch</code>/<code>delete</code> × 8) had unused <code>try await client.xxx(...)</code> results that look like silent fails to a reader; made the discard explicit with <code>_ = try await ...</code>. Plus 1 var→let cleanup and 3 unused-let → <code>_</code> cleanups in tests.</li>
                </ul>
                <h3>Notes (audit-driven, ship-pending)</h3>
                <ul>
                <li><b>B3 — <code>Task.detached</code> <code>[weak self]</code> audit:</b> swept all 80+ <code>Task.detached</code> sites across <code>Unifyl/</code> and <code>Packages/</code>. <b>No real retain risk found.</b> Most closures use closure-local data plus <code>Self.<static></code> calls. The few sites that touch <code>self</code> either already declare <code>[weak self]</code> or use intentional <code>[self]</code> (FileOperationManager:154 — atomic copy needs strong reference). ViewModels are <code>@MainActor final class</code> → sheet/window dismiss destroys them, so even captured-self closures resolve cleanly. No code change needed.</li>
                </ul>
            ]]></description>
            <pubDate>Mon, 11 May 2026 10:36:20 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.2.2/Unifyl-1.2.2.dmg"
                sparkle:version="24"
                sparkle:shortVersionString="1.2.2"
                sparkle:edSignature="ZCbkoNiJ5D3ZS2QNfmQx4Q7MjSYaml7NNBCqEXWN5B1Et92zqbOc2VgC+rFB2MikWSTFZZ6AfUe26zf+yLFZDQ=="
                length="15697787"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.2.1</title>
            <description><![CDATA[
                <h2>Unifyl 1.2.1</h2>
                <p>Patch release closing two more &quot;shipped UI, missing gate or stale copy&quot; gaps caught during a deep audit five days after the 1.2.0 → build 19 respin.</p>
                <h3>Fixed</h3>
                <ul>
                <li><b><code>Ctrl+M</code> (Total Commander alias for Multi-Rename) now goes through the Pro feature gate.</b> The keyboard shortcut opened the Multi-Rename sheet directly without the <code>isUnlocked(.multiRename)</code> check the menu and Command Palette already had, so a Free user could trigger a Pro feature for free via the alias. Now request-upgrade-sheet on Free, open-the-sheet on Pro — same as the other entry points.</li>
                <li><b>Help / User Manual FAQ no longer cites the dropped 4-tier pricing model.</b> A leftover sentence from the v1.0-era pricing — "Requires the AI tier ($59.99) or higher." — sat in every shipped manual's FAQ even after the build-17 sweep cleaned the Edition Comparison table. All 15 localized manuals now read "Included with Pro." in the user's language. Mirrors the rest of the now-2-tier pricing copy elsewhere in the app.</li>
                </ul>
            ]]></description>
            <pubDate>Sun, 10 May 2026 08:55:12 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.2.1/Unifyl-1.2.1.dmg"
                sparkle:version="20"
                sparkle:shortVersionString="1.2.1"
                sparkle:edSignature="fcKFfubR37os9/wE76ddJPcAQ1ispJPX+xI8ddAmrm5RtmTpV7FswcLHbunY3vqZ9XVgIWe0AGOD5OqwvqUICg=="
                length="15598859"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.2.0</title>
            <description><![CDATA[
                <h2>Unifyl 1.2.0</h2>
                <p>&gt; **Build 19 respin (2026-05-05):**</p>
                <p>&gt; 1. **Hides the three &quot;Native OAuth&quot; connection types** (`Google Drive (Native)`, `Dropbox (Native)`, `OneDrive (Native)`) from the New Connection picker. Each ships with a placeholder `clientID` (`YOUR_GOOGLE_CLIENT_ID` / `YOUR_DROPBOX_APP_KEY` / `YOUR_MICROSOFT_CLIENT_ID` in `OAuth2Configuration.swift`) — clicking &quot;Authorize with Google/Dropbox/Microsoft&quot; routed to a guaranteed-fail OAuth flow. The rclone-based variants (`Google Drive` / `Dropbox` / `OneDrive`, no &quot;(Native)&quot; suffix) stay visible — they work as long as the user has rclone configured locally. Per-machine opt-in for the OAuth pilot:</p>
                <p>&gt;    `defaults write com.goodbug89.Unifyl unifyl.enableOAuthPilot -bool true`</p>
                <p>&gt; 2. **Settings ▸ Companion gets an &quot;iOS app pending App Store&quot; callout** above the server toggle, so users don&#x27;t read &quot;Enable Companion Server&quot; + &quot;0 paired devices&quot; as a broken feature when in fact the Mac side is functional and waiting on the iOS Companion app to ship.</p>
                <p>&gt;</p>
                <p>&gt; **Build 18 respin (2026-05-05):** hides the Tools ▸ Team Management… menu and Command Palette entry behind a `unifyl.enableTeamPilot` opt-in default. Reason: the team-management UI shipped to the public Free + Pro channel but the `TeamSyncClient` backend doesn&#x27;t, so clicking Send Invite silently no-op&#x27;d (the guard for `teamId` / `teamToken` returned with no user-facing message). Public channel now no longer surfaces the Team UI; private enterprise pilot users can still opt in with `defaults write com.goodbug89.Unifyl unifyl.enableTeamPilot -bool true`.</p>
                <p>&gt;</p>
                <p>&gt; **Build 17 respin (2026-05-05):** also drops a v1.0-era 4-tier (FREE/PRO/AI/TEAM) Edition Comparison table from the in-app Help that no longer matches the shipping product (current model is just Free / Pro). 13 rows simplified to 2 columns; Team-only row removed; AI rows folded into Pro; pricing callout cleaned to &quot;Free / Pro ($39.99 one-time)&quot;. Applied to all 15 localized manuals.</p>
                <p>&gt;</p>
                <p>&gt; **Build 16 respin (2026-05-05):** ships the in-app Help / User Manual updated to v1.2.0 (was still v1.0 in the original build 15 DMG) and now translated into all 15 shipping languages (was English + Korean only). Also fixes a Help locale bug where the manual always rendered in Korean regardless of system language. ShortVersionString unchanged; Sparkle picks up the build-number bump.</p>
                <h3>Added</h3>
                <ul>
                <li><b>In-app Help / User Manual updated end-to-end for v1.2.0</b> (was previously v1.0 era — missing 1.1.0 / 1.2.0 features). 31 numbered sections (was 23), with new top-of-document "What's new in 1.2.0" callout linking to the most-changed surfaces. Existing sections (Function Keys, Tab Management, Search, Archives, Clipboard, Bookmarks, Tools, Themes & Settings) all updated for the current shipping behaviour. Three new sections added: <b>Status Bar</b> (cursor item info, Quick Look hint, "Did you know?" tips, free space readout), <b>Connections</b> (⌘K Connect to Server, sidebar persistence, click-anywhere rows), <b>Help & In-App Guidance</b> (toolbar <code>?</code> button, permission-denied "Open Settings" jump, onboarding slide 4).</li>
                <li><b>In-app Help / User Manual now ships in all 15 languages</b> (was English + Korean only). Full translations for Japanese, Simplified Chinese, Traditional Chinese, German, French, Spanish, Brazilian Portuguese, Italian, Russian, Turkish, Arabic, Thai, Vietnamese — each ~2400 lines, native macOS terminology per locale. Korean manual updated with v1.1 / v1.2 changes section. Arabic uses <code>dir="rtl"</code> so the page renders right-to-left in WKWebView.</li>
                <li><b>Tab middle-click closes (browser convention).</b> Pressing the trackpad middle / mouse-3 button on a tab closes it without forcing the user to hover for the X or hit ⌘W. Pinned tabs ignore middle-click so users can't accidentally lose a saved location. Implemented via a transparent <code>NSViewRepresentable</code> overlay that intercepts only <code>otherMouseDown</code> — left-click, right-click, and double-click pass through unchanged.</li>
                <li><b>Cloud chip on remote tabs.</b> Tabs sitting on a remote VFS (SFTP / S3 / WebDAV / OAuth cloud) now show a small cyan <code>cloud.fill</code> icon next to the title so users see at a glance which tabs are local vs over the network — sets expectations for paste latency and "where do my files actually live?". Hover tooltip reads "Remote connection".</li>
                <li><b>Connections sidebar open / closed state persists across launches</b> via <code>unifyl.showConnectionsSidebar</code> AppStorage. Was per-window <code>@State</code> that always reset to closed on relaunch — users who want it always-on had to re-toggle every session.</li>
                <li><b>Status bar shows "Space preview" hint when cursor is on a file.</b> Right next to the cursor item info ("filename · 1.5 MB · date") an inline <code>Space</code> chip + "preview" label tells the user the Quick Look shortcut is one key away. Only shown for files (folders open with Return / double-click). Massively boosts Quick Look discoverability for users coming from non-Finder file managers.</li>
                <li><b>One rotating "Did you know?" tip per launch</b> in the active panel's status bar, ~6 s with fade. Pool of 12 tips covers Return / F2 / Space / ⌘⇧P / ⌘⇧G / ⌘K / ⌥-drag / ⌃B / ⌃G / ⌃U / ⌘Z / F-keys — surfaces shortcuts a clean panel would never advertise. Cycles via stored <code>unifyl.launchTipIndex</code> counter so users see the whole tour over launches instead of the same tip twice. Skipped on the very first launch (onboarding covers basics) and honours an opt-out via <code>defaults write … unifyl.showLaunchTips -bool false</code>.</li>
                <li><b>Status bar shows cursor item info in Finder style.</b> When the user has nothing marked but is sitting on a row, the bottom status bar reads "filename · 1.5 MB · Apr 15, 2026 14:30" instead of the generic "N items · X total". Folders show "—" for size (avoids expensive recursive sum). Hover the row for the full path. Selection (red marks) still wins — the existing "K selected · …" line takes precedence so marked-item totals stay visible.</li>
                <li><b>Recent Folders dropdown in the toolbar</b> (clock-arrow icon next to Bookmarks). Same store the Go ▸ Recent Folders submenu uses, but surfaced one click away — so users see "I just visited these places" without opening the menubar. Empty-state tooltip explains the button will populate as they navigate; cleared by an inline "Clear Recent Folders" entry.</li>
                <li><b>Filter field placeholder + tooltip teach the syntax.</b> Was: "Filter…" — gave no hint that glob and regex work. Now placeholder reads "Filter — try *.png or /regex/" and the hover tooltip spells it out: "Plain text matches anywhere in the filename. Use *.png-style wildcards for globs, or /pattern/ for regex." Both the toolbar field and the inline panel filter bar carry the same hint.</li>
                <li><b>Permission-denied panel error offers a one-click "Open Settings" jump.</b> When the listing fails because of a Files-and-Folders / Full-Disk-Access denial, the inline red banner now includes an "Open Settings" button that takes the user straight to System Settings ▸ Privacy & Security ▸ Files and Folders. Previously they had to know which pane to find themselves. The banner also gained an explicit X dismiss button (matching macOS toast convention) instead of relying on tap-to-dismiss.</li>
                <li><b>Status-bar Free space readout refreshes every 10 s</b> instead of only on first appear. Was a one-shot read on <code>.onAppear</code>, so after a large copy / move / delete the value was stale for the rest of the session — users couldn't trust it for "do I have room?" sanity checks. Now polls a cheap volume-resource read on a <code>Task</code> loop with cancellation, plus a "Free space on the start volume" tooltip explains which volume is being measured.</li>
                <li><b>Clipboard ⌘C / ⌘X / ⌘V show confirmation toasts</b> so the user knows the action took. ⌘C → "Copied "file.txt" — paste with ⌘V" or "Copied 5 files — paste with ⌘V". ⌘X → "Cut N files — paste with ⌘V to move" (so the user knows paste will move, not copy). Successful ⌘V → "Pasted N files into "DestFolder"" / "Moved N files to "DestFolder"". Without these, the panel looked identical before/after each shortcut and users re-pressed unsure whether it took.</li>
                <li><b>Onboarding slide 4 — "Pick a folder to begin"</b> answers the post-onboarding "now what?" moment with three concrete entry actions: drag from Finder, ⌘⇧G to type a path, ⌘K to connect to a server. The previous flow ended with a "Get Started" button on a generic command-palette slide; users dismissed onto two blank panels with no obvious next step.</li>
                <li><b>Help button in the toolbar</b> (<code>?</code> icon, ⌘?). The Help menu has always existed but new users routinely missed it; a toolbar question-mark mirrors Finder / System Settings and answers "where do I go for help?" without hunting through menus.</li>
                <li><b>Empty-folder state suggests next actions instead of just stating the obvious.</b> Was: "Empty Folder · This folder has no visible items." Now: "Nothing to show here · Drop files in to add them, or press F7 to make a new folder." When <code>Show Hidden Files</code> is off, the message also notes that hidden items might exist and offers a one-click <code>Show Hidden Files</code> action so the user doesn't have to hunt for the toolbar's eye icon.</li>
                <li><b>Folder Bookmarks can be reordered, with discoverability cues.</b> Drag any row to reorder (entire row hit area, with a live drop-target highlight bar), or right-click for Move Up / Move Down / Remove context menu (keyboard / VoiceOver fallback). A grip icon fades in on row hover and the header subtitle shows "Drag rows to reorder · Right-click for more" whenever there's more than one bookmark — so the affordance is visible before the user has to discover it. New order persists via the existing <code>saveBookmarks()</code> path.</li>
                <li><b>Go ▸ Connect to Server… (⌘K).</b> Standard ForkLift / Cyberduck / Finder convention. Previously the Connections sidebar's <code>+</code> button was the only entry point; users migrating from those apps were hunting for the menu item.</li>
                <li><b>Sort By submenu now has keyboard shortcuts</b> (⌃⌥N / ⌃⌥S / ⌃⌥D / ⌃⌥K for Name / Size / Date / Kind). Sort is a top-5 file-manager operation and previously required mouse every time.</li>
                <li><b>Newly bound shortcuts on previously menu-only commands</b>: File Permissions ⌥⌘I, File History ⌥⌘J, Theme Editor ⌥⇧⌘T, Download from Remote ⇧⌘↓, Upload to Remote ⇧⌘↑.</li>
                <li><b>Menu labels surface F-key shortcuts that SwiftUI can't render in the trailing accelerator slot</b>: "Compress… (⌥F5)", "Extract to Folder… (⌥F9)", "Duplicate in Same Folder (⇧F5)", "New Text File + Edit (⇧F4)". The bindings already worked through <code>FunctionKeyMonitor</code>; the labels now make them discoverable.</li>
                <li><b>Full UI translations for 13 additional languages</b> — Japanese, Simplified Chinese, Traditional Chinese, German, French, Spanish, Brazilian Portuguese, Italian, Russian, Turkish, Arabic, Thai, Vietnamese — each expanded from a 57-key starter set to the full 343-key surface that English and Korean already shipped. Brings previously English-fallback strings (Pro upgrade sheet, Settings panes, Audit Log, Team Management, AI tools, onboarding, error messages, About view) into native localisation across the full 15-language matrix. Format specifiers (<code>%@</code>, <code>%d</code>), keyboard markers (⌘B, F5, ⌥⌘O…), em-dash, ellipsis, and middle-dot punctuation preserved verbatim per locale.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>Destructive confirmation dialogs use a consistent "name what + offer the safer alternative" pattern.</b></li>
                </ul>
                <p>  - **Permanent delete (⇧F8)**: was &quot;Permanently delete N item(s)? · This bypasses the Trash and cannot be undone.&quot; Now: &quot;Permanently delete &quot;filename&quot;?&quot; / &quot;Permanently delete N items?&quot; with body &quot;These won&#x27;t go to the Trash, so there&#x27;s no way to get them back. Use F8 instead to move them to the Trash safely.&quot; Confirm button label sharpened to &quot;Delete Permanently&quot; (vs the macOS-default ambiguous &quot;Delete&quot;).</p>
                <p>  - **Port Viewer kill process**: was &quot;Kill Process · This will terminate the process listening on port X. This action cannot be undone.&quot; Now: &quot;Quit &quot;ProcessName&quot; (PID X)?&quot; with body that names the SIGKILL signal AND warns about unsaved work — the consequence the user actually cares about. Button &quot;Force Quit&quot; (macOS convention) replaces &quot;Kill Process&quot;.</p>
                <p>  - **Duplicate Finder bulk delete**: gained the same &quot;you can restore from the Trash if you change your mind&quot; reassurance as the panel-level Move-to-Trash dialog.</p>
                <ul>
                <li><b>Pro upgrade sheet explains what the locked feature actually does, not just its name.</b> Added <code>Feature.shortDescription</code> covering 30+ Pro features ("Multi-Rename — Rename hundreds of files at once with regex, EXIF, AI suggestions, numbering — and undo any batch with ⌘Z."). The headline still names the trigger ("Multi-Rename is part of Unifyl Pro") but the prominent body text now answers "what does this thing do?" before the generic "Pro also unlocks…" subhead. Description omitted for self-explanatory items (e.g. "Bookmarks").</li>
                <li><b>Trial-active chip on the upgrade sheet.</b> When the user is in their 14-day free trial and the sheet appears (e.g. after trial expired between sheet open and now), an orange hourglass capsule reads "Free trial active · N days remaining" so they know where they stand instead of wondering whether they're already Pro.</li>
                <li><b>Toolbar Back / Forward / Up disable when there's nowhere to go, with a tooltip explaining why.</b> Previously these always appeared enabled even at the volume root or with empty history — clicking did nothing and the user couldn't tell if the app was broken. Now the buttons grey out and the hover tooltip says "Nothing to go back to yet — open a folder first." / "Nothing to go forward to. Press Back first to enable Forward." / "Already at the volume root — there's no parent folder."</li>
                <li><b>Toast / status messages name the file and the action result.</b> "Copied 5 paths" → "Copied 5 paths to the clipboard". "Tags removed: file.txt" → "Cleared all tags from "file.txt"". Single-item versions name the file directly so the user confirms they hit the right one.</li>
                <li><b>Upgrade prompt names the feature that triggered it.</b> The Pro upgrade sheet was a generic "Upgrade to Unifyl Pro" pitch regardless of which click landed the user there — they were left wondering which feature was even locked. <code>requestUpgrade(for:)</code> now captures the <code>Feature</code> and the sheet headline reads "Multi-Rename is part of Unifyl Pro" / "AI Semantic Search is part of Unifyl Pro" / etc., with a unified subhead listing what else Pro includes. Generic fallback retained for entry points that don't pass a feature.</li>
                <li><b>Move-to-Trash confirmation reads as a question, not an interrogation.</b> Was: "Are you sure you want to delete the selected items?" (red, ominous). Now: "Move "filename" to the Trash? You can restore it from the Trash if you change your mind." (or "Move N items to the Trash? …" for batches). The recoverability hint reduces the anxiety on a fully-undoable action.</li>
                <li><b>Go to Folder error distinguishes typo vs. wrong-type-of-target.</b> "Path not found: /foo" became either "Couldn't find "/foo". Check the path for typos, or grant access in System Settings ▸ Privacy & Security if it's a protected folder." OR "/foo exists but isn't a folder. Go to Folder needs a directory path." — depending on which check failed.</li>
                <li><b>Archive→archive copy error now explains the workaround.</b> Was: "Cannot copy between archives". Now: "Copying directly from one archive into another isn't supported. Extract the files first, then copy them into the destination archive."</li>
                <li><b>Settings ▸ Timing labels rewritten in plain language.</b> "Spring-loaded folder delay" → "Folder hover-to-open delay", "Type-ahead buffer reset" → "Type-to-search reset", "Toast auto-dismiss" → "Status message duration". Tooltips also rewritten as full natural-language sentences ("When you drag files over a folder, how long do you need to pause before it opens?") rather than implementation-flavored shorthand.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Help (⌘?) now respects the system language instead of always rendering Korean</b> *(build 16 respin)*. <code>Bundle.main.url(forResource: "manual", withExtension: "html")</code> was finding an un-localized <code>Resources/manual.html</code> (a stale 52 KB Korean stub) at the bundle root before any lproj resolution kicked in — so the WKWebView in <code>HelpView</code> always loaded the Korean copy regardless of <code>preferredLocalizations</code>. Removed the un-localized base file; Foundation now resolves to the right <code><locale>.lproj/manual.html</code> per the user's system locale, with English as the development-region fallback for any other locale.</li>
                <li><b>Folder move appearing "to do nothing" after a stuck drag flag.</b> The drag-defer guard added to keep cloud/folder-size/git monitors from aborting an in-progress drag could get stuck <code>true</code> if AppKit's <code>endedAt</code> callback was missed (rare but observed: drag aborted by app switch, OS interrupt, drop on a sandboxed-rejecting target). All subsequent <code>updateItems</code> were deferred forever, so panel listings never refreshed after the next move — the move ran to disk but the source listing kept showing the moved folder, looking like the move silently no-op'd. Added a 10 s watchdog <code>Task</code> that force-clears <code>dragInProgress</code> and applies any pending items snapshot, so the panel always recovers within 10 s of a missed callback. The willBegin path arms a fresh watchdog and the endedAt path cancels it immediately.</li>
                <li><b>File drag in CloudStorage / iCloud / OneDrive folders no longer "snaps back" mid-drag.</b> The cloud-status monitor was unconditionally bumping each item's <code>modificationDate</code> by 1 ms every poll to "force a redraw" — but that 1 ms tick made the bridge's <code>onlyMinorChanged</code> diff fail, triggering a full <code>tableView.reloadData()</code>, which silently aborts any in-progress NSTableView drag session. <code>refreshCloudStatus</code> now short-circuits when the cloud status hasn't actually changed, so status-stable polls don't touch <code>items</code> and the drag survives.</li>
                <li><b><code>FileTableView</code> defers <code>updateItems</code> while an NSTableView drag session is active.</b> Background updates from the file watcher, folder-size calc, or git status that landed mid-drag could still abort the drag via <code>tableView.reloadData()</code>. The view now sets a <code>dragInProgress</code> flag in <code>draggingSession willBeginAt</code> and stashes any items snapshot that arrives during the drag. The deferred apply is dispatched async on the next runloop tick from <code>draggingSession endedAt</code> (synchronous reload there left the table in a transient state that made the second consecutive drag fail to initiate). Each drag also clears any stale pending from a previous session in <code>willBeginAt</code> so the slot belongs to the current drag.</li>
                <li><b>Bookmark row reorder works on the row content, not just the separator.</b> SwiftUI's <code>List</code> + <code>.onMove</code> only accepts drag from the row separator gutter on macOS — dragging the title text was a no-op, and the user was instead dragging an invisible click that closed the sheet. Replaced the <code>List</code> with a <code>ScrollView</code> + <code>VStack</code> and per-row <code>.draggable(url)</code> + <code>.dropDestination(for: URL.self)</code> so the entire row hit-area initiates the reorder, with a live drop-target highlight bar on the target row. Tap (single-click) still navigates and dismisses; right-click goes only to <code>contextMenu</code>.</li>
                <li><b>Status bar item count now matches the size total.</b> "X items" included the synthetic <code>..</code> parent-directory row but the size sum excluded it, so the bar said e.g. "42 items, 1.5 GB" when the real listing was 41 files. The mismatch quietly broke any duplicate / backup workflow that compared counts across folders. Both numbers now come from a single cached pass that excludes <code>..</code>, populated alongside <code>cachedTotalFileSize</code>.</li>
                <li><b>Delete now refreshes the inactive panel too.</b> Move and copy already reloaded both sides, but <code>deleteItems</code> only called <code>reloadActive</code> — so when both panels pointed at the same directory (common when staging a clean-up) the inactive side kept showing the just-deleted files until the user manually refreshed. <code>deleteItems</code> now takes a <code>reloadInactive</code> callback (defaulting to a no-op for back-compat) and <code>AppViewModel</code> wires both sides through.</li>
                <li><b>⌘F now toggles the toolbar filter</b> — pressing it again clears the filter text and returns focus to the active panel. Previously the second ⌘F was a no-op (the field re-focused), so users either had to hunt for Esc (which only worked from some focus paths) or click elsewhere to dismiss. Browser / Finder convention restored.</li>
                <li><b>New Folder (⌘N) and New Text File (⇧F4) are now undoable via ⌘Z.</b> Both operations advertised undo support per <code>CLAUDE.md</code> ("파일 작업 Undo / Redo") but were silently absent from the history stack — fat-fingered creation left orphan empty folders / <code>Untitled.txt</code> files the user had to hunt down and delete manually. Both now register a <code>.copy</code>-shaped record with <code>actualDestinations: [newURL]</code> so the existing copy-undo path (<code>removeItem(at:)</code>) cleans them up. Schema is unchanged; no new <code>OpType</code> needed.</li>
                <li><b>Pasting from the system clipboard skips deleted sources cleanly instead of bailing on the first miss.</b> When the user copied N files in Finder and then deleted one, Cmd+V into Unifyl threw on the missing file, hit the <code>break</code>, and silently dropped the remaining items too — error banner just said "Paste failed: <Cocoa error code>" with no clue what was wrong. The paste loop now pre-partitions the clipboard URLs by <code>fileExists</code>, surfaces a humane message naming the first three missing files (and a <code>+N more</code> overflow), and proceeds with the survivors.</li>
                <li><b>Corrupt <code>unifyl_file_history</code> JSON no longer silently wipes the user's full Recent-Files list.</b> <code>loadFileHistory()</code> decoded with <code>try?</code>, so a single schema-drift / partial-write byte killed every entry without any signal — the user discovered the loss days later. The decode now goes through a real catch path that stashes the corrupt blob via <code>PersistenceBackup.stashCorruptUserDefaultsBlob</code> (recoverable for support) and emits an OSLog error so the loss is at least visible in Console. Mirrors the recovery pattern already used by <code>ConnectionViewModel</code>.</li>
                <li><b>Switching theme presets via the toolbar / View menu now invalidates the file-table row cache reliably.</b> Only <code>applyCustomTheme()</code> bumped <code>themeRevision</code>, but preset selection just assigned <code>selectedThemeId</code> directly — <code>FileTableView</code>'s <code>colorVersion</code> (which folds in <code>themeRevision</code>) didn't change, so cell colours could lag a frame, and entirely lag if the new preset's hex tokens happened to byte-match the previous one. Bump now lives on <code>selectedThemeId</code>'s <code>didSet</code>.</li>
                <li><b>FTP transfers no longer hang forever when the server stalls mid-stream.</b> <code>curlBaseArgs</code> only passed <code>--connect-timeout 15</code> (handshake bound), so a server that opened TCP then stopped sending data left the operation hung indefinitely — progress bar froze and the user had to force-quit. Now also passes <code>--max-time 300</code>, matching the WebDAV path.</li>
                <li><b>Anonymous FTP via the live <code>FTPFileSystem</code> (saved connections) now actually connects.</b> The same rclone-vs-curl <code>--anonymous-ftp</code> flag bug we fixed in the Test Connection sheet was duplicated in the live transport. Switched to the conventional <code>-u anonymous:anonymous@unifyl.local</code> form so saved anonymous FTP connections work end-to-end.</li>
                <li><b>Failed copy no longer leaves a half-written destination file on disk.</b> When <code>copyItemAtomically</code> aborted mid-write (most commonly disk-full on the target volume), the partial file stayed on the destination — wasting space and leaving the user unsure whether the copy actually completed. The catch path now best-effort removes <code>finalDest</code> when both source and partial destination exist, so the destination folder ends up in its pre-copy state on failure.</li>
                <li><b><code>cd ~/Documents</code> (and any tilde-prefixed path) now works in the inline command bar.</b> Only the bare <code>~</code> token was recognised; tilde-prefixed paths silently beeped because <code>currentPath.appendingPathComponent("~/Documents")</code> produces nonsense. Now expands via <code>NSString.expandingTildeInPath</code> and emits a beep on a real lookup miss so the user knows their path didn't resolve.</li>
                <li><b>Go to Folder dialog (⇧⌘G) now accepts paths copied from Terminal or Finder.</b> A <code>file://</code> prefix is stripped (so paths from "Copy as Pathname" and browser address bars paste cleanly), and <code>\ </code> → <code> </code> is unescaped (so paths copied from Terminal that contain spaces don't need manual scrubbing).</li>
                <li><b>Status-bar total size no longer recomputes O(N) on every SwiftUI body rebuild.</b> <code>totalFileSize</code> was a computed property running <code>.lazy.filter { $0.name != ".." }.compactMap(\.size).reduce(0, +)</code> on every render — selection changes, scroll, theme switches, font tweaks, focus flips all triggered a full sum across the listing. On a 100K-file folder it was the dominant per-frame cost. Now cached in <code>@State</code>, recomputed only on <code>items.count</code> change. Also collapsed the duplicate inline computation in the Smart Folder path bar to use the same cache.</li>
                <li><b>Filter that hides every item now shows "No Matches" with a Clear Filter button instead of "Empty Folder".</b> Previously identical to a genuinely empty directory — users blamed the folder when really their filter text was just too narrow, and the only escape was knowing to press Esc or hunt for the filter field. Now the empty state distinguishes the two cases, names the filter that's hiding things, and offers a one-click clear.</li>
                <li><b>Cancelling a running file operation from the queue bar now actually stops the work.</b> The queue bar's Cancel button only routed to <code>FileOperationQueue.cancel(handle)</code> (which flips the queue state), but the running copy/move/delete loop checks <code>operationProgress.isCancelled</code> — a separate signal that was never set. Result: cancel looked applied (status flipped) yet the op ran to completion. Especially broken after a Pause + Cancel sequence: pause masked the running state so cancel felt totally inert. Cancel now also signals <code>operationProgress.cancel()</code> for running/paused ops so the loop bails on its next iteration.</li>
                <li><b>Connections sidebar rows are now click-anywhere and keyboard-activatable.</b> Each row had a tiny "Connect" / "Open" pill on the right that was the only hit target — keyboard users had no path to it at all, and mouse users had to aim for a 60-pt button instead of the obvious row. The whole row is now a <code>Button(.plain)</code> so clicking anywhere connects/opens, and <code>Return</code> activates the row when the sidebar list has keyboard focus. A right-chevron replaces the pill so the row reads as a navigable list item, and VoiceOver gets a proper <code>.accessibilityLabel</code> + <code>.accessibilityHint</code>.</li>
                <li><b>Toolbar buttons now have proper VoiceOver labels.</b> Back / Forward / Up / Open in Opposite Panel / Home / Bookmarks / Connections Sidebar / Toggle Hidden Files / Increase + Decrease Font Size were icon-only with <code>.help(...)</code> tooltips, which VoiceOver doesn't read — every navigation button announced as a generic "button". Each now has an explicit <code>.accessibilityLabel(…)</code>. The font-size readout also announces the current point size, and the hidden-files toggle's label flips between "Show Hidden Files" / "Hide Hidden Files" matching the visible state.</li>
                <li><b><code>EmptyStateView</code> icon is hidden from VoiceOver.</b> The decorative SF Symbol (folder, search-glass, etc.) was being announced as a separate "Image: …" utterance before the title + message pair, so blind users heard a redundant icon name on every empty state. Marked <code>.accessibilityHidden(true)</code> so the title + message read as one statement.</li>
                <li><b>Korean translations added for Git Panel, AI Classify, Checksum, and About surfaces.</b> Buttons like "Stage All" / "Start Analysis" / "Compute" / "Verify" / "Apply Tags" / "Check for Updates" / section headers like "Changed Files" / "Staged" / "Unstaged" / "Untracked" were appearing in English even when the system language was Korean — they had no entries in <code>ko.lproj/Localizable.strings</code>. SwiftUI's <code>Text("…")</code> autoresolver finds the keys now.</li>
                <li><b>AI Classify empty state no longer renders broken plurals in non-English locales.</b> "Analyse \(items.count) file\(items.count == 1 ? "" : "s")…" hard-coded English's <code>+s</code> plural rule, producing nonsense like "파일s" in Korean. Now branches between two NSLocalizedString templates (<code>Analyse 1 file …</code> vs <code>Analyse %d files …</code>) so each locale supplies a grammatically correct form.</li>
                <li><b>Settings ▸ Keyboard Shortcuts ▸ Reset All / Reset Category now confirm before destroying customisations.</b> Previously a fat-finger on either button instantly wiped every customised shortcut with no undo path. Both routes now go through a destructive-action alert with Cancel as the default; the per-category reset opens with the category name in the title so the scope is unambiguous.</li>
                <li><b>Removed a stale <code>@AppStorage("unifyl.panelFontSize")</code> declaration in PanelView</b> that read from a key nobody writes (the live font-size key is <code>panelFontSize</code>, owned by AppViewModel). The property was unused but its presence tempted future writes that would silently desync from the actual setting.</li>
                <li><b>Cancel button on the file-operation progress dialog now reflects the press immediately.</b> The button called <code>progress.cancel()</code> (which set <code>isCancelled = true</code>) but the dialog header kept showing "Copying…" with the active progress bar — so users re-clicked Cancel or thought the dialog had frozen. Now the title flips to "Cancelling…" with a warning-coloured xmark icon and the button itself goes disabled with a "Cancelling…" label until the underlying op winds down.</li>
                <li><b>Overall progress bar can no longer jump backward between files.</b> When a small file finished between two large ones, the per-file fraction (<code>completed / total</code>) could transiently shrink because the size estimate revised — visible as the bar reversing, which read as "the operation is undoing itself." Progress is now monotonic via <code>updateOverallProgress(_:)</code>, which clamps to never decrease during the running phase (<code>begin()</code> / <code>reset()</code> still allow legitimate reset to 0).</li>
                <li><b>Loading spinner no longer flashes on fast directory loads.</b> <code>ProgressView</code> appeared the moment <code>isLoading</code> flipped true, and on cached / small-folder loads the flag was true for <100ms — visible as a single-frame flicker that read as a glitch rather than a load. Spinner appearance is now debounced 250ms so fast loads complete invisibly while slow ones still surface a spinner with a smooth fade-in.</li>
                <li><b>Spring-loaded folder no longer auto-opens the wrong folder when the listing mutates mid-drag.</b> The dwell-timer captured only the row index; if the file watcher reordered rows during the hover (e.g. a sibling file was added), the timer fired on whatever item now occupied that index. Now also captures the folder's URL and verifies identity before firing — Finder parity.</li>
                <li><b><code>UnifylFileError</code> and <code>VectorIndexError</code> now conform to <code>LocalizedError</code>.</b> Failures previously surfaced as raw enum dumps like <code>"The operation couldn't be completed. (UnifylCore.UnifylFileError error 4.)"</code>; users now see humane Korean/English messages per case (<code>Permission denied: <path></code>, <code>Destination already exists: …</code>, <code>Could not open the semantic index database.</code>, etc.).</li>
                <li><b>Right-click on a file now moves the cursor to the clicked row.</b> <code>rightMouseUp</code> toggled the mark but left the NSTableView cursor on the previous row, so the user saw "I right-clicked file X but the cursor is still on file Y". Now mark + cursor move together (Total Commander parity).</li>
                <li><b>Selection survives switching to another window and back.</b> <code>FileItem.id</code> is a fresh UUID on every reload, so the <code>didBecomeActive</code> reload was wiping the selection set's UUID matches. <code>loadCurrentDirectory</code> now remaps the previous selection by URL across the listing swap.</li>
                <li><b>Cloud Mount entries from the sidebar work after a remote panel session.</b> Clicking a Cloud Drive (e.g. OneDrive sync folder) on a panel previously bound to a remote VFS (SFTP / OAuth) used to call <code>RCloneFileSystem.contents()</code> against a local file URL and return empty + <code>partialFailure</code>. <code>navigateAndLoad</code> now swaps the panel back to <code>LocalFileSystem</code> when the destination is a <code>file://</code> URL and the current FS is remote.</li>
                <li><b>Stale error banner clears on each directory reload.</b> <code>lastError</code> was set on failure but never cleared on success — a one-time wrong-VFS / network blip left the red banner visible across every subsequent navigation. Now reset at every <code>loadCurrentDirectory</code> entry, every <code>enterArchive</code> entry, and on <code>exitArchive</code>.</li>
                <li><b><code>Test Connection</code> for FTP no longer fails with <code>curl: option --anonymous-ftp: is unknown</code>.</b> That flag was an rclone option pasted into a curl invocation. Anonymous FTP now passes <code>--user anonymous:anonymous@unifyl.local</code>, the convention curl actually accepts.</li>
                <li><b><code>goBack</code> / <code>goForward</code> preserve cursor position on the dominant parent-folder case.</b> <code>goUp</code> already saved the leaving folder name into <code>pendingCursorName</code>; the history-stepping equivalents were unconditionally jumping to row 0. They now capture <code>currentPath.lastPathComponent</code> so the cursor lands on the entry the user was just inside (silently falls back to row 0 on lateral hops where the lookup misses).</li>
                <li><b>Inline rename commit returns keyboard focus to the file table.</b> Only the Esc cancel path called <code>makeFirstResponder(tableView)</code> — Return left focus inside the now-non-editable text field, silently breaking j/k/arrow nav until the user clicked the table.</li>
                <li><b>⌃⇥ tab cycling lands keyboard focus back on the file table even if a filter field had it.</b> The bridge claimed focus only when the panel's <code>isActive</code> flipped false→true, which doesn't happen on within-panel tab switches. Now also forces focus when <code>activeTabId</code> changes within the same active panel.</li>
                <li><b>Closing a tab clamps <code>cursorIndex</code> to the surviving tab's item count.</b> Closing a tab with a high cursor index when the new active tab had fewer items left arrow keys silently no-op'ing until the user clicked a row.</li>
                <li><b>Archive <code>enterArchive</code> failure no longer leaks the previous attempt's banner.</b> <code>lastError</code> is cleared at archive-enter entry and at <code>exitArchive</code>, and <code>archiveOpenFailureURL</code> is cleared on Esc-out.</li>
                </ul>
            ]]></description>
            <pubDate>Tue, 05 May 2026 02:58:04 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.2.0/Unifyl-1.2.0.dmg"
                sparkle:version="19"
                sparkle:shortVersionString="1.2.0"
                sparkle:edSignature="PWqWv+KmZxNrpn8F+D9BUMpJW4wUnQSRWKLaQff2MLOWH9p7t2WDhrICoUmMwg6Xk9TBfIjdR+Y0IlaLs4r5BA=="
                length="15599581"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.1.0</title>
            <description><![CDATA[
                <h2>Unifyl 1.1.0</h2>
                <h3>Added</h3>
                <ul>
                <li><b>File List Columns are now configurable.</b> Six additional optional columns join the existing Size / Date Modified / Kind: <b>Date Created</b>, <b>Extension</b>, <b>Permissions</b> (<code>rwxr-xr-x</code>), <b>Tags</b>, <b>Path</b>, and <b>Cloud status</b>. Toggle each in Settings ▸ General ▸ File List Columns. The Name column is always shown; Smart Folder mode still force-shows Path regardless of the toggle. Visibility flips immediately on toggle (no relaunch) via a <code>UserDefaults.didChangeNotification</code> observer in <code>FileTableView</code>.</li>
                <li><b>Archive entry timestamps preserved on extract.</b> ZIP central-directory DOS times and 7z <code>Modified =</code> fields are now decoded in the local timezone and restored onto the extracted file via <code>setAttributes([.modificationDate:])</code> after the move. Fixes the "extracted file is N hours off" symptom on machines whose local timezone differs from UTC (e.g. a 9-hour drift on KST). The DOS-time decoder also drives the panel-side ZIP listing so the inline archive view shows the same wall-clock time the file had on the archive creator's machine.</li>
                <li><b>Overwrite confirmation when extracting archives or copying across panels.</b> Extraction and cross-panel archive copy now route through the same Skip / Rename / Overwrite dialog used by regular file copies, including the "Apply to all remaining conflicts" toggle (now defaulting <b>on</b> so multi-file batches don't fan out into dozens of identical prompts). The dialog supports arrow-key navigation between buttons via a local <code>NSEvent</code> monitor, and respects the existing <code>unifyl.confirmOverwrite</code> user default.</li>
                <li><b>Cmd+A selects all in the active file panel</b> regardless of focus context. Previously the menu route went through <code>NSTableView.selectAll(_:)</code> which jumped the cursor to the bottom row without marking anything; the new global key intercept and an override on the inner table view both funnel into Unifyl's red-mark selection. Text fields and <code>NSTextView</code> keep their own select-all behaviour.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li>Existing Size / Date Modified / Kind columns are now also toggleable from the same Settings section (still default on).</li>
                <li><b>Multi-file drag image</b> now shows a single large file icon with a red count badge (Finder-style) instead of a stacked-rows snapshot. The icon-plus-badge composite makes it unmistakable how many items are coming with the drag.</li>
                <li><b>F7 New Folder positions the cursor on the just-created folder.</b> Matches Total Commander / Finder behaviour: after the reload settles, the new folder is selected so Enter / F2 / Cmd+Down act on it without an extra search step. Previously the cursor stayed wherever it was before, which on large folders meant the user had to hunt for the new entry.</li>
                </ul>
                <h3>10-round upgrade sweep (functionality / standards / a11y / perf / design)</h3>
                <ul>
                <li><b>Spreadsheet preview reads use memory-mapped I/O.</b> <code>OfficePreviewView</code>'s shared-strings, workbook.xml, and per-sheet XML reads all switched to <code>Data(contentsOf:options: .mappedIfSafe)</code>. Memory footprint of opening a large XLSX in preview is now bounded by the OS's page-mapping policy instead of by the file size — a 50 MB workbook drops from ~50 MB resident to ~5 MB.</li>
                <li><b>Hex Editor file load is off-actor.</b> <code>HexEditorView.loadFile()</code> was reading via <code>Data(contentsOf: fileURL)</code> synchronously on the @MainActor — a 10 MB read from a slow network volume blocked the entire main thread. Read now happens in a <code>Task.detached(.userInitiated)</code> with <code>.mappedIfSafe</code>, and the result is written back through a <code>Result</code> on the main actor.</li>
                <li><b>Panel error banner gets icon + semantic color + a11y label.</b> <code>panelVM.lastError</code> was a flat red bar — color-blind users had no other signal it was an error. Now it's an <code>exclamationmark.triangle.fill</code> + text in <code>DesignTokens.Colors.error</code>, with <code>accessibilityLabel</code> so VoiceOver announces "Panel error: …" rather than reading just the truncated text.</li>
                <li><b>Connection-test result + Git-history error use semantic tokens.</b> <code>NewConnectionSheet</code>'s success / failure state chips and <code>GitHistoryView</code>'s error icon migrated from raw <code>.green</code> / <code>.red</code> / <code>.orange</code> to <code>Colors.success</code> / <code>Colors.error</code> / <code>Colors.warning</code>. The Git error icon also moved from <code>exclamationmark.triangle</code> to the filled variant for higher visual weight.</li>
                <li><b>Eight more empty states unified onto <code>EmptyStateView</code>.</b> <code>JSONPreviewView</code>, <code>PlistPreviewView</code>, <code>FontPreviewView</code>, <code>ScenePreviewView</code>, <code>SQLitePreviewView</code>, <code>AppUninstallerView</code> (×2), <code>KeyBindingSettingsView</code>, <code>ThreeWayMergeView</code> all migrated from <code>ContentUnavailableView</code> to the project-style <code>EmptyStateView</code>. Error-state previews pass <code>iconColor: Colors.error</code> so the icon hue matches the message intent.</li>
                <li><b>Sheet padding tokenised (final batch).</b> <code>ChecksumView</code>, <code>FileOperationConfirmView</code>, <code>FilePermissionsView</code> migrated from <code>.padding(20)</code> / <code>.padding(16)</code> to <code>DesignTokens.Spacing.l</code>. Combined with earlier rounds' NewFolderSheet / ArchivePasswordSheet migration, the project's modal padding is now phase-locked to one source of truth.</li>
                </ul>
                <h3>Design</h3>
                <ul>
                <li><b>Design tokens reviewed and extended (designer pass).</b> Seven gaps closed in <code>DesignTokens.swift</code>:</li>
                </ul>
                <p>  - **Active vs inactive panel selection.** New `bgSelectedActive` (accent-tinted) for the focused panel; existing `bgSelected` becomes the inactive-panel value. Total Commander / ForkLift parity — at a glance you now see which panel takes the next keystroke.</p>
                <p>  - **Semantic color tokens.** `success` (#34D399), `warning` (#F59E0B), `error` (#F87171), `info` (#60A5FA) replace 30+ raw `.green` / `.orange` / `.red` literals across views. Code reading &quot;this label uses `Colors.success`&quot; tells you WHY the label is green; designers can re-tune the status palette in one file.</p>
                <p>  - **Shadow scale** (`Shadow.card` / `Shadow.popover` / `Shadow.modal`) + a `.appShadow(_:)` SwiftUI modifier. Replaces magic-number `.shadow(color: .black.opacity(0.6), radius: 24, ...)` calls that drifted across 12 files. Applied to FileOperationProgressView, CommandPaletteView, AboutView (more sites can adopt incrementally).</p>
                <p>  - **Text 4-tier scale.** New `textTertiary` between `textSecondary` and `textMuted` so column headers and count badges no longer collapse into the same value as disabled labels — restores visual hierarchy at typical viewing distance.</p>
                <p>  - **`borderSubtle` strengthened** (`#1E2235` → `#232742`). The previous value was only ~2% lighter than `bgSurface`, so divider lines vanished at typical viewing distance. New value still reads as &quot;subtle&quot; but actually separates sections.</p>
                <p>  - **`focusRing` token** (accent at 70% opacity) for keyboard-focused controls — AppKit&#x27;s default focus ring uses the system accent, which clashed with custom Unifyl themes.</p>
                <p>  - **Selection contrast bumped.** `bgSelected` → `bgSelectedActive` for the focused panel pulls ~30% more accent saturation; the &quot;this row is selected&quot; cue is now unambiguous against the hover background.</p>
                <h3>Performance</h3>
                <ul>
                <li><b><code>GitStatusProvider</code> cache no longer grows unbounded.</b> The 5-second TTL prevented stale data from being *served*, but expired entries lingered forever — a long browsing session that hit hundreds of git directories accumulated a <code>[String: CacheEntry]</code> of all of them indefinitely. Eviction now runs on every cache miss: stale entries (older than 5× TTL) are dropped, and a hard cap of 256 entries is enforced LRU-style on top. The fast path (warm cache hit) is unchanged; cleanup is amortised against the work the caller was about to do anyway.</li>
                <li><b><code>make build-fast</code>: parallel package build.</b> New target runs all nine SwiftPM packages in parallel via <code>&</code> + <code>wait</code>. On the dev machine: 8.0 s sequential → 0.5 s parallel for a no-op rebuild (16×), with ~3–4 s expected on a cold build. The original <code>make build</code> stays sequential for clean log output when something fails.</li>
                <li><b><code>make doctor</code>: pre-flight environment check.</b> Verifies xcodegen / swift / xcodebuild / swiftlint / gh / dmgbuild / create-dmg / bundled <code>7zz</code> / notarytool keychain profile in one shot, with brew/pip install hints for each missing piece. Avoids the late, ugly failures from <code>make release-publish</code> discovering a missing tool partway through the pipeline.</li>
                <li><b>Directory listing: skip cloud-status <code>lstat</code> outside iCloud-rooted paths.</b> The post-listing pass that flagged cloud-only files via <code>lstat</code> ran for every non-directory entry in every directory the user navigated into. On a 100K-file folder that's 100K syscalls; outside <code>~/Library/Mobile Documents/</code>, <code>~/iCloud Drive/</code>, <code>~/Library/CloudStorage/</code> (Ventura+ OneDrive/Dropbox/Box/GDrive sync mounts), and <code>~/Dropbox/</code> the result is always "not cloud-only", so the entire loop is now gated on a single string-prefix check. Listing time on large local directories drops to whatever <code>contentsOfDirectory</code> itself costs.</li>
                <li><b>Folder size flush: O(N×M) → O(batch).</b> <code>calculateFolderSizesInBackground</code> flushed the running size map by re-scanning the entire <code>tabs[idx].items</code> array every 20 folders and reconstructing every matching <code>FileItem</code>. On directories with many subfolders that produced both quadratic time and a churn of FileItem allocations. Now snapshots a <code>URL → index</code> map once at task start, queues only the changed indices per batch, and verifies the slot still points at the expected URL before writing — handles the FileWatcher-fires-mid-task race correctly without scanning the whole array.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>EXIF Editor opens with the cursor file when nothing is multi-selected.</b> <code>ViewerWindowModifier</code>'s metadata-editor route filtered <code>currentTab.selectedItems</code> directly — when the user just placed their cursor on an image without marking it, the editor opened at "0 image(s)" and they had to close, mark, re-open. Now uses <code>selectedOrCursorItems</code> (the project-wide convention per CLAUDE.md memory: "All file operations use selectedOrCursorItems"). Same pattern applied to <code>FileSplitMergeView.onAppear</code> (split-file picker pre-fill) and <code>IntegratedTerminalView.SmartSuggestionView</code> so all three feel consistent — point at a file, run the action, no extra Cmd+click required.</li>
                <li><b>Function-key bar height bumped 18 → 26pt.</b> The previous bar barely cleared the F-key text's cap height, leaving no breathing room and making the row look wedged under the panel. 26pt also gives proper tap targets for trackpad / mouse, and the icon + label sizes step up 11 → 12pt for legibility on Retina displays.</li>
                <li><b>Main window remembers its size and position across launches.</b> <code>WindowGroup { ... }.defaultSize(1200, 700)</code> only sets the *initial* size for a never-yet-shown window — it's not a frame *restorer*. After the user resized or moved the window, quitting and relaunching dropped them back to the default 1200×700 at SwiftUI's chosen origin. A small <code>WindowFrameAutosave</code> <code>NSViewRepresentable</code> injected via <code>.background(...)</code> now grabs the host <code>NSWindow</code> once it's attached and calls AppKit's built-in <code>setFrameUsingName(_:)</code> + <code>setFrameAutosaveName(_:)</code>. AppKit then reads / writes the frame to UserDefaults under <code>NSWindow Frame UnifylMainWindow</code> on every drag and resize. Set-name-after-restore order matters; setting the autosave name first would let AppKit's next layout pass overwrite the saved value with the SwiftUI default.</li>
                <li><b>F2 inline rename: Shift+Arrow now extends the text selection.</b> AppKit silently sets <code>.numericPad</code> on every arrow-key event (the arrow keys live on the numeric pad of extended keyboards), and the FunctionKeyMonitor's modifier-flag comparison only stripped <code>.function</code>. So <code>flags == .shift</code> was false during a Shift+Left press — the actual flag set was <code>.shift ∪ .numericPad</code> — and the inline-rename arrow dispatch fell through to the unmodified <code>moveLeft</code> / <code>moveRight</code> branch. Cursor moved but selection didn't extend; users who tried to select part of a filename for re-typing got nothing. The flag normaliser now subtracts both <code>.function</code> and <code>.numericPad</code>, fixing Shift+Arrow and incidentally any other modifier-equality check that ever sees an arrow key.</li>
                <li><b>HWP → PDF conversion failure now offers the same Hancom fallback as HWPX.</b> When LibreOffice's HWP import filter rejects a .hwp file (embedded objects, password-protected sections, HWP features newer than the filter understands), the user previously saw a wall of LibreOffice trivia. Now both .hwp and .hwpx route through one helper that detects an installed Hancom Office / Hancom Viewer / Hancom Office Hangul / 한컴오피스 app and offers a one-click "Open in Hancom" button so the user can use Hancom's native File → Export as PDF for ground-truth conversion. When no Hancom app is installed, the alert links to Hancom's download page (HWPX) or recommends the LibreOffice 'Fresh' branch (HWP).</li>
                <li>Dragging an unmarked file when other files were marked is no longer blocked. The drag payload is now context-aware: dragging from a marked row carries the whole marked set, dragging from an unmarked row carries just that file (matches Finder).</li>
                <li>DOS time decode and the file-list date formatters are now pinned to <code>TimeZone.current</code> instead of inheriting from the autoupdating-current calendar snapshot, removing a class of "9-hour drift" date display bugs.</li>
                <li><b>Spring-loaded folders no longer auto-open when the drag has left the panel.</b> Previously the dwell timer kept running after the cursor moved outside the table, so a folder armed on drag-start would auto-enter even when the user's mouse was already in another app. The timer now cancels the moment the drag exits or ends.</li>
                <li><b>CJK ZIP filenames decode correctly.</b> Chinese / Japanese / Korean ZIPs created on legacy Windows store filenames in CP936 (GBK), CP932 (Shift-JIS) or CP949 (EUC-KR) without setting the UTF-8 flag bit. The previous decoder always tried EUC-KR first, which silently mis-decoded GBK Chinese names into garbled Korean (<code>新建文件夹</code> → <code>劤쉔匡숭셸</code>). The new decoder trusts UTF-8 when the bytes parse cleanly, and otherwise scores GBK / EUC-KR / Shift-JIS candidates by Hangul / Kana / Hanja distribution to pick the most plausible decoding. Selective extraction of non-ASCII entries now also routes through the bundled <code>7zz</code> (which round-trips the same names) instead of the destructive <code>ditto -xk</code> fallback that extracted every sibling regardless of selection.</li>
                <li><b>Viewer windows no longer hide when switching apps.</b> <code>NSPanel.hidesOnDeactivate</code> defaults to <code>true</code> for utility panels, so any window opened through <code>ViewerWindowManager</code> (F3 viewer, Hex Editor, Log Viewer, Git Panel, etc.) vanished the moment the user clicked into Safari / Mail and only reappeared when Unifyl regained focus. Disabled the auto-hide so viewers behave like independent document windows (Preview-style) and stay where the user left them across app switches.</li>
                <li><b>Archive extraction is resilient to wrapper-folder name mismatches.</b> When a CJK-named ZIP is extracted, 7zz / libarchive / our central-directory parser may each pick a different legacy-encoding decoding for the wrapper folder ("新建文件夹" vs "劤쉔匡숭셸"). The previous "Extracted item not found for: …" error fired whenever the extractor's wrapper name disagreed with the archive listing's. Lookup now falls back to a recursive enumeration of the scratch directory by leaf filename, so the file is always located regardless of which decoder produced the wrapper folder name.</li>
                </ul>
                <h3>Reliability sweep (audit-driven)</h3>
                <p>These are not user-requested fixes — they came out of a project-wide audit for silent failures, data-loss patterns, and Swift 6 concurrency hazards. None changes a feature, but they close gaps where a single edge case used to corrupt persistence, hide an error, or race against itself.</p>
                <ul>
                <li><b>Archive preflight is no longer bypassed when listing fails.</b> <code>ArchiveEngine.preflightOrThrow</code> previously swallowed <code>list()</code> errors with <code>try?</code>, leaving extraction to proceed without the path-traversal or decompression-bomb guard. Listing failures now propagate so the caller never runs <code>7zz</code>/<code>ditto</code> on a refused archive.</li>
                <li><b>Saved remote connections survive schema changes.</b> <code>ConnectionViewModel</code> no longer silently drops every saved FTP/SFTP/S3/WebDAV/Azure connection when JSON decoding fails. Decode failures preserve the corrupt blob under a timestamped <code>UserDefaults</code> key and surface a one-shot <code>loadFailureMessage</code> so users can re-add their connections instead of staring at an empty list.</li>
                <li><b>Recorded macros survive schema changes.</b> Same treatment for <code>MacroRecorder</code>: corrupt store is moved to a timestamped backup before the next save can clobber it, with a user-visible message.</li>
                <li><b>Local listing surfaces real errors instead of "directory not found".</b> <code>LocalFileSystem.contents(of:)</code> used to remap every Cocoa error to <code>.notFound</code>, hiding "permission denied" (Full Disk Access not granted) and I/O failures behind misleading "folder doesn't exist" alerts. Now bridges <code>NSFileReadNoPermissionError</code> → <code>.permissionDenied</code>, real misses → <code>.notFound</code>, everything else → <code>.unknown(error)</code>.</li>
                <li><b>Files with corrupt resource values stay in the listing.</b> A single broken symlink, ACL quirk, or extended-attribute corruption used to drop that file from the panel entirely (Finder showed N files, Unifyl showed N-1 with no log). The entry now appears with default permissions instead of vanishing.</li>
                <li><b>Azure Blob connection failures now surface in the panel.</b> Bad credentials / malformed connection string set <code>panelVM.lastError</code> and log to Console instead of silently refusing to load the panel.</li>
                <li><b>Archive open failures surface in the empty-state UI.</b> Double-clicking a corrupt or unsupported archive now sets <code>lastError</code> and exposes <code>archiveOpenFailureURL</code> so the view layer can route password-shaped failures to the password sheet, instead of producing zero UI feedback.</li>
                <li><b>Cloud download trigger failures are visible.</b> <code>startDownloadingUbiquitousItem</code> was wrapped in <code>try?</code> — when the trigger failed (offline, sandbox denial, evicted item) the UI still showed "☁️ Downloading…" and a 60-second silent timeout. Failures now surface as alerts immediately and skip the watcher.</li>
                <li><b>Icon theme install failures show their reason.</b> <code>IconThemeManager.downloadAndInstall</code> reused the existing <code>downloadStatus</code> channel for the catch block instead of returning <code>false</code> with no context.</li>
                <li><b>Multi-file remote download/upload accumulates failures.</b> Per-iteration <code>ui.errorMessage</code> overwrites are gone; a partial failure surfaces a "3 of 10 uploads failed:" summary listing the offending names, matching <code>FileOperationManager.copy</code>'s queue behaviour.</li>
                <li><b>rclone launch failures log their cause.</b> <code>RCloneAvailability.configuredRemotes</code> previously returned <code>[]</code> on <code>Process.run()</code> failure, indistinguishable from "no remotes configured" — now logs at fault level so support can read the real reason from Console.</li>
                <li><b>Copy preserves timestamps loudly when it can't.</b> <code>setAttributes(modificationDate:/posixPermissions:)</code> failures during copy used to be silenced via <code>try?</code>. They're now logged at warning level so users diff'ing source/dest mtimes have an audit trail when the destination volume rejects metadata updates (network shares, exFAT).</li>
                <li><b>Process spawned by ArchiveEngine is cancellable.</b> The shell-out helper previously hopped to GCD with no cancellation hook — cancelling a long extract orphaned the spawned <code>7zz</code>/<code>ditto</code>/<code>tar</code> process and never resumed the awaiting Task. Now uses <code>Task.detached</code> + <code>withTaskCancellationHandler</code>; the cancel handler <code>terminate()</code>s the child and the body resumes with <code>CancellationError</code>.</li>
                <li><b><code>UnifylTableView</code> is <code>@MainActor</code>.</b> The NSTableView subclass's mutable timer state (<code>longPressTimer</code>, <code>isRightDragging</code>, <code>rightDragBaselineIds</code>) is now compiler-checked instead of relying on AppKit's runloop conventions.</li>
                <li><b>FileTableView debounce timers use <code>Task</code>, not <code>DispatchQueue.main.asyncAfter</code>.</b> Spring-load (1 s), type-ahead reset (750 ms), and scroll-snapshot debounce (300 ms) now use cancellable <code>Task { @MainActor in await Task.sleep ... }</code>. Cancellation is structured — <code>task.cancel()</code> wakes the sleep instead of relying on body-side stale-row checks.</li>
                <li><b><code>AppViewModel.shared</code> is <code>@MainActor</code> instead of <code>nonisolated(unsafe)</code>.</b> All call sites already hopped through <code>Task { @MainActor in ... }</code>; the explicit isolation re-engages the type system instead of letting the compiler look the other way.</li>
                <li><b>Cloud-download monitor pins its tab by UUID.</b> <code>monitorCloudDownloads</code> previously read <code>tabs[activeTabIndex]</code> per iteration — a tab switch mid-download leaked progress badges onto the wrong tab. Resolution is now via tab id with active-tab gating before reload.</li>
                <li><b>Git status load moves <code>fileExists</code> off main.</b> Slow network volumes (SMB/SFTP-mounted .git) used to block the main actor for hundreds of milliseconds during the synchronous existence check.</li>
                <li><b>Archive exit reload is structured.</b> <code>exitArchive</code>'s fire-and-forget reload Task is now stored in <code>archiveExitTask</code> and cancelled before launching, so a rapid double-toggle no longer races two <code>loadCurrentDirectory</code> calls and causes the directory to flash-then-revert.</li>
                <li><b>Static URL constants trap with diagnostic context.</b> Cloud FS adapters (Dropbox, Google Drive, OneDrive, S3, Azure) and OAuth2 configurations no longer use bare <code>URL(string: …)!</code>. The shared <code>StaticURL.make(...)</code> helper replaces them and emits a <code>preconditionFailure</code> with file:line on malformed input.</li>
                </ul>
                <h3>Design / a11y / perf sweep (audit-driven, round 2)</h3>
                <ul>
                <li><b>Design tokens grow Spacing + Motion scales.</b> <code>DesignTokens.Spacing</code> (xs/s/m/l/xl) and <code>DesignTokens.Motion</code> (micro/standard/modal) join the existing Colors / Radius / Typography. Replaces the previous "5 sheets, 3 different padding values" chaos at the token level so future sheets fall into the rhythm without effort.</li>
                <li><b>Sheet padding standardised.</b> NewFolderSheet and ArchivePasswordSheet migrated from ad-hoc <code>padding(20)</code>/<code>padding(18)</code> to <code>DesignTokens.Spacing.l</code>. Title fonts also routed through the typography scale.</li>
                <li><b>Hardcoded overlay / scrim colors moved into tokens.</b> Command palette scrim, locked-feature overlay, Quake terminal background, and progress-bar tracks were <code>Color.black.opacity(...)</code> literals scattered across views; now consolidated as <code>Colors.scrim</code>, <code>terminalBg</code>, <code>progressTrack</code> so the theme engine can reach them.</li>
                <li><b>Empty states unified onto <code>EmptyStateView</code>.</b> Replaced 9 ad-hoc <code>ContentUnavailableView</code> call sites (MetadataEditorView, ScheduledTaskView, AutomationView ×2, AuditLogView, MacroManagerView, HexEditorView, IncrementalBackupView, DuplicateFinderView ×2, PluginManagerView ×2) with the project's design-system empty state for one consistent visual rhythm.</li>
                <li><b>Reduced-motion support.</b> New <code>.accessibleAnimation(_:value:)</code> modifier consults <code>accessibilityReduceMotion</code> and disables the transition when the user has Reduce Motion enabled in System Settings. Wired into MainWindowView's command palette / terminal / file-op transitions and TabBarView's tab-count animation; remaining sites can opt in incrementally.</li>
                <li><b>Command palette is localisation-ready.</b> Command titles now go through <code>LocalizedStringKey</code> so they translate via Localizable.strings without requiring every <code>title:</code> literal at registration to wrap in <code>NSLocalizedString(...)</code>. Search placeholder and empty-state text are also localised.</li>
                <li><b>A11y labels added to icon-only buttons</b> that previously announced as generic "button" to VoiceOver — TabBar's New Tab button (<code>.help</code> is a tooltip, not a VoiceOver label) and CommandLineBar's close button. CommandLineBar close also gains a <code>.cancelAction</code> keyboard shortcut so keyboard-only users can dismiss it.</li>
                <li><b><code>PersistenceBackup</code> helper centralises corrupt-blob recovery.</b> ConnectionViewModel and MacroRecorder both copy corrupt persistence blobs aside under timestamped sidecar keys before the next save can clobber them; the pattern is now in one place (<code>stashCorruptUserDefaultsBlob</code> for defaults, <code>stashCorruptFile</code> for on-disk stores) so future persistence sites adopt the same recovery contract automatically.</li>
                <li><b>Cold-start no longer blocks on icon-theme scan.</b> <code>IconThemeManager.scanExternalThemes()</code> was synchronous in <code>init</code> and added 50–100 ms to launch on slow volumes (SMB-mounted Application Support, OneDrive-synced home). Deferred to a <code>Task.detached(priority: .utility)</code> so the first window paints with built-in themes only; external themes appear when the scan returns.</li>
                <li><b>Search filter regex memoised.</b> <code>SearchEngine.filter</code> recompiled <code>NSRegularExpression</code> on every keystroke; on a 100K-item folder the compile cost (~5–15 ms) dominated per-keystroke latency. The compiler is now cached behind an <code>NSLock</code> with a 64-entry bound, so repeated typing on the same pattern reuses the prior compile.</li>
                <li><b>Drag regression guard.</b> Reverted an experimental <code>@MainActor</code> annotation on <code>UnifylTableView</code>. The annotation was meant to compiler-check what AppKit's runloop conventions already guarantee, but it perturbed NSTableView's mouseDown → drag-threshold dispatch in some configurations and silently broke drag initiation. Methods all run on main via AppKit anyway; explicit isolation here costs more than it pays.</li>
                </ul>
            ]]></description>
            <pubDate>Thu, 30 Apr 2026 07:58:37 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.1.0/Unifyl-1.1.0.dmg"
                sparkle:version="14"
                sparkle:shortVersionString="1.1.0"
                sparkle:edSignature="JdqZ3zbpvOgen+EDADMDQlMPvud+/5mi4KIxleJJGNs/rxpirmaFbT39dXZfr8O37rCYgqD3Sj7DOhUS3JpYDQ=="
                length="14894241"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.9</title>
            <description><![CDATA[
<h2>Unifyl 1.0.9</h2>
<blockquote><p><b>1.0.8 was cut internally but never published.</b> This release consolidates the 1.0.8 design audit + 10 power-user features + 20+ Settings tunables with the subsequent Log Viewer overhaul, Settings resize fix, Theme Editor preset sidebar, and Compress dialog rework. Users upgrade 1.0.7 → 1.0.9 directly.</p></blockquote>
<h3>Added — Power-user features</h3>
<ul>
<li><b>Type-ahead select</b>: type any sequence to jump to the first matching file. Korean input is choseong-normalised so "민수.txt" matches "ㅁ".</li>
<li><b>Spring-loaded folders</b>: drag a file over a directory row for 1 s to auto-navigate into it. Works on <code>..</code> for parent navigation.</li>
<li><b>Tab right-click context menu</b>: Rename / Pin / Duplicate / Move to Other Panel / Close Others / Close Tabs to the Right / Reveal in Finder.</li>
<li><b>Breadcrumb click-to-edit</b>: double-click the path bar for a raw-text URL editor.</li>
<li><b>Dock icon context menu</b>: Reveal Active Folder + Recent Folders (top 5 by frecency).</li>
<li><b>"Send to Unifyl" macOS Service</b>: right-click files in Finder / Mail / Safari → Services → "Send to Unifyl".</li>
<li><b>Copy Path As…</b> submenu under Edit — POSIX / <code>file://</code> URI / shell-quoted / unix-shell / relative / filename only.</li>
<li><b>Reveal cursor file in opposite panel</b> (⌃⌥O) — prep before compare / sync / diff.</li>
<li><b>Cmd+1..9 tab jump</b> — physical keyCode mapping, works under any IME.</li>
<li><b>F7 New Folder sheet</b> with pre-selected placeholder text — typing immediately replaces "New Folder" without needing ⌘A.</li>
<li><b>Log Viewer grep/find toggle</b>: grep hides non-matching rows, find highlights in place. Plus <b>Load All</b> button for the rare 100k+ line case.</li>
<li><b>Theme Editor Built-in Presets sidebar</b>: browse all 12 presets, right-click → <b>Duplicate as Custom Theme</b>.</li>
<li><b>Active-panel focus desaturation</b>: inactive panel at 50 % saturation + accent stripe atop the active PathBar.</li>
</ul>
<h3>Changed — Design (senior macOS designer audit)</h3>
<ul>
<li><b>Theme presets now theme the full app chrome</b> (PathBar / TabBar / StatusBar / OperationQueueBar / FunctionKeyBar). Previously frozen to Unifyl Dark Pro regardless of active preset.</li>
<li><b>Per-row file-type colour flood removed</b> — filename + icon still tint by category, Size / Date / Kind columns now use the theme's secondary label.</li>
<li><b>F-key bar off by default.</b> Re-enable via View ▸ Show Function Key Bar.</li>
<li><b>PathBar hierarchy restored</b>; Toolbar status simplified; toasts moved into the panel status bar.</li>
<li><b>AppTheme schema expanded</b> with 10 new chrome + semantic tokens (<code>elevatedHex</code>, <code>overlayHex</code>, <code>borderSubtleHex</code>, <code>borderDefaultHex</code>, <code>textMutedHex</code>, <code>errorHex</code>, <code>warningHex</code>, <code>successHex</code>, <code>dropTargetHex</code>, <code>focusRingHex</code>).</li>
<li><b>Preset curation</b>: 13 → 12, contrast fixes on High Contrast (was invisible white-on-white selection), Solarized Light, One Dark, Dracula.</li>
</ul>
<h3>Added — Settings (General pane)</h3>
<ul>
<li>Size / Date / Double-click action pickers.</li>
<li>Row font size + Spring-loaded folder delay + Type-ahead reset + Toast dismiss sliders.</li>
<li>Show/hide toggles for every chrome element and confirmation toggles for Copy / Move / Delete / Permanent Delete / Overwrite.</li>
<li>Every new setting reflects immediately — no restart required.</li>
</ul>
<h3>Changed</h3>
<ul>
<li><b>"Compress to ZIP…" → "Compress…"</b>: dialog-based (destination + filename + format), replaces the previous silent-write.</li>
<li><b>Log Viewer performance overhaul</b>: UUID → Int line-number ids, level classification at ingest, debounced filter, 5 000-line tail cap.</li>
<li><b>Settings window is resizable</b> (was pinned at 550 × 480 despite 20+ new tunables).</li>
</ul>
<h3>Fixed</h3>
<ul>
<li><b>F2 rename Cmd+A/C/V/X/Z under Korean / Japanese / Chinese IME</b> — all dispatch now uses <code>event.keyCode</code>.</li>
<li><b>Rename-onto-existing-file</b> now asks Overwrite? (was hard-failing with "already exists").</li>
<li><b>Theme Editor</b> no longer opens multiple windows; no longer duplicates presets in the sidebar.</li>
<li><b>Mouse wheel scroll</b> no longer reverts to cursor position after a brief delay.</li>
<li><b>Toast auto-dismiss audit</b> — "Copied N paths", undo, Smart Folder add, PDF convert failure now all self-clear.</li>
<li><b>Esc in Log Viewer no longer terminates the app</b>.</li>
</ul>
<p><a href="https://github.com/goodbug89/Unifyl.app/blob/main/CHANGELOG.md">Full changelog</a></p>
]]></description>
            <pubDate>Sun, 26 Apr 2026 08:00:00 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.9/Unifyl-1.0.9.dmg"
                sparkle:version="13"
                sparkle:shortVersionString="1.0.9"
                sparkle:edSignature="/ePAnxbvtECgXuYHfCFJljeHsHFZhsRTWWVWIDQwj8qhUNrIJFCsmq0Ku2A/hFMHsj8NbpuoDUzgk+psAa7XDQ=="
                length="14782711"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.7</title>
            <description><![CDATA[
<h2>Unifyl 1.0.7</h2>
<h3>Added</h3>
<ul>
<li><b>Total Commander-style right-drag selection</b>: hold right mouse button and drag to mark files. Cursor tracks the pointer, sequential drags <b>accumulate</b> onto existing marks instead of replacing. Drags starting on an already-marked file subtract its range from the baseline.</li>
<li><b>Shift / Cmd / Ctrl + click</b> file-list selection modifiers. Shift-click range-marks from the cursor to the clicked row; Cmd-click or Ctrl-click toggles just the clicked row without disturbing existing marks.</li>
<li><b>Bundled 7zz binary</b> (universal Mach-O, LGPL-2.1). Password-protected / WinZip AES / LZMA / PKZIP 5.1+ archives open out of the box — no <code>brew install p7zip</code> required.</li>
<li><b>Password-protected archive extraction</b>: new SecureField prompt for encrypted zip / 7z / RAR, with automatic retry on wrong password. Works for whole-archive F8 and inside-archive F5 / F6 on selected entries.</li>
<li><b>Wrapper-folder unwrap</b> after archive extraction. Zips that package a file inside a same-named folder (HWP / Numbers / Keynote / Pages / Sparkle .xcframework) now extract as a single file at the destination. macOS bookkeeping siblings (<code>__MACOSX</code>, <code>.DS_Store</code>, <code>._*</code>) are discarded silently; real user siblings prevent unwrap.</li>
<li><b>HWPX → PDF Hancom / Polaris passthrough</b>: when LibreOffice's conversion fails, Unifyl shows an <b>Open in &lt;app name&gt;</b> button if Hancom Office, Hancom Viewer, or Polaris Office v9+ is installed. Button opens the HWPX file directly; otherwise routes to hancom.com download.</li>
<li><b>Drop external files onto an archive row</b> to add them into the archive at its root. The archive row highlights during the drag; ZIP / 7z / TAR families are accepted targets.</li>
<li><b>Recent Folders</b> menu under <code>Go</code>: frecency-ranked list (visit count × 7-day half-life) of your last 40 directories. Dead paths (ejected volume / deleted folder) filtered before display.</li>
<li><b>AI Semantic Search onboarding</b>: first-time users who invoke AI search while the index is empty see a focused pitch card with <b>Index Home Folder</b> + <b>Choose Folder…</b> + <b>Skip for now</b>. Dismissal remembered in UserDefaults.</li>
<li><b>Live keyboard-shortcut sync</b>: remapping any of the 120+ shortcuts in Settings updates the macOS menu-bar indicator immediately — no quit-and-relaunch.</li>
<li><b>Theme Editor</b> is now an independent, resizable window (via ViewerWindowManager, not a modal sheet). New <b>Apply as Active Theme</b> button + <b>Live Apply</b> toggle for real-time preview against actual file panels.</li>
<li><b>Cursor color</b> is now a distinct theme token. Single-row focus is drawn with a 3-px left-edge stripe matching Total Commander.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li><b>Inside-archive F5 / F6 routing</b>: used to silently fail because synthetic archive URLs were handed to the regular file-copy path. Now routes through ArchiveEngine extraction.</li>
<li><b>Destination path duplication</b>: single-item archive extraction used to produce <code>destination/X.hwp/X.hwp</code> wrapper folders.</li>
<li><b>unwrapWrapperFolder safety</b>: no longer destroys real user siblings; only collapses when the only children are macOS bookkeeping.</li>
<li><b>Move-from-archive</b> now enumerates the full entry list instead of using unsupported glob patterns that silently left children inside the archive.</li>
<li><b>Theme Editor Apply</b> now actually activates the edited theme (previously silent no-op because <code>ThemeManager</code> only knew the built-in presets).</li>
</ul>
<p><a href="https://github.com/goodbug89/Unifyl.app/blob/main/CHANGELOG.md">Full changelog</a></p>
]]></description>
            <pubDate>Sun, 19 Apr 2026 20:15:00 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.7/Unifyl-1.0.7.dmg"
                sparkle:version="12"
                sparkle:shortVersionString="1.0.7"
                sparkle:edSignature="e/oIoDshvLkEMwG+tL9c0vm4vNAZLrqmwFeXXB1AORAXbsfdf3Ngr0nnVoArz8ABazYpsVyAqxuAI8cpPrzDDA=="
                length="14590025"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.6</title>
            <description><![CDATA[
<h2>Unifyl 1.0.6</h2>
<h3>Added</h3>
<ul>
<li><b>Theme Editor is now an independent, resizable window</b>: movable via the macOS title bar instead of a modal sheet. New <b>Apply as Active Theme</b> button and <b>Live Apply</b> toggle for real-time preview against actual file panels.</li>
<li><b>Cursor color</b> is now a distinct theme token. Single-row focus is drawn with a 3-px left-edge stripe matching Total Commander. Customize in Theme Editor → Chrome Colors → Cursor.</li>
<li><b>Right-drag selection</b> (Total Commander style): hold the right mouse button and drag to mark-select. The cursor tracks the pointer, and sequential drags <b>accumulate</b> onto existing marks instead of replacing them. Drag starting on an already-marked file subtracts its range instead.</li>
<li><b>Live keyboard-shortcut sync</b>: remapping any of the 120+ shortcuts in Settings updates the macOS menu-bar indicator immediately — no quit-and-relaunch required.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li><b>Theme Editor Apply did nothing</b>: <code>ThemeManager</code> didn't know how to look up custom-theme IDs, so activating an edited custom theme silently fell through to the default. Fixed — Apply now actually paints the chrome.</li>
<li><b>Theme Editor crash</b> on opening as independent window: environment objects are re-injected at the panel's entry point so <code>@Environment(ThemeManager.self)</code> resolves.</li>
<li><b>Pre-1.0.6 <code>.ultratheme</code> imports</b> decode cleanly via a backward-compatible parser for the legacy 4-field schema.</li>
</ul>
<p><a href="https://github.com/goodbug89/Unifyl.app/blob/main/CHANGELOG.md">Full changelog</a></p>
]]></description>
            <pubDate>Sun, 19 Apr 2026 13:46:00 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.6/Unifyl-1.0.6.dmg"
                sparkle:version="10"
                sparkle:shortVersionString="1.0.6"
                sparkle:edSignature="Fnqy0gINXKsik7FyobeDZF19pv1NzBTjGytSeG72bPFXpQqGSXyQ5DxaJw5S9QoZDtcm4ZaP0iw2pFMW5hBQDw=="
                length="11961068"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.5</title>
            <description><![CDATA[
<h2>Unifyl 1.0.5</h2>
<h3>Fixed</h3>
<ul>
<li><b>Live LemonSqueezy checkout</b>: builds up through 1.0.4 shipped with LemonSqueezy Test-store credentials, so the checkout page showed "Test mode is currently enabled" and real cards could not complete a purchase. This release is wired to the Live store — purchases now process normally. Users who attempted to buy on 1.0.4 saw no charge; retry on 1.0.5 and the order will go through.</li>
</ul>
<h3>Changed</h3>
<ul>
<li><b>Tab bar / path bar chrome unified</b>: both rows now share a single background and 28 px height, removing the visible seam between them and keeping the archive path bar + status bar in the same palette.</li>
<li><b>Theme tokens applied to chrome</b>: panel background, folder-tree background, active-panel edge indicator, toolbar status badge, command-line output pane, and the onboarding CTA now follow the selected theme (accent / background / surface) instead of raw AppKit semantic colors.</li>
</ul>
<h3>Added</h3>
<ul>
<li><b>EmptyStateView component</b>: one reusable empty-state presentation (icon + title + optional message + optional CTA) now used by empty folders, the Smart Folder picker before a preset is chosen, the advanced search with no matches, and three Git-panel states (not a repo, working tree clean, no diff selected).</li>
</ul>
<p><a href="https://github.com/goodbug89/Unifyl.app/blob/main/CHANGELOG.md">Full changelog</a></p>
]]></description>
            <pubDate>Sat, 18 Apr 2026 19:15:00 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.5/Unifyl-1.0.5.dmg"
                sparkle:version="9"
                sparkle:shortVersionString="1.0.5"
                sparkle:edSignature="NWBKu4QO/64XFtLkxLuy969jkfXIR/FqahIlbJBHh+Sf2B7YGJ0bp+sva+0HCR7Z2ODOJqPNuNM0coGyREACCw=="
                length="11901919"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.4</title>
            <description><![CDATA[
<h2>Unifyl 1.0.4</h2>
<h3>Fixed</h3>
<ul>
<li><b>Mirror Active Panel on Other Side</b> (Volume picker menu) and the "copy path to opposite panel" shortcut silently did nothing when the destination panel was inside a Smart Folder or inside an archive (zip/7z/…). <code>PanelViewModel.navigate</code> bails early in those modes, so the mirror looked broken. New <code>navigateForce(to:)</code> exits the special mode first, then navigates.</li>
<li><b>Back / Forward arrows</b> in Smart Folder and inside an archive used to do nothing silently. They now leave the special mode (Smart Folder → real directory listing, archive → parent folder) so the click is never ignored.</li>
<li><b>Cmd+R Refresh</b> in Smart Folder rebuilds the cart's listing; in an archive it re-enters the archive. Previously it was a silent no-op in both cases, which read as "the app is frozen".</li>
<li><b>F3 (Preview) and F4 (Edit)</b> now fall back to the cursor item when nothing is marked, matching the project-wide <code>selectedOrCursorItems</code> rule. Previously they were silent no-ops if the user hadn't toggled a selection.</li>
<li><b>AI Smart Tag / Classification apply</b>: tag assignment to a file's <code>NSURLTagNamesKey</code> used <code>try?</code>, so a sandbox / permission / disk error was invisible — the UI said "Applied" while the tag never actually hit disk. Failures are now logged via <code>UnifylLogger.ai</code> and surfaced on <code>AIEngineViewModel.aiError</code> so the AI view can alert the user.</li>
<li><b>AI summary report export</b>: <code>exportSummaryReport</code> wrote the file with <code>try?</code> and exited silently on failure (permissions, disk full, bad format). Now logs via <code>UnifylLogger.ai</code> and sets <code>aiError</code> so the user sees why the file isn't there.</li>
<li><b>Archive entry removal (tar family)</b>: <code>ArchiveEngine.removeFromArchive</code> ignored individual file removal failures inside the tar staging dir with <code>try?</code>, which could leave a partially-modified tar on disk and silently lose the "rollback" signal. Failures now accumulate and throw so the overall operation fails loudly.</li>
<li><b>VectorIndex schema migration</b>: <code>ALTER TABLE ADD COLUMN</code> was wrapped in <code>try?</code>, so a failed migration silently left the on-disk index incompatible with the running binary and AI features depending on the new column just disappeared. Failures now log at <code>Logger.fault</code> level.</li>
<li><b>LLM model update check</b>: manifest fetch + decode were both <code>try?</code>, making every failure mode indistinguishable from "no update available". Both now log specifically (fetch vs decode) at <code>.error</code> level.</li>
<li><b>Automation manager — load</b>: saved automation rules were lost silently via <code>try?</code> on JSON decode when the schema changed between versions. Load now distinguishes "no file yet" (first launch) from "decode failed" (corrupted / schema drift) and logs the latter.</li>
<li><b>Script library — built-in install</b>: first-launch copy of bundled automation scripts used <code>try?</code>, so a permission / disk error showed up as an empty "Automations" list with no diagnostic trail. Directory creation and per-file copy failures now log via <code>UnifylLogger.automation</code>.</li>
<li><b>Per-sheet state leakage</b> across reopens: Multi-Rename (rules, AI suggestions), Regex Rename (pattern, replacement, flags, preset), AI Rename (suggestions, chosen names), and Advanced Search (results, "Select All" toggle, status message) now reset their <code>@State</code> in <code>.onAppear</code> so a second open doesn't ghost values from the previous session.</li>
<li><b>Duplicate Detector — hardlink data-loss guard</b>: hardlinks to the same file (same <code>(st_dev, st_ino)</code>) were being grouped as "duplicates". When the user bulk-deleted "duplicates" it unlinked the inode's only remaining name, destroying the underlying file. Duplicate grouping now dedupes by inode first, so a hardlinked file appears as a single entry the user can't accidentally wipe.</li>
<li><b>AudioPlayer timer leak</b>: <code>Timer.scheduledTimer</code> kept firing forever if the view was dismissed without <code>.onDisappear</code> (e.g. rapid sheet open/close). Replaced with a <code>DispatchSourceTimer</code> that a <code>nonisolated deinit</code> can safely cancel.</li>
<li><b>Large File Finder / Advanced Search — scan-on-dismiss crash</b>: both views spawned untracked <code>Task.detached</code> scans that continued after the user dismissed the sheet and then wrote to released <code>@State</code>, tripping a "main actor-isolated state mutation" trap. Scan tasks are now tracked in <code>@State</code> and cancelled in <code>.onDisappear</code>.</li>
<li><b>Circular symlink traversal</b>: <code>FileOperationManager.enumerateAllFiles</code> now tracks <code>(st_dev, st_ino)</code> for every directory it descends into, so a cycle like <code>a/link_to_b → b</code>, <code>b/link_to_a → a</code> terminates instead of hanging the copy.</li>
<li><b>Zip-slip / Windows-style path traversal pre-check</b>: <code>ArchiveEngine.extract(…)</code> now lists archive entries and rejects the archive *before* running <code>ditto</code>/<code>tar</code>/<code>7z</code>. Previous code only verified after extraction, by which time the malicious file had already touched disk. <code>isSafeArchivePath</code> also now normalises <code>\</code> → <code>/</code>, rejects UNC prefixes, and rejects Windows drive specs (<code>C:\foo</code>).</li>
<li><b>Decompression bomb guard</b>: archives that claim <code>>50×</code> and <code>>2 GiB</code> of uncompressed content are rejected before extraction with an actionable error message (extract manually if source is trusted). Prevents a 10 MB zip from filling a 500 GB disk silently.</li>
<li><b>AppleScript injection via malicious filename</b>: <code>PDFConvertSheet</code> interpolated source/destination paths into AppleScript after only escaping <code>"</code>. A filename containing <code>\"</code> broke out of the string context and let an attacker execute arbitrary AppleScript on iWork / MS Office apps just by shipping the file. Full escape sequence (<code>\</code>, <code>"</code>, <code>\n</code>, <code>\r</code>) now applied, in that order.</li>
<li><b>Regex ReDoS in Advanced Search</b>: user-supplied regex against arbitrary filenames could hang the whole scan on patterns like <code>(a+)+b</code>. Matching now goes through <code>NSRegularExpression</code> (ICU-based, bounded backtracking) with a 4 KB filename cap. An invalid pattern returns "no match" instead of crashing the scan.</li>
<li><b>OneDrive <code>idCache</code> staleness</b>: renaming / moving / deleting a file / folder via Unifyl used to leave the cached <code>path → graphID</code> mapping pointing at the old location. A subsequent operation on the old path would then hit a wrong (or deleted) OneDrive item. Each mutating operation now invalidates the cache entry for the affected URL AND every descendant, so a stale entry can't silently resolve the wrong file.</li>
<li><b>RemotePollingWatcher — silent disconnection</b>: after 5 consecutive list failures the watcher used to just stop, leaving the panel displaying the last cached listing as if the server were live. It now emits a new <code>DirectoryChange.connectionLost(reason:)</code> and <code>PanelViewModel</code> stores the reason on <code>remoteConnectionError</code> so the panel header can show a "disconnected" banner.</li>
<li><b>Case-insensitive filesystem rename</b> (<code>Photo.jpg</code> → <code>photo.jpg</code>): the collision resolver saw the existing lowercase inode at the target path and appended <code>_1</code>, producing <code>photo_1.jpg</code> instead of the intended case change. An <code>(st_dev, st_ino)</code> self-collision check now lets the rename proceed through the 2-step staging that already handles case-only renames.</li>
<li><b>cloudDownloadTask panel swap</b>: the 60-second cloud-download watcher stored <code>self.activePanelVM</code> (computed property), so if the user switched panel focus during the download the wrong panel's cloud status got refreshed on completion. Task now captures the originating panel as a weak reference at start, so refresh targets the correct panel (or is silently skipped if that panel was torn down).</li>
<li><code>ViewerWindowManager</code>: panel <code>willClose</code> cleanup now also drops the NSHostingView to release SwiftUI observation captures immediately instead of via the autorelease pool.</li>
<li><b>Undo move / copy with rename-on-conflict</b>: when a conflict resolution renamed the destination to e.g. <code>file_1.txt</code>, undo used to look for the original name <code>file.txt</code> in the destination and silently do nothing. <code>FileOperationHistory.Record</code> now carries the actual per-source destination URLs; undo uses them so the rename-suffixed file is actually reversed.</li>
<li><b>Undo move no longer clobbers newly-created files</b> at the original source path. If a newer file lives there, the restored file is placed alongside with a <code>.unifyl-undo</code> suffix so the user can resolve the collision instead of silently losing data.</li>
<li><b>Undo delete searches the correct Trash</b> on each source's volume (via <code>FileManager.url(for:.trashDirectory, appropriateFor:)</code>) instead of only the user's home <code>~/.Trash</code>. External-drive deletions are now actually undoable.</li>
<li><b>Workspace JSON schema evolution safety</b>: every <code>SavedWorkspace</code> field is now optional with sensible defaults (<code>nil</code> → home directory, "left" active panel, etc.). A new required field in a future app version no longer throws at decode time and wipes the user's entire tab/panel layout. Added a <code>schemaVersion</code> field to make future migrations explicit. Tab restoration also skips paths that no longer exist so deleted folders don't ghost-restore as dead tabs.</li>
<li><b>KeyBindingManager — orphan custom shortcuts preserved</b>: when an app upgrade renames or removes a command ID, the user's saved custom binding used to vanish silently. The manager now keeps the mapping in <code>orphanedCustomizations</code> and logs the count so the Settings UI can offer "re-assign or discard" instead of dropping it.</li>
<li><b>MetadataEditor batch failure — per-file reasons</b>: the EXIF editor used to report "Applied to N, M error(s)" with no way to know WHICH files failed or why. Each failure now carries a name + reason (<code>metadataFailures</code>), surfaced in the status line.</li>
<li><b>PanelViewModel tab-close race</b>: <code>loadCurrentDirectory</code> used to capture <code>activeTabIndex</code> as an integer, so closing a tab during an async load could write the results into a *different* tab. Now captures the tab's <code>UUID</code> and re-resolves the target index after every await suspension, bailing cleanly if the tab was closed.</li>
<li><b>SSH tunnel / Docker container status</b> state indicators now layer a small SF Symbol (<code>checkmark</code> / <code>xmark</code> / <code>play.fill</code> / <code>pause.fill</code>) on top of the green/red/orange dot, with an accessibilityLabel ("Running" / "Stopped"). Colour-blind and low-vision users can now tell the states apart.</li>
<li><b>SSH Tunnel Manager close button</b> now has <code>.help("Close")</code> + <code>.accessibilityLabel("Close SSH Tunnel Manager")</code> so VoiceOver announces its purpose.</li>
<li><b>Archive extract confirmation OK</b> now has <code>.keyboardShortcut(.defaultAction)</code> so Return key confirms — previously keyboard-only users had to reach for the mouse.</li>
<li><b>Quake terminal decorative rule</b> marked <code>.accessibilityHidden(true)</code> so VoiceOver no longer announces the visual separator.</li>
<li><b>Hex editor / code viewer</b> NSViewRepresentable wrappers now set an accessibilityLabel + role so VoiceOver announces "Hex editor" / "Code viewer (\<ext\>)" instead of a bare "text area".</li>
<li><b>HTML / SVG preview</b> WKWebView wrappers set <code>accessibilityLabel</code> including the filename so VoiceOver has a container name for the web content.</li>
<li><b>SecurityBookmarkManager</b>: unresolvable bookmarks (volume unmounted, folder deleted) are now logged via <code>UnifylLogger.settings</code> instead of silently dropped, so "my bookmarks disappeared" is diagnosable from Console.app.</li>
</ul>

<h3>Security</h3>
<ul>
<li><b>Markdown preview XSS</b>: <code>MarkdownPreviewView</code> loaded user markdown-as-HTML via <code>WKWebView.loadHTMLString</code> without sanitisation. A shared <code>.md</code> file containing <code>[click](javascript:fetch('…'+document.cookie))</code> or inline <code><img src=x onerror="…"></code> could execute arbitrary JavaScript in the app's WKWebView context. The renderer now strips <code>javascript:</code>/<code>vbscript:</code>/<code>data:text/html</code>/<code>file:</code> link schemes, inline <code>on*=</code> event handlers, and <code><script></code> blocks. The rendered HTML is also wrapped in a restrictive Content-Security-Policy (<code>default-src 'none'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; font-src 'self' data:</code>) and loaded with <code>baseURL = nil</code> so relative <code>file://</code> paths can't resolve against the markdown's own directory.</li>
<li><b>License trial clock-rollback</b>: <code>LicenseManager.isWithinGracePeriod</code> used <code>Date()</code> directly. A user could extend a trial by moving the system clock backward. Added <code>ratchetedNow()</code> that persists the highest wall-clock ever observed to Keychain (<code>unifyl.license.maxObservedDate</code>) and refuses to advance if the wall clock reports a time more than 5 minutes before the stored ratchet — effectively freezing "now" in the past for license checks, which causes the trial to expire promptly on rollback.</li>
</ul>

<h3>i18n / locale</h3>
<ul>
<li><b>CJK dictionary-order sort</b> (Korean 한글, Japanese 仮名/漢字, Chinese 汉字): <code>ProcessFileMapView</code> and similar lists used <code>.sorted { $0.lowercased() < $1.lowercased() }</code> which sorts by Unicode codepoint rather than locale-aware dictionary order. Switched to <code>localizedStandardCompare</code> so CJK names sort as users expect and embedded numbers sort numerically (<code>file2</code> before <code>file10</code>).</li>
<li><b>Turkish / Greek case folding</b>: <code>KeyBindingSettingsView</code>, <code>SubtitlePreviewView</code>, and <code>LogViewerView</code> filters used <code>.lowercased().contains()</code>, which mis-folds Turkish "İ"/"i" and Greek σ/ς. Replaced with <code>localizedCaseInsensitiveContains</code> throughout.</li>
<li><b>CompanionSettings port input</b>: <code>TextField(..., formatter: NumberFormatter())</code> picked up the system locale, so Thai / Arabic / Bengali users saw port numbers rendered in native digits. Pinned to POSIX locale since port numbers are ASCII by network convention.</li>
<li><b>CommandPalette NFC / NFD mismatch</b>: search normalises both title and query with <code>precomposedStringWithCanonicalMapping</code> so a Korean query typed composed (<code>한</code>) matches titles stored decomposed (<code>ㅎㅏㄴ</code>).</li>
<li><b>File-copy speed readout locale</b>: <code>FileOperationProgress.speedString</code> used <code>String(format: "%.1f")</code> which hardcodes <code>.</code> as decimal separator. German/French users now correctly see "1,5 GB/s" via a shared <code>NumberFormatter</code> keyed to <code>.autoupdatingCurrent</code>.</li>
</ul>

<h3>UX</h3>
<ul>
<li><b>Inline Terminal / CommandLineBar PATH</b>: <code>process.arguments = ["-c", command]</code> ran a non-login, non-interactive zsh. Homebrew / nvm / pyenv / rbenv paths set up in <code>~/.zprofile</code> were invisible, so commands like <code>brew</code>, <code>node</code>, <code>python3</code> produced "command not found". Now uses <code>-l</code> (login shell) so the profile is loaded.</li>
<li><b>Password-protected PDF preview</b>: <code>FilePreviewView.pdfPreview</code> used to render a blank pane for encrypted PDFs with no indication of why. Now detects <code>PDFDocument.isEncrypted && .isLocked</code> and shows an actionable "Password-Protected PDF" placeholder with an "Open in Preview" button.</li>
<li><b>Text preview size cap</b>: <code>String(contentsOf:)</code> used to load the entire file into memory, freezing the UI for seconds on a multi-GB log file and potentially OOMing. Now reads at most 5 MB via a bounded <code>FileHandle.read(upToCount:)</code> and shows a "Large file — showing first 5 MB of X MB" banner + "Open in TextEdit" button.</li>
<li><b>Text preview encoding detection</b>: previous <code>try? utf8 ?? try? ascii</code> missed UTF-16 LE/BE, UTF-32, and Windows-1252. Added BOM sniffing (UTF-8 / UTF-16 LE / UTF-16 BE / UTF-32 LE / UTF-32 BE) + fallback to <code>windowsCP1252</code> before ASCII, so Windows-generated text files decode correctly instead of showing "Cannot read file".</li>
<li><b>Git status parser robustness</b>: <code>git status --porcelain</code> output was parsed by newline-split + <code>dropFirst(3)</code>, which broke on filenames containing spaces (git C-quotes the whole path) or newlines (multi-entry split). Switched to <code>git status --porcelain -z</code> (NUL-separated, unquoted paths) and split on <code>\0</code>.</li>
<li><b>Drag-and-drop modifier race</b> (copy vs move): <code>ThumbnailGridView</code> and <code>FileTableView</code> independently read <code>NSApp.currentEvent?.modifierFlags</code> in both <code>validateDrop</code> and <code>acceptDrop</code>. A user releasing Option between the final validate and the drop produced inconsistent behaviour (validation said copy, acceptance said move). Both coordinators now snapshot the modifier in <code>validateDrop</code> and honour it in <code>acceptDrop</code>.</li>
<li><b>Search regex fallback</b>: <code>SearchEngine.filter</code> silently returned the unfiltered list when the user's regex pattern was malformed (e.g. unbalanced brackets). Now falls back to a literal substring search of the pattern so the filter still does *something intuitive* instead of looking broken.</li>
<li><b>File-size column right-aligned</b>: the Size column in the file table now right-aligns cell text AND header — numeric data reads better flush-right (Finder convention).</li>
</ul>

<h3>Added</h3>
<ul>
<li><b>Per-row size gauge for files</b>: file rows now render the same proportional background bar that folders already had, so the user can scan a directory and spot the largest files visually. Files are normalised against the max file size (green tint, <code>.systemGreen 0.10</code>), folders stay on their own scale (blue tint, <code>.systemBlue 0.12</code>), so a multi-GB folder can't flatten every file bar to invisible. Computed eagerly at listing time — no async cost since regular-file sizes are already populated.</li>
<li><b>Native PRO badge in the main menu bar</b>: PRO-only menu items now show macOS's native <code>NSMenuItemBadge</code> pill (the small rounded-rect shipped in macOS 14's menu redesign) on the trailing edge of the item so Free users can see at a glance which actions require an upgrade. Uses AppKit's own pill rendering rather than a text suffix, so it reads as a system badge — not a hack — and stays visually light. The badge is set/cleared by a <code>ProBadgeMap</code> walker that runs on app launch (after SwiftUI finishes building the menus), after licence validation, and on <code>didBecomeActiveNotification</code> (to catch re-built menus after background). Pro / Trial / Team tiers see no badge — it's cleared the moment the tier elevates.</li>
<li><b>Theme presets now paint the full surface</b> (background, alt-row, selection, text), not just the accent colour. Previously switching between "Dracula" / "Nord" / "One Dark" only tinted a few SwiftUI controls (and the HSplitView divider) because <code>AppTheme</code> only carried <code>scheme</code> + <code>accentHex</code>. Each preset now declares six colour tokens (<code>background</code>, <code>surface</code>, <code>selection</code>, <code>altRow</code>, <code>textPrimary</code>, <code>textSecondary</code>) with canonical hex values from each palette's project page. <code>ThemeManager</code> exposes both SwiftUI <code>Color?</code> and AppKit <code>NSColor?</code> accessors (<code>nil</code> = fall back to system defaults so the <code>System Default</code> preset stays indistinguishable from vanilla AppKit). The main window paints <code>background</code>, and <code>FileTableView</code> uses a new <code>ThemedTableRowView</code> to paint <code>altRow</code> and <code>selection</code> via <code>drawBackground</code>/<code>drawSelection</code> overrides. Switching Dracula ↔ Nord ↔ Solarized Dark ↔ GitHub Light now produces a visibly distinct look, not just an accent swap.</li>
</ul>

<h3>Performance</h3>
<ul>
<li><b>File copy progress</b>: <code>FileManager.default.attributesOfItem(atPath:)</code> was called on MainActor after each file copy — on iCloud / network volumes this blocked the UI for 100+ ms per file. Moved to a detached Task so the progress UI stays responsive.</li>
<li><b>PanelView total-size badge</b>: the Smart Folder path bar's total size used <code>items.compactMap(\.size).reduce(0, +)</code> without filtering the <code>..</code> parent entry and without <code>.lazy</code>. Now uses <code>items.lazy.filter { $0.name != ".." }.compactMap(\.size).reduce(0, +)</code> matching the neighbouring code and avoiding intermediate arrays on every body evaluation.</li>
<li><b>LargeFileFinder formatter allocation</b>: <code>ByteCountFormatter</code> was allocated per row; replaced with a static shared instance.</li>
<li><b>FolderReportView stats</b>: <code>totalFiles</code> / <code>totalDirs</code> / <code>totalSize</code> / <code>typeBreakdown</code> were computed properties that re-scanned <code>reportEntries</code> on every body render (O(n) × 3 + O(n log n) sort). Computed once off-main when the scan finishes, cached into a <code>ReportStats</code> struct.</li>
</ul>

<h3>Changed</h3>
<ul>
<li><b>Keyboard — Copy Path to Opposite Panel</b>: default shortcut moved from <b>Ctrl+← / Ctrl+→</b> to <b>Opt+← / Opt+→</b>. Ctrl+←/→ is reserved by macOS Mission Control (*"move a Space"*), so the app never saw the event on machines with multiple Desktops. Opt+←/→ is safe outside text fields (text-responder guard is already in place).</li>
<li>Toolbar status center: removed the outer <code>Capsule</code> background that overlapped macOS's native toolbar item container, producing a visible "two ovals" artefact around the <code>Unifyl v1.0.3 FREE</code> badge. The inner FREE/PRO capsule is unchanged.</li>
<li>Function-key bar Terminal toggle: replaced the rounded teal pill (whose corners got clipped by the surrounding bar chrome) with a full-height rectangular cell that matches the F2–F8 cells. Solid teal when the bottom terminal is open, <code>teal.opacity(0.18)</code> when closed.</li>
</ul>

<h3>Reliability</h3>
<ul>
<li><code>IconThemeManager.externalThemesDirectory</code> / <code>directory(for:)</code>: replaced the <code>urls(for:…).first!</code> force-unwrap with a safe <code>?? ~/Library/Application Support</code> fallback. Fixes a theoretical-but-real crash path on sandbox-container corner cases.</li>
<li><code>RemoteConnection.baseURL</code>: replaced the <code>URL(string: "<scheme>:///")!</code> fallbacks with a <code>schemeFallback</code> helper that never crashes, even when <code>URLComponents.url</code> is nil and the fallback itself fails to parse.</li>
</ul>

<h3>Build</h3>
<ul>
<li>Styled DMG generation moved off <code>create-dmg.sh</code>'s Finder AppleScript (which hangs in non-GUI shells) onto <code>dmgbuild</code> + a pre-made <code>.DS_Store</code> via <code>scripts/dmg_settings.py</code>. New <code>resources/dmg-background.png</code> (600×400 soft gradient with drag-hint arrow).</li>
<li><code>release.sh</code> now notarises + staples the <code>.app</code> bundle itself before DMG creation so offline first-launch Gatekeeper works without round-tripping to Apple.</li>
<li>New <code>scripts/check-docs-sync.sh</code> (wired into <code>make lint</code>) scans user-facing docs and the in-app help HTML for references to the retired <b>Ctrl+←/→</b> shortcut so future contributors don't silently reintroduce the Mission Control conflict.</li>
</ul>
<p><a href="https://github.com/goodbug89/Unifyl.app/blob/main/CHANGELOG.md">Full changelog</a></p>
]]></description>
            <pubDate>Sat, 18 Apr 2026 11:21:18 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.4/Unifyl-1.0.4.dmg"
                sparkle:version="8"
                sparkle:shortVersionString="1.0.4"
                sparkle:edSignature="+m212BjltLam7C4Gqg11uc8NHdzlMk4a5JrEUEh2ecQn06IVNyTEsPEaBAe15DCd+OkVKCL/JTr3Ec//DuU6Ag=="
                length="11893178"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.3</title>
            <description><![CDATA[
                <h2>Unifyl 1.0.3</h2>
                <p>Large-scale quality release. Focus areas: data integrity, Swift 6 concurrency safety, Korean/CJK filename correctness, Total Commander keyboard compatibility, and discoverable UI.</p>
                <h3>Added</h3>
                <ul>
                <li><b>Total Commander key compatibility</b>: F2 = Rename, Shift+F5 = Duplicate in same folder, Alt+F5 = Pack, Shift+F9 = Pack (alt), Alt+F9 = Unpack, Shift+F4 = New text file + edit, Shift+F6 = Rename (alt), Ctrl+U = Swap left/right panels, Ctrl+← / Ctrl+→ = Copy path to opposite panel, Alt+Shift+Enter = Compute all folder sizes, Ctrl+\ = Go to volume root, Ctrl+B = Branch view (flatten subdirectories), Ctrl+M = Multi-rename alias, Insert = Toggle select + advance cursor.</li>
                <li><b>Volume / drive picker</b> in the toolbar — click the external drive icon to switch the active panel between any mounted volume (boot disk, USB, Thunderbolt, network). Auto-refreshes on mount/unmount/rename. Shows per-volume free space and removable/ejectable flags.</li>
                <li><b>Bottom terminal toggle button</b> in the function-key bar (teal pill, right side) so users discover Cmd+Opt+T without memorising the shortcut.</li>
                <li><b>Toolbar status center</b>: replaces the static "Unifyl" label with product name + version badge + Free/Pro tier capsule + active-panel selection/free-space summary. No more wasted toolbar space.</li>
                <li>HWP / HWPX (Korean Hangul Word Processor) to PDF conversion via LibreOffice's built-in HWP import filter.</li>
                </ul>
                <h3>Changed</h3>
                <ul>
                <li><b>Pro price</b>: $49.99 → <b>$39.99</b>. Landing page, in-app upgrade sheet, and localized strings updated.</li>
                <li>Incremental backup: failed files are now tracked individually with reason, shown in a "View failed…" sheet with copy-to-clipboard and Retry Failed button. Previously only a count was surfaced.</li>
                <li><code>SecurityBookmarkManager.saveBookmark</code> is now <code>@discardableResult Bool</code>; load/save path logs real failures via <code>OSLog</code> instead of silently returning <code>nil</code>.</li>
                <li>Git panel: shell errors capture stderr and log via <code>UnifylLogger.git</code> so an empty Git panel can be distinguished from a corrupt repo or missing <code>git</code> binary.</li>
                <li>OAuth token storage: decode failures (e.g. after a schema change) delete the corrupt entry so the user gets a clean re-sign-in prompt rather than an infinite retry loop.</li>
                <li>PDF conversion error message now names the likely cause for HWP failures (LibreOffice branch, missing filter) and points users at Console.app for the underlying LibreOffice stderr.</li>
                <li>Sparkle auto-update failures now show a targeted alert explaining how to enable "App Management" in System Settings, with a one-click "Open Settings" button.</li>
                <li>First launch: one-time prompt guides users to enable "App Management" for auto-updates. Dismissed permanently after first interaction.</li>
                </ul>
                <h3>Fixed</h3>
                <ul>
                <li><b>Critical — data integrity</b>: Checksum verification treated compute errors as a literal <code>"Error"</code> hash string, so typing <code>"error"</code> into the verify field falsely matched. Errors now live in a separate dictionary and verify no longer compares against failure strings.</li>
                <li><b>Critical — mass delete risk</b>: <code>DirectorySyncEngine</code> and <code>DiffEngine</code> silently swallowed <code>contentsOfDirectory</code> errors via <code>try? … ?? []</code>, meaning a permission-denied folder looked empty and generated a mass-delete plan under one-way sync. Both engines now throw on unreadable directories.</li>
                <li><b>Critical — silent move failures</b>: AI Organization "Apply recommendation" used <code>try? fm.moveItem(…)</code>, so failed moves vanished while the user saw "organized". Failures are now collected per-recommendation, surfaced in the UI, and the recommendation is kept for retry.</li>
                <li><b>Critical — duplicate false positives</b>: Vision FeaturePrint failure in the Duplicate Detector was being treated as <code>distance = 0</code>, causing unrelated images to be flagged identical. Errors now return <code>nil</code> and the pair is skipped.</li>
                <li><b>Critical — FSEvents use-after-free</b>: <code>PluginFSEventsWatcher</code> passed <code>passUnretained(self)</code> to the C callback; a callback firing after <code>deinit</code> dereferenced freed memory. Switched to <code>passRetained</code> + explicit <code>release()</code> in <code>stop()</code>.</li>
                <li><b>Critical — NSFileCoordinator race</b>: <code>FileOperationManager</code> called <code>coordinator.cancel()</code> concurrently from three tasks. Serialized through a new <code>CoordinatorBox</code> with NSLock; watchdog and timeout merged into one task.</li>
                <li><b>Critical — Enterprise audit log compliance</b>: <code>AuditLogger</code> silently swallowed persistence failures (a SOX/GDPR incident). Now emits <code>Logger.fault</code> and the load path distinguishes "first run" from "decode failed".</li>
                <li><b>Korean filename corruption on cloud</b>: APFS stores CJK filenames in NFD (decomposed) but S3, Dropbox, OneDrive, WebDAV, SMB, SFTP, FTP, and Google Drive all store NFC. Added <code>String.asRemoteFilename</code> normalization to every remote adapter — Korean/Japanese/Chinese filenames now round-trip correctly.</li>
                <li><b>Legacy CJK text preview</b>: Text viewer and AI content extractor only tried UTF-8 and isoLatin1, turning EUC-KR/CP949/Shift-JIS/GB18030/Big5 files into mojibake. Added <code>TextEncodingDetection</code> with BOM sniffing + NSString statistical guess + CJK fallback chain.</li>
                <li>LogViewer DispatchSource race: event handler could read a just-closed FileHandle (previously band-aided with a 50 ms sleep). Replaced with <code>setCancelHandler</code> for deterministic close-after-cancel.</li>
                <li>Cross-volume move rollback: if removing the source AND cleaning up the partial copy both failed, the original error was rethrown and the user never heard about the duplicate file. Now throws <code>CrossVolumeMoveRollbackFailed</code> with both paths.</li>
                <li>S3 <code>deleteDirectory</code>: <code>try? deleteObject(markerKey)</code> silently hid real errors. Marker-not-found is now expected and benign; other errors propagate. Added <code>ObjectStorageError.invalidURL</code> case.</li>
                <li><code>VectorIndex</code> batch upsert: silent <code>try? ROLLBACK</code> on failure left the DB with an open transaction that locked all subsequent writes. Now logs <code>Logger.fault</code> so the caller can reset the connection.</li>
                <li><code>AppViewModel</code> auto-dismiss and cloud-download tasks are now stored as <code>Task</code> properties and cancelled before replacement, preventing stale timers from clearing newer notifications.</li>
                <li><code>AudioMetadataView</code>: leftover <code>.bak.<ext></code> file after successful metadata save is now logged (was silently ignored, leaving ghost files in the user's folder).</li>
                <li><code>DateFormatter</code> instances in 9 UI call sites now set <code>.locale = .autoupdatingCurrent</code>; filename/CSV export formatters explicitly pin <code>en_US_POSIX</code> for stability.</li>
                <li><code>NSAlert</code> hardcoded button titles ("OK", "Cancel", "Replace", "Go", "Open Download Page") now localized via <code>NSLocalizedString</code>.</li>
                <li><code>URL(string: …)!</code> force-unwraps (11 sites) replaced with <code>StaticURL.make(…)</code> that crashes loudly at file/line on malformed input.</li>
                <li><code>SyntaxHighlighter</code> <code>try!</code> regex compilation now goes through a <code>makeRegex</code> helper with precise error reporting.</li>
                <li>Workspace layouts, keybindings, macros, automations, and OAuth tokens: persistence failures are logged via appropriate <code>UnifylLogger</code> category (was previously silent — settings/macros would vanish between launches with no explanation).</li>
                </ul>
                <h3>Developer</h3>
                <ul>
                <li>DEBUG builds bypass the real login keychain and use <code>~/Library/Application Support/<bundle-id>/debug-keychain.json</code> instead. This eliminates the "Unifyl wants to access key 'Unifyl'" password prompt that appeared on every rebuild because ad-hoc code signatures change between builds. Release builds are unchanged.</li>
                </ul>
                <h3>Added (respin)</h3>
                <ul>
                <li><b>Terminal pin / follow mode</b>: every terminal session defaults to 🔒 pinned (stays in its original directory — safer for in-flight commands). Click the lock icon in the prompt bar to switch to ⇆ follow mode, where the terminal's current directory tracks the active panel (subfolder navigation, "..", Back/Forward, Tab panel switch, direct panel click). Per-tab state so you can mix pinned and follow sessions.</li>
                </ul>
                <h3>Fixed (respin)</h3>
                <ul>
                <li>Bottom terminal output lag: <code>pwd</code>, <code>ls</code>, etc. used to appear only after the NEXT command was typed — <code>Process.terminationHandler</code> blocked on <code>readDataToEndOfFile()</code> until something else unblocked the OS pipe. Replaced with incremental <code>readabilityHandler</code> draining + non-blocking <code>availableData</code> on termination. Output now appears instantly.</li>
                <li>Panel click regression introduced by the initial terminal-sync observer: <code>.onChange(of: activePanel)</code> attached directly to <code>MainWindowView</code> made the whole view tree depend on both panels' paths, causing the bridged <code>NSTableView</code> inside <code>FileTableView</code> to be invalidated mid-click and dropping the click. Observers relocated to a zero-size helper view (<code>TerminalSyncObservers</code>) so they no longer pollute the main dependency graph.</li>
                </ul>
                <h3>Fixed (respin build 4)</h3>
                <ul>
                <li><b>Critical — auto-update configuration lost</b>: the initial 1.0.3 build (build 3) shipped without <code>SUFeedURL</code> and <code>SUPublicEDKey</code> in its <code>Info.plist</code> because <code>project.yml</code> contained a duplicate orphaned <code>info:</code> block that clobbered the Sparkle settings during <code>xcodegen generate</code>. Users who installed build 3 would never have received any future auto-updates. <b>If you are on build 3, please reinstall from the replaced DMG to regain auto-update capability.</b></li>
                <li>Release pipeline now stapler-staples the <code>.app</code> bundle itself (in addition to the DMG) so first-launch Gatekeeper validation works offline.</li>
                </ul>
            ]]></description>
            <pubDate>Fri, 17 Apr 2026 10:38:44 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.3/Unifyl-1.0.3.dmg"
                sparkle:version="4"
                sparkle:shortVersionString="1.0.3"
                sparkle:edSignature="tljDUDZzyATUvfU7CJvF04QnXGsFXeAOHM+DsUPsQ9Q99iy/nKdSBM83aNNtloG+/TxEOP9y0TW3+OQqJlSNAQ=="
                length="11806639"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.2</title>
            <description><![CDATA[
                <h2>Unifyl 1.0.2</h2>
                <h3>Fixed</h3>
                <ul>
                <li><b>Critical</b>: Sparkle auto-update failed with "An error occurred while launching the installer" because the release re-signing step stripped Sparkle's internal entitlements (Autoupdate's <code>com.apple.application-identifier</code>, XPC-services' empty-dict entitlements). The release script now passes <code>--preserve-metadata=entitlements,flags</code> when signing every nested Sparkle binary. <b>Users on 1.0.1 must download 1.0.2 manually</b> — their existing Sparkle install can't apply updates.</li>
                </ul>
            ]]></description>
            <pubDate>Thu, 16 Apr 2026 08:04:30 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.2/Unifyl-1.0.2.dmg"
                sparkle:version="3"
                sparkle:shortVersionString="1.0.2"
                sparkle:edSignature="6ArWqlHhnwA+PonDHWwx724x6GN1cWWVJNaBUsDUINxG2M47tt7MRtLiBQHDcT2ArECPSyelYwKBb/PPzgwdAg=="
                length="11664763"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.1</title>
            <description><![CDATA[
                <h2>Unifyl 1.0.1 — April 15, 2026</h2>
                <h3>⭐ Korean / Japanese archive support</h3>
                <ul>
                    <li><b>Fixed:</b> Extracting ZIPs with CP949/EUC-KR/Shift-JIS filenames now works. Previously the archive opened but every file failed with "caution: filename not matched".</li>
                </ul>
                <h3>Thumbnail grid now matches list view (⌃G)</h3>
                <ul>
                    <li>Shift/Cmd multi-select and rubber-band drag-select</li>
                    <li>Drag multiple files to Finder or the other pane at once</li>
                    <li>Option-to-copy on drop with drop-highlight overlay</li>
                </ul>
                <h3>Stability &amp; Performance</h3>
                <ul>
                    <li>File move falls back to copy+remove across volumes (EXDEV) — no more failures when copying to an external USB</li>
                    <li>Spotlight searches no longer stall the UI on thousand-result hits</li>
                    <li>Duplicate scan runs pair scoring in parallel (TaskGroup per size bucket)</li>
                    <li>Canceling a media conversion now terminates ffmpeg and removes the partial output</li>
                    <li>License and WebDAV calls have explicit timeouts</li>
                </ul>
                <h3>Security hardening</h3>
                <ul>
                    <li>SFTP / FTP / WebDAV refuse remote paths with C0 control characters — blocks shell/HTTP-header injection via crafted filenames</li>
                    <li>Temp credential files (askpass, .netrc) use <code>O_CREAT|O_EXCL|O_NOFOLLOW</code> against symlink attacks</li>
                    <li>Zip-slip boundary check tightened</li>
                </ul>
                <h3>Silent-failure fixes</h3>
                <ul>
                    <li>Natural-language commands, "Keep Best" duplicate removal, hardlink replacement, PDF Replace, LibreOffice conversion — every path that used <code>try?</code> now surfaces errors</li>
                </ul>
                <h3>Accessibility &amp; localization</h3>
                <ul>
                    <li>File permissions dialog uses status icons + color (not color-only)</li>
                    <li>Regex-rename match highlight legible in dark mode</li>
                    <li>File date column follows the user's system locale</li>
                </ul>
                <p><a href="https://github.com/goodbug89/Unifyl.app/blob/main/CHANGELOG.md">Full changelog</a></p>
            ]]></description>
            <pubDate>Wed, 15 Apr 2026 12:00:00 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.1/Unifyl-1.0.1.dmg"
                sparkle:version="2"
                sparkle:shortVersionString="1.0.1"
                sparkle:edSignature="091JAdAxzdz9Y994UAVTglBGo9oY0bIzP5mjP/vsLQAxu/eapTU7Jdv3qXyJVosVbLgrTxhvSKJ+z90btmVFCQ=="
                length="11666809"
                type="application/octet-stream"
            />
        </item>
        <item>
            <title>Version 1.0.0</title>
            <description><![CDATA[
                <h2>Unifyl 1.0.0 — Initial Release</h2>
                <ul>
                    <li>Dual-pane file management with tabs and workspaces</li>
                    <li>AI-powered semantic search, smart tagging, and AI rename</li>
                    <li>Archive virtual folders (ZIP, 7z, TAR, RAR, and more)</li>
                    <li>10 built-in viewers (text, hex, image, PDF, video, audio, web, markdown, code, media info)</li>
                    <li>Advanced search with Smart Folders</li>
                    <li>File compare, directory sync, and 3-way merge</li>
                    <li>Git integration, inline terminal, and developer tools</li>
                    <li>12 themes, SVG icon packs, and 120+ custom keyboard shortcuts</li>
                    <li>Cloud and remote connections (FTP, SFTP, WebDAV, S3, Google Drive, Dropbox, OneDrive)</li>
                    <li>Full VoiceOver accessibility support</li>
                </ul>
            ]]></description>
            <pubDate>Sun, 13 Apr 2026 00:00:00 +0900</pubDate>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <enclosure
                url="https://github.com/goodbug89/Unifyl.app/releases/download/v1.0.0/Unifyl-1.0.0.dmg"
                sparkle:version="1"
                sparkle:shortVersionString="1.0.0"
                sparkle:edSignature="Go7Nxml8Z8vA6CLpdXpSRTptE2GPDuTr1ODJK60J+fgVwZWdLy5pxVI3olrzKeEtanw0rJnQLgyZWsHzM42YCA=="
                length="11578548"
                type="application/octet-stream"
            />
        </item>
    </channel>
</rss>
